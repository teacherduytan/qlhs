# Theo dõi tiến độ — Giai đoạn 1

Cập nhật file này sau **mỗi commit hoàn thành** (xem quy trình ở [05-quy-tac-ai-agent.md](05-quy-tac-ai-agent.md)). Danh sách commit đã cập nhật ngày 11/07/2026 theo tài liệu 04 (bản 28 commit, gồm cả C021a).

## Nhóm A — Chuẩn bị dữ liệu (deadline: Thứ Hai 13/07/2026)

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C001 | ✅ Xong | 11/07/2026 |  |
| C002 | ✅ Xong | 11/07/2026 |  |
| C003 | ✅ Xong | 11/07/2026 | Script SetupSheet.gs + seed DanhMucDiem |
| C004 | ✅ Xong | 11/07/2026 | 36 HS + 2 tuần trong du-lieu-mau/ |
| C005 | ✅ Xong | 11/07/2026 | Phiếu 3 phần + bảng tra cứu mã |
| C006 | ✅ Xong | 11/07/2026 | huong-dan-quy-trinh-import.md |

## Nhóm B — Khung sườn ứng dụng web

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C007 | ✅ Xong | 11/07/2026 | Vite + React + TS strict |
| C008 | ✅ Xong | 11/07/2026 | Tailwind v4 + ResponsiveDemo |
| C009 | ✅ Xong | 11/07/2026 | GitHub Actions deploy-pages |
| C010 | ✅ Xong | 11/07/2026 | HashRouter + Layout |
| C011 | ✅ Xong | 11/07/2026 | DataSource + types |
| C012 | ✅ Xong | 11/07/2026 | Code.gs doGet/doPost |
| C013 | ✅ Xong | 11/07/2026 | Import batch + Drive + log |

## Nhóm C — Tính năng cốt lõi

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C014 | ✅ Xong | 11/07/2026 | GoogleSheetsDataSource đọc dữ liệu + trang học sinh kiểm chứng getStudents |
| C015 | ✅ Xong | 11/07/2026 | Trang học sinh có tìm kiếm + thêm/sửa/xoá qua GoogleSheetsDataSource |
| C016 | ✅ Xong | 11/07/2026 | Màn hình Import JSON chung: chọn loại, dán/tải file, preview, xác nhận import |
| C017 | ✅ Xong | 11/07/2026 | Hồ sơ theo token + vai trò BanCanSu, không hiển thị SĐT phụ huynh |
| C018 | ✅ Xong | 11/07/2026 | Hồ sơ hiển thị lịch sử GhiNhan theo học sinh, sắp xếp mới nhất và gom theo tuần |
| C019 | ✅ Xong | 11/07/2026 | Logic tính điểm thi đua: 4 thành phần, học tập, tổng hợp chia 6, bỏ qua tập thể/tổ trực — ~~⚠️ có lỗi 66,67~~ đã sửa ở C031 |
| C020 | ✅ Xong | 11/07/2026 | Hồ sơ hiển thị điểm thi đua, điểm học tập, tổng hợp và xếp loại |
| C021 | ✅ Xong | 11/07/2026 | Dashboard giáo viên: điểm thi đua, cảnh báo, sự kiện tập thể/tổ trực |
| C021a | ✅ Xong | 11/07/2026 | Dashboard xử lý sự kiện tập thể: gán 1 HS, áp dụng lớp/tổ, bỏ qua |
| C022 | ✅ Xong | 11/07/2026 | Gợi ý xử lý sư phạm rule-based trên dashboard |
| C023 | ✅ Xong | 11/07/2026 | Tối ưu hồ sơ di động với khối ghi nhận hôm nay nổi bật |
| C024 | ✅ Xong | 11/07/2026 | Nút tải mẫu phiếu ghi nhận HTML in được từ web app |

## Nhóm D — Hoàn thiện & bảo mật

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C025 | ✅ Xong | 11/07/2026 | Ẩn SĐT khỏi giao diện và response hồ sơ public theo token |
| C026 | ✅ Xong | 11/07/2026 | Rà responsive: header/nav tối ưu mobile và laptop |
| C027 | ✅ Xong | 11/07/2026 | README hướng dẫn sử dụng + tag v0.1.0-phase1 |

---

## Cải tiến sau triển khai (xem chi tiết ở [06-cai-tien-sau-trien-khai.md](06-cai-tien-sau-trien-khai.md))

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C028 | ✅ Xong | 11/07/2026 | Cơ chế write_secret ban đầu cho doPost; đã được thay thế bởi đăng nhập giáo viên + session token ở C060 |
| C029 | ✅ Xong | 11/07/2026 | README/hướng dẫn deploy ghi rõ Execute as Me, Anyone, Manage deployments; phần bảo mật đã cập nhật lại theo C060 |
| C030 | ✅ Xong | 11/07/2026 | Danh sách học sinh mở rộng accordion, xem/copy link hồ sơ |
| C031 | ✅ Xong | 11/07/2026 | Chưa có điểm học tập thì chia 4 thành phần, không còn mặc định 66,67 |
| C032 | ✅ Xong | 11/07/2026 | Thêm trường to cho HocSinh, hiển thị tổ trên danh sách/hồ sơ và xử lý tổ trực |
| C033 | ✅ Xong | 11/07/2026 | Dashboard có nhật ký theo ngày trong tuần, kể cả ngày chưa có ghi nhận |
| C034 | ✅ Xong | 11/07/2026 | Import GhiNhan tự khớp ho_ten sang ma_hs, tự điền tuần/diện/điểm và log lỗi từng dòng |
| C035 | ✅ Xong | 11/07/2026 | Thêm mẫu JSON và prompt chuyển ảnh phiếu sang JSON vào du-lieu-mau |
| C036 | ✅ Xong | 11/07/2026 | Xoá dữ liệu theo lần import: nút xoá trên lịch sử import, Apps Script xoá GhiNhan theo ma_log_import và đánh dấu log da_xoa |
| C037 | ✅ Xong | 11/07/2026 | Bộ chọn tuần dùng chung cho Dashboard và hồ sơ học sinh, mặc định chọn theo CauHinhTuan |
| C038 | ✅ Xong | 11/07/2026 | Hồ sơ mặc định hiển thị toàn bộ lịch sử, có bộ lọc tuần đang chọn và ngày cụ thể |
| C039 | ✅ Xong | 11/07/2026 | Apps Script tự tính tuan_so khi import, cảnh báo nếu ngoài CauHinhTuan và thêm vaLaiTuanSoChoGhiNhan() để vá dữ liệu cũ |
| C040 | ✅ Xong | 11/07/2026 | Thêm bộ chọn ngày cụ thể cho Nhật ký theo ngày và lịch sử ghi nhận hồ sơ |
| C041 | ✅ Xong | 11/07/2026 | Xác minh danh sách học sinh; thêm nút Chi tiết/Thu gọn rõ ràng cho từng dòng |
| C042 | ✅ Xong | 11/07/2026 | Xác minh màn Import bằng Chrome headless: có Lịch sử import và nút Xoá dữ liệu của lần này |
| C043 | ✅ Xong | 11/07/2026 | Dashboard có vùng thống kê tổng quan theo tài liệu 07, chia nhóm hành động ngay và quan sát chung |
| C044 | ✅ Xong | 11/07/2026 | Thêm nút thu gọn/mở rộng cả khối danh sách trên Dashboard và trang Học sinh |
| C045 | ✅ Xong | 11/07/2026 | Drill-down cho các thẻ TK02-TK05 ngay trên Dashboard, mở đúng danh sách chi tiết cấu thành |
| C046 | ✅ Xong | 11/07/2026 | Bấm sự kiện tổ trực mở Thông tin Tổ: tổ trưởng, danh sách học sinh và lịch sử nhắc tổ gần đây |
| C047 | ✅ Xong | 11/07/2026 | Thêm helper màu chung và áp dụng cho Dashboard, hồ sơ, lịch sử ghi nhận, nhật ký ngày, danh sách học sinh |
| C048 | ✅ Xong | 11/07/2026 | Sửa ngưỡng xếp loại theo trạng thái có/chưa có điểm học tập, thêm ghi chú so sánh điểm và dữ liệu đối chiếu cá nhân |
| C049 | ✅ Xong | 12/07/2026 | Thêm khu vực Xem theo Nhóm vi phạm trên Dashboard, có chọn nhóm, hiện đủ/ẩn học sinh sạch và TK06 nhảy tới nhóm Kỷ luật |
| C050 | ✅ Xong | 12/07/2026 | Mở rộng điều hướng TK01/TK06/TK07/TK08 và thêm popup tra cứu mã cho badge ở Dashboard/Hồ sơ |
| C051 | ✅ Xong | 12/07/2026 | Thêm loai_tuan và script taoCauHinhTuanNamHoc() sinh cấu hình tuần cả năm, đánh dấu tuần nghỉ |
| C052 | ✅ Xong | 12/07/2026 | Bộ chọn tuần có nút trước/sau, chọn nhanh nhóm theo tháng/học kỳ và bỏ qua tuần nghỉ |
| C053 | ✅ Xong | 12/07/2026 | Drill-down TK hiển thị bằng modal overlay, đóng bằng X/nền tối/Esc |
| C054 | ✅ Xong | 12/07/2026 | Bấm lại nhóm đang chọn sẽ bỏ lọc và quay về danh sách tổng mặc định |
| C055 | ✅ Xong | 12/07/2026 | Thêm STT cho bảng điểm Dashboard, Xem theo Nhóm và các danh sách học sinh trong modal TK |
| C056 | ✅ Xong | 12/07/2026 | Thêm route giáo viên /quan-ly/hoc-sinh/:maHs có SĐT, tel:, edit trực tiếp; link danh sách trỏ đúng route mới |
| C056a | ✅ Xong | 12/07/2026 | Sửa lỗi SĐT từ Sheet trả về dạng số làm trang chi tiết giáo viên crash khi bấm Xem hồ sơ đầy đủ |
| C057 | ✅ Xong | 12/07/2026 | Mẫu phiếu in trong app và docs/mau-phieu-ghi-nhan.md chuyển sang 1 bảng duy nhất, không mã |
| C058 | ✅ Xong | 12/07/2026 | Prompt AI suy luận mã/phạm vi từ phiếu tự do, map thành tích sang KT và bổ sung KT01-KT05 vào seed DanhMucDiem |
| C059 | ✅ Xong | 12/07/2026 | Cập nhật Vite base path thành /qlhs/ cho GitHub Pages repo teacherduytan/qlhs |
| C059a | ✅ Xong | 12/07/2026 | Hoàn thiện CI/CD GitHub Pages: deploy bằng Actions, tránh trùng artifact và đọc VITE_APPS_SCRIPT_URL từ GitHub Secrets |
| C060 | ✅ Xong | 13/07/2026 | Thêm đăng nhập giáo viên thật qua Apps Script, session token lưu sessionStorage, bỏ VITE write_secret khỏi bundle, ẩn lối điều hướng từ hồ sơ học sinh sang vùng giáo viên |
| C061 | ✅ Xong | 13/07/2026 | Sửa prompt tách điểm số thành dòng hoc_tap riêng; import cảnh báo/bỏ qua diem_so_mon sai loại và Điểm học tập chỉ đọc loai=hoc_tap |
| C062 | ✅ Xong | 13/07/2026 | Đưa ghi nhận của học sinh thành nội dung nổi bật đầu trang hồ sơ public; thông tin cá nhân chuyển vào tab phụ |
| C063 | ✅ Xong | 13/07/2026 | Tách khen thưởng/KT khỏi Vi phạm phổ biến và thêm ghi nhận tích cực trên Dashboard |
| C064 | ✅ Xong | 13/07/2026 | Phân màu nền/viền các section Dashboard và hồ sơ học sinh theo khối nội dung |
| C065 | ✅ Xong | 13/07/2026 | Lọc/sắp xếp bảng, tổng tích cực/tiêu cực, đếm lặp vi phạm và gợi ý xử lý |
| C066 | ✅ Xong | 13/07/2026 | Thêm điều hướng nhanh tới section và nút thu gọn/mở rộng từng section |
| C067 | ✅ Xong | 13/07/2026 | TK05 đếm/mở đầy đủ danh sách vi phạm và đổi nhãn tiêu cực thành vi phạm |
| C068 | ✅ Xong | 13/07/2026 | Đưa vi phạm học tập tự do như quên dụng cụ vào Xem theo Nhóm vi phạm |
| C069 | ✅ Xong | 13/07/2026 | Xem theo Nhóm vi phạm hiển thị nội dung vi phạm thay vì chỉ mã |
| C070 | ✅ Xong | 13/07/2026 | Phân loại Tích cực/Vi phạm ngay trong cột Nội dung vi phạm/ghi nhận |
| C071 | ✅ Xong | 13/07/2026 | Thêm trang quản lý danh mục vi phạm/tích cực và CRUD tab DanhMucDiem |
| C072 | ✅ Xong | 13/07/2026 | Gắn ghi nhận vi phạm/tích cực lên học sinh theo cá nhân, tổ hoặc cả lớp |
| C073 | ✅ Xong | 13/07/2026 | Đồng bộ logic ghi nhận tay với import JSON và chỉ gắn danh mục cá nhân lên học sinh |
| C074 | ✅ Xong | 13/07/2026 | Liên kết nội dung Xem theo nhóm vi phạm với DanhMucDiem |
| C075 | ✅ Xong | 14/07/2026 | Xoá ghi nhận/vi phạm trong hồ sơ giáo viên của học sinh |
| C076 | ✅ Xong | 14/07/2026 | Báo rõ lỗi xoá ghi nhận khi Apps Script chưa deploy action delete_record |
| C077 | ✅ Xong | 14/07/2026 | Thêm health check Apps Script và lỗi xoá ghi nhận chi tiết hơn |
| C078 | ✅ Xong | 14/07/2026 | Chuyển đăng nhập/kiểm tra phiên giáo viên sang GET để tránh Failed to fetch |
| C079 | ✅ Xong | 14/07/2026 | Cập nhật Web App URL Apps Script hiện hành và hướng dẫn đồng bộ endpoint |
| C080 | ✅ Xong | 15/07/2026 | Xoá học sinh/ghi nhận khỏi danh mục theo chiều ngược và hỗ trợ xoá nhiều dòng |
| C081 | ✅ Xong | 15/07/2026 | Đổi danh sách học sinh theo danh mục sang modal thay vì section dưới bảng |
| C082 | ✅ Xong | 15/07/2026 | Rà và ép import GhiNhan khớp DanhMucDiem động, cảnh báo mã chưa có |
| C083 | ✅ Xong | 15/07/2026 | Rà soát dữ liệu mẫu/dữ liệu giả, cập nhật prompt AI theo import danh mục động |
| C084 | ✅ Xong | 15/07/2026 | Cập nhật prompt AI: đối chiếu DanhMucDiem hiện hành và đề xuất danh mục mới từ mô tả thô |
| C085 | ✅ Xong | 15/07/2026 | Tạo nhanh danh mục từ đề xuất AI ngay trong màn hình Import |
| C086 | ✅ Xong | 15/07/2026 | Chuẩn hoá luồng tạo mới bằng modal và wizard |
| C087 | ✅ Xong | 15/07/2026 | Tự sinh mã danh mục không trùng theo nhóm |
| C088 | ✅ Xong | 15/07/2026 | Cập nhật prompt: cung cấp danh sách học sinh để AI khớp tên viết tay/viết tắt |
| C089 | ✅ Xong | 15/07/2026 | Cập nhật prompt: AI đọc ngữ cảnh phiếu viết tay không đúng cấu trúc |
| C090 | ✅ Xong | 15/07/2026 | Cập nhật prompt: bắt AI tự kiểm tra mã danh mục và mã học sinh trước khi xuất JSON |
| C091 | ✅ Xong | 15/07/2026 | Đồng bộ bản prompt cũ trong docs với bản chuẩn ở du-lieu-mau để tránh lấy nhầm |
| C092 | ✅ Xong | 15/07/2026 | Làm rõ cách import xử lý dòng JSON có ma_hs null |
| C093 | ✅ Xong | 15/07/2026 | Màn Import tự xử lý dòng ma_hs null bằng gắn/tạo học sinh với mã tự sinh; prompt không bắt AI tự quyết ma_hs |
| C094 | ✅ Xong | 15/07/2026 | Màn Import tạo danh mục thiếu từ mã JSON và cho import trước các dòng đủ điều kiện |
| C095 | ✅ Xong | 15/07/2026 | Sửa hồi quy C094 để logic xử lý ma_hs null của C093 vẫn nhận diện dòng cá nhân theo ma_hs/ho_ten |
| C096 | ✅ Xong | 15/07/2026 | Sửa nhãn hồ sơ/chi tiết học sinh để mã KT hoặc điểm dương hiển thị là Tích cực/thành tích |
| C097 | ✅ Xong | 15/07/2026 | Đồng bộ giao diện chi tiết học sinh của giáo viên với hồ sơ học sinh |
| C098 | ✅ Xong | 15/07/2026 | Làm rõ màu sắc tích cực và vi phạm trên trang quản lý học sinh |
| C099 | ✅ Xong | 15/07/2026 | Đổ màu toàn bộ dòng học sinh theo tích cực/vi phạm trong danh sách quản lý |
| C100 | ✅ Xong | 15/07/2026 | Đổ màu tích cực/vi phạm trong trang chi tiết học sinh |
| C101 | ✅ Xong | 15/07/2026 | Cập nhật prompt để không dùng mã NN08/NN09 cho lỗi không mang dụng cụ học tập |
| C102 | ✅ Xong | 15/07/2026 | Import cho so sánh đề xuất danh mục mới với danh mục cũ và chọn dùng mã có sẵn |
| C103 | ✅ Xong | 15/07/2026 | Bổ sung mô tả và đề xuất xử lý/phạt cho danh mục vi phạm/tích cực |
| C104 | ✅ Xong | 15/07/2026 | Cập nhật prompt AI để đề xuất danh mục mới có mô tả và đề xuất xử lý |
| C105 | ✅ Xong | 15/07/2026 | Tạo danh mục mã đề xuất xử lý/phạt và liên kết từ danh mục điểm/import |
| C106 | ✅ Xong | 15/07/2026 | Báo lỗi rõ khi Apps Script chưa deploy action DanhMucXuLy |
| C107 | ✅ Xong | 15/07/2026 | Chặn tạo mã xử lý/phạt trùng nội dung khi người dùng bấm lặp |
| C108 | ✅ Xong | 16/07/2026 | Sửa luồng import từng phần để tiếp tục xử lý các dòng còn lại |
| C109 | ✅ Xong | 16/07/2026 | Tự gợi ý/gắn danh mục cho dòng import thiếu ma_danh_muc |
| C110 | ✅ Xong | 16/07/2026 | Chặn spam bấm import gây tạo dư dữ liệu |
| C111 | ✅ Xong | 16/07/2026 | Cập nhật prompt để AI không đưa ghi chú đối chiếu DanhMucDiem vào nội dung |
| C112 | ✅ Xong | 16/07/2026 | Cập nhật prompt bằng ngữ cảnh DanhMucDiem và logic import hiện tại |
| C113 | ✅ Xong | 16/07/2026 | Thêm unit test cho logic so khớp DanhMucDiem khi import |
| C114 | ✅ Xong | 17/07/2026 | Rà soát nút Tải phiếu mẫu sau các thay đổi import/danh mục |
| C115 | ✅ Xong | 17/07/2026 | Chuyển phiếu ghi nhận sang dạng ghi theo nội dung, nhiều học sinh chung một dòng |
| C116 | ✅ Xong | 20/07/2026 | Xây dựng tính năng Báo cáo sĩ số từ spec |
| C117 | ✅ Xong | 20/07/2026 | Thiết kế lại bộ lọc thời gian Dashboard và tự mở rộng CauHinhTuan theo ngày thực tế |
| C118 | ✅ Xong | 22/07/2026 | Tạo migration Supabase theo schema thật đã đối chiếu từ Apps Script và docs |
| C119 | ✅ Xong | 22/07/2026 | Chuyển đăng nhập giáo viên sang Supabase Auth và gửi access token cho Apps Script |
| C120 | ✅ Xong | 23/07/2026 | Chuyển lớp đọc/ghi dữ liệu chính sang SupabaseDataSource; Apps Script chỉ còn bridge cho hồ sơ công khai và báo cáo sĩ số |
| C121 | ⛔ Bị chặn (cần xác nhận) | 23/07/2026 | Export/import dữ liệu thật sang Supabase: enum sạch, 36 học sinh và 29 ghi nhận đã nhập; còn lệch DanhMucXuLy do 3 dòng trùng unique và chưa kiểm UI đăng nhập thật |
| C122 | ✅ Xong | 23/07/2026 | Bỏ unique sai trên DanhMucXuLy.noi_dung_xu_ly và import đủ 7/7 mã xử lý; NN09 vẫn tham chiếu XL05 |
| C123 | ✅ Xong | 23/07/2026 | Chuyển hồ sơ học sinh public sang RPC Supabase SECURITY DEFINER theo token, không mở select anon trực tiếp |
| C124 | ✅ Xong | 23/07/2026 | Siết RPC hồ sơ public: bỏ to_jsonb, liệt kê rõ cột public cho records/catalog/ban_can_su/week_config |
| C125 | ✅ Xong | 23/07/2026 | Ghi nhận cách sinh token hồ sơ public hiện tại và khuyến nghị nâng độ dài token ở bước sau |
| C126 | ✅ Xong | 23/07/2026 | Cập nhật GitHub Pages workflow để truyền VITE_SUPABASE_URL và VITE_SUPABASE_ANON_KEY vào bước build |
| C127 | ✅ Xong | 23/07/2026 | Tạo schema điểm danh Supabase, seed nhóm điểm danh và chuyển báo cáo sĩ số chính khóa sang RPC tinh_bao_cao_si_so |
| C128 | ✅ Xong | 23/07/2026 | Import CSV điểm danh thật 4 tuần: 22 ngoại lệ vào diem_danh và 22 dòng liên lạc phụ huynh; RPC sĩ số 09/07/2026 buổi sáng trả đúng danh sách vắng |
| C129 | ⛔ Bị chặn (cần xác nhận) | 23/07/2026 | Chưa tự bấm UI trang Sĩ số bằng trình duyệt tự động vì .env không có tài khoản/session giáo viên Supabase; đã kiểm DB/RPC là nguồn dữ liệu frontend sẽ gọi |
| C130 | ✅ Xong | 24/07/2026 | Tạo trang Điểm danh giáo viên chính khóa: số liệu ngày/tuần/tháng, lưới tuần, sửa vắng/trễ qua RPC upsert_diem_danh và ghi liên lạc phụ huynh |
| C131 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Chưa tự bấm UI Điểm danh bằng trình duyệt đăng nhập thật vì .env không có tài khoản/session giáo viên Supabase; đã kiểm build, test và DB rollback |
| C132 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Chuyển tạo link Google Form điểm danh từ Apps Script sang RPC Supabase `tao_link_form_diem_danh`; app không còn phụ thuộc Apps Script cho bất kỳ thao tác nào. Cần giáo viên tự INSERT dòng cấu hình thật (form_base_url, form_password, entries) vào bảng `cau_hinh_form_diem_danh` vì chỉ họ có các giá trị bí mật đang lưu trong Apps Script Script Properties |
| C133 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Rà lại giao diện Điểm danh/Sĩ số trên điện thoại: modal sửa điểm danh tự cuộn, thu hẹp cột lưới tuần, sửa khối Nội trú/Bán trú/Hai buổi bị chèn chữ; sửa lỗi Safari iOS chặn tab Google Form vì window.open() gọi sau await. Không có trình duyệt/điện thoại thật để tự bấm kiểm — cần giáo viên xác nhận trên điện thoại thật (đặc biệt Safari iOS) trước khi đánh dấu Xong |
| C134 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Sửa mất lịch sử liên lạc phụ huynh khi sửa lại điểm danh: `lien_lac_phu_huynh.diem_danh_id` đổi từ `on delete cascade` sang `on delete set null`, thêm cột chụp nhanh `ma_hs`/`ho_ten`/`ngay`/`buoi` ngay lúc ghi để lịch sử không mất khi dòng `diem_danh` gốc bị xoá lúc chuyển về Có mặt. Migration đã apply thành công (giáo viên xác nhận), nhưng chưa tự bấm UI thật: đánh dấu vắng + ghi liên lạc, chuyển lại Có mặt, kiểm tra dòng `lien_lac_phu_huynh` vẫn còn với `ma_hs`/`ngay`/`buoi` đúng |
| C135 | ✅ Xong | 24/07/2026 | Sửa trạng thái C133/C134 từ "Xong" về đúng "Bị chặn (cần xác nhận)" theo mục 6 của 05-quy-tac-ai-agent.md — chưa ai tự bấm UI thật trên điện thoại/trình duyệt để xác nhận |
| C136 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Nội dung liên lạc phụ huynh trước đây chỉ ghi một chiều, không có nơi xem lại. Thêm trang riêng "Lịch sử liên lạc" (`/lien-lac-phu-huynh`), lọc theo tên/mã học sinh và khoảng ngày, đọc từ `lien_lac_phu_huynh` (không cần migration mới, RLS đã cho `authenticated` SELECT sẵn từ C127). Chưa tự bấm UI thật để xác nhận danh sách hiển thị đúng dữ liệu thật |
| C137 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Trang cá nhân học sinh (chế độ giáo viên, `/quan-ly/hoc-sinh/:maHs`) chưa lưu/hiện thông tin liên lạc phụ huynh dù đây là nơi tổng hợp mọi thông tin của học sinh. Thêm khối "Liên lạc phụ huynh" hiện lịch sử đã liên lạc của đúng học sinh đó, kèm link sang trang Lịch sử liên lạc lọc sẵn theo mã học sinh. Cố ý KHÔNG thêm vào trang hồ sơ public `/hs/:token` vì đây là ghi chú nội bộ giáo viên, tương tự lý do SĐT phụ huynh không hiện ở trang public (tài liệu 01 mục 6). Chưa tự bấm UI thật để xác nhận |
| C138 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Rà toàn app (Dashboard, Ghi nhận, Điểm danh, Lịch sử liên lạc, Danh sách học sinh) tìm chỗ tên/mã học sinh hoặc mã danh mục hiển thị dạng chữ thường thay vì link điều hướng. Gắn `Link` sang `/quan-ly/hoc-sinh/:maHs` hoặc `/danh-muc?ma=...` cho các chỗ tìm thấy: bảng điểm thi đua, sự kiện tổ trực/tập thể, nhật ký theo ngày, Tổ trưởng trong TeamInfoPanel (Dashboard); badge danh mục trong bảng ghi nhận (RecordEntryPage); cột Mã HS (StudentsPage); lưới tuần, danh sách ngoại lệ Sáng/Chiều, danh sách "Cần liên lạc phụ huynh" (AttendanceManagementPage); tên học sinh trong Lịch sử liên lạc. Cố ý KHÔNG đổi: 2 tiêu đề modal sửa điểm danh (đang sửa đúng học sinh đó, link sẽ mất dữ liệu form dở dang); trang hồ sơ public `/hs/:token` (route giáo viên không được lộ ra public); các nút chọn danh mục dạng picker (bấm để chọn cho form, không phải hiển thị dữ liệu đã có) |
| C139 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Trang Lịch sử liên lạc (C136) chỉ mới xem/thêm, chưa sửa/xoá được nội dung đã ghi. Thêm `updateParentContact`/`deleteParentContact` vào `DataSource`/`SupabaseDataSource` (không cần migration mới, RLS `authenticated` đã cho UPDATE/DELETE từ C127); `ParentContactHistoryPage.tsx` thêm nút Sửa (mở form sửa hình thức + nội dung tại chỗ) và Xoá (có xác nhận) cho từng dòng. Chưa tự bấm UI thật để xác nhận sửa/xoá hoạt động đúng trên dữ liệu thật |
| C140 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Giáo viên đánh dấu vắng nhưng dễ quên đã gọi phụ huynh hay chưa, phải cuộn xuống khối "Cần liên lạc phụ huynh" mới biết. Thêm badge "Chưa gọi PH" (màu cam) ngay tại chỗ hiển thị bản ghi vắng/trễ: danh sách ngoại lệ Sáng/Chiều ở tab Ngày, và ô trong lưới tuần. Dùng lại đúng tập id từ `getPendingParentContacts()` (đã loại các bản ghi đã có `lien_lac_phu_huynh`, chỉ áp dụng `vang_co_phep`/`vang_khong_phep` theo đúng quy tắc cũ, không áp dụng cho `trễ`) — không cần query Supabase thêm. Chưa tự bấm UI thật để xác nhận badge hiện đúng vị trí và biến mất sau khi ghi liên lạc |
| C141 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Lưới tuần chỉ có bảng ma trận học sinh × ngày, không có cách xoá nhanh 1 lượt điểm danh nhầm, và trang thiếu 3 số liệu tổng quan Sĩ số/Hiện diện/Vắng. Theo lựa chọn của người dùng: (1) thêm bên dưới lưới tuần danh sách chi tiết **từng ngày** trong tuần, mỗi ngày 2 khối Sáng/Chiều (dùng lại `CompactDayAttendance`, lọc theo `weekEntries`); (2) thêm nút "Xoá" cạnh "Sửa" trong `SessionBlock` — xác nhận rồi chuyển thẳng về Có mặt, không cần mở modal; (3) thêm 3 thẻ **Sĩ số/Hiện diện/Vắng** tính theo **số học sinh duy nhất** (Vắng = học sinh có ≥1 lượt vắng có phép/không phép trong khoảng đang xem; Trễ không tính là vắng; Hiện diện = Sĩ số − Vắng), áp dụng chung cho cả Ngày/Tuần/Tháng vì dùng lại đúng `summaryEntries` đã có sẵn theo từng scope. Chưa tự bấm UI thật để xác nhận |
| C142 | ✅ Xong | 24/07/2026 | Thiết kế lại toàn bộ trang Điểm danh theo yêu cầu người dùng, đã hỏi lại 2 câu chốt cấu trúc trước khi code: gộp tab Ngày + Tuần (C133/C141) thành **1 công cụ điểm danh duy nhất** (chọn ngày bất kỳ bằng input ngày + dropdown Sáng/Chiều), bỏ bảng ma trận và danh sách theo từng ngày; tab Tháng giữ riêng để xem tổng quan. Danh sách học sinh giờ là **thẻ** cho từng học sinh (toàn bộ lớp, không chỉ ngoại lệ), mỗi thẻ hiện nhanh trạng thái hiện tại, số lần vắng trong tuần/tháng của đúng học sinh đó, và badge "Chưa gọi PH" nếu chưa liên lạc; đổi trạng thái ngay trên thẻ (không cần modal). **Bỏ "Trễ" khỏi lựa chọn điểm danh** (chỉ còn Có mặt/Vắng có phép/Vắng không phép) vì Trễ thuộc danh mục vi phạm ở trang Ghi nhận — bản ghi Trễ cũ vẫn hiển thị đúng ở tab Tháng vì `TrangThaiDiemDanh`/DB không đổi, chỉ ẩn khỏi các lựa chọn ghi mới. Tab Tháng liệt kê toàn bộ ngoại lệ trong tháng, mỗi dòng có nút "Sửa ở Điểm danh" nhảy thẳng sang đúng ngày/buổi đó. Xoá `EditAttendanceModal`, `CompactDayAttendance`, `SessionBlock`, `SessionBadge`, bảng ma trận tuần vì không còn cần. Chưa tự bấm UI thật để xác nhận |
| C143 | ✅ Xong | 24/07/2026 | Ngay sau C142, người dùng thấy danh sách thẻ toàn bộ 36 học sinh vẫn cồng kềnh — yêu cầu gộp lại 1 vùng: dropdown Sáng/Chiều (đã có), dropdown chọn học sinh, dropdown chọn trạng thái vắng, rồi hiện kết quả là danh sách học sinh đã điểm danh kèm trạng thái. Bỏ hẳn `StudentAttendanceCard` cho toàn bộ lớp, quay lại đúng kiểu form "Thêm nhanh" (chọn học sinh + chọn trạng thái + ô liên lạc phụ huynh khi cần) nhưng gộp về 1 vùng duy nhất thay vì 2 khối Sáng/Chiều song song như bản cũ trước C142. Bên dưới form là "Danh sách đã điểm danh" chỉ liệt kê học sinh đang có ngoại lệ cho đúng buổi đang chọn, giữ nguyên các badge Tuần vắng/Tháng vắng/Chưa gọi PH và nút Xoá nhanh. Chưa tự bấm UI thật để xác nhận |
| C144 | ✅ Xong | 24/07/2026 | "Danh sách đã điểm danh" chỉ có nút Xoá (chuyển thẳng về Có mặt), không có cách đổi sang trạng thái vắng khác hoặc sửa nội dung liên lạc phụ huynh mà không xoá rồi ghi lại từ đầu. Thêm nút "Sửa" cạnh "Xoá", mở modal `EditEntryModal` (dropdown trạng thái + ô liên lạc phụ huynh khi chọn vắng) cho đúng học sinh/buổi/ngày đang chọn, Lưu gọi lại `saveAttendance` rồi tự đóng modal. Người dùng đã tự bấm UI thật và xác nhận ổn |
| C145 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Người dùng test điểm danh Thứ Bảy 25/07/2026, tính Sĩ số không ra đúng. Nguyên nhân: `CauHinhTuan` chỉ cấu hình 5 ngày Thứ Hai-Thứ Sáu (`so_ngay: 5`), Thứ Bảy/Chủ Nhật không nằm trong `tu_ngay..den_ngay` của tuần nào; `activeWeek` cũ fallback `\|\| weeks[0]` khi không khớp, tức lấy nhầm tuần đầu tiên từng cấu hình (Tuần 1) thay vì tuần hiện tại, làm sai `tuan_so` lúc lưu và sai toàn bộ số liệu Sĩ số/Hiện diện/Vắng/Tuần vắng dựa trên đó. | Thêm `resolveWeekForDate`: nếu không có tuần chứa đúng ngày, lấy tuần có `tu_ngay` lớn nhất mà vẫn `<= ngày đang chọn` (tuần Thứ Hai-Thứ Sáu gần nhất đã bắt đầu), chỉ dùng tuần đầu tiên nếu ngày đang chọn còn trước cả tuần cấu hình sớm nhất. **Lưu ý**: bản ghi Thứ Bảy 25/07 test trước khi sửa có thể đã lưu nhầm `tuan_so` của Tuần 1 — cần bạn tự kiểm tra lại trong "Danh sách đã điểm danh" hoặc Supabase, xoá/sửa lại nếu còn sai. Người dùng đã xác nhận đúng hiện tượng "ngày 25/07 ghi Tuần 1" trên UI thật, khớp chẩn đoán |
| C146 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Khối "Cần liên lạc phụ huynh" chỉ có nút "Ghi nhận" — nếu học sinh bị đánh dấu vắng nhầm (không cần liên lạc phụ huynh) thì phải nhảy sang Điểm danh mới xoá được, không xoá thẳng tại đây. Thêm nút "Xoá" cạnh "Ghi nhận": `window.confirm` rồi gọi thẳng `upsertAttendanceEntry` với `trang_thai: 'co_mat'` dùng đúng `ma_hs`/`ngay`/`tuan_so`/`buoi` sẵn có trên `DiemDanhCanLienLac` (không cần tính lại `activeWeek`), refresh danh sách sau khi xoá. Chưa tự bấm UI thật để xác nhận |
| C147 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Trang Ghi nhận: mục "1. Chọn nội dung" hiện lưới thẻ toàn bộ danh mục cá nhân (nhiều hàng, chiếm rất nhiều chỗ), mục "2. Chọn cách gắn lên học sinh" cũng là 3 nút thẻ song song — người dùng yêu cầu chuyển sang dropdown cho gọn. | Mục 1: bỏ lưới `button` thẻ, thay bằng 1 `<select>` liệt kê `visibleCatalog` (vẫn lọc được qua ô tìm + dropdown Tất cả/Tích cực/Vi phạm/Theo dõi có sẵn); bên dưới hiện 1 khối xem trước chi tiết (badge mã, điểm, tên mục, nhóm/phạm vi/nghiêm trọng) chỉ cho đúng mục đang chọn thay vì cả danh sách. Mục 2: bỏ 3 `ModeButton`, thay bằng 1 `<select>` 3 lựa chọn (Chọn từng học sinh/Theo tổ/Cả lớp), mỗi lựa chọn có số đếm ngay trong nhãn; thêm `teamStudentCount` tính riêng theo tổ đang chọn để không phụ thuộc `targetStudents` (vốn chỉ đúng khi tổ đang là chế độ active). Xoá hẳn component `ModeButton` không còn dùng. Không đổi phần chọn học sinh theo checkbox (đa lựa chọn, dropdown đơn không thay được) và dropdown chọn tổ (đã là dropdown từ trước). Chưa tự bấm UI thật để xác nhận |
| C148 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Thêm phân quyền cho học sinh giữ chức vụ "Lớp trưởng" tự gửi đề xuất ghi nhận (vi phạm hoặc tích cực) cho bạn cùng lớp qua link hồ sơ công khai `/hs/:token`, giáo viên duyệt trước khi tính điểm. Đã hỏi rõ 3 quyết định nghiệp vụ trước khi code: (1) bắt buộc giáo viên duyệt, không tính điểm ngay; (2) lớp trưởng được đề xuất cả vi phạm lẫn tích cực (không giới hạn chỉ vi phạm); (3) xác thực bằng mã PIN riêng do giáo viên cấp, ngoài token của link. | **Migration** `20260725000100_lop_truong_de_xuat_ghi_nhan.sql`: thêm cột `ban_can_su.ma_pin` (không grant SELECT cho anon/authenticated, chỉ đọc được trong function security definer); bảng `de_xuat_ghi_nhan` (trang_thai cho_duyet/da_duyet/tu_choi, RLS `authenticated` full quyền); 3 RPC — `xac_thuc_pin_lop_truong`, `lay_du_lieu_lop_truong` (trả roster + danh mục cá nhân nếu PIN đúng, ngược lại 0 dòng, không lộ dữ liệu), `gui_de_xuat_ghi_nhan` (ghi đề xuất, raise exception nếu PIN sai). **DataSource**: thêm `addBanCanSu`/`updateBanCanSu`/`deleteBanCanSu` (trước đây chỉ đọc được, không có UI gán chức vụ), `verifyLopTruongPin`, `getLopTruongData`, `submitDeXuatGhiNhan`, `getDeXuatGhiNhan`, `approveDeXuatGhiNhan` (tạo `ghi_nhan` thật qua `addRecords`, gắn `ma_ghi_nhan` vào đề xuất), `rejectDeXuatGhiNhan`. **`TeacherStudentDetailPage.tsx`**: khối "Ban cán sự" mới — dropdown chọn chức vụ, ô Tổ/Ngày bắt đầu, ô Mã PIN chỉ hiện khi chọn "Lớp trưởng". **`StudentProfilePage.tsx`**: nếu `role === 'Lớp trưởng'`, hiện khối "Nhập đề xuất ghi nhận cho lớp" — nhập PIN trước, đúng thì mở form chọn học sinh + danh mục + nội dung, gửi qua RPC. **`RecordEntryPage.tsx`**: thêm khối "Đề xuất ghi nhận từ lớp trưởng" liệt kê các đề xuất `cho_duyet`, nút Duyệt/Từ chối. Chưa tự bấm UI thật để xác nhận; migration chưa được apply lên Supabase |
| C149 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Sau C148, người dùng yêu cầu thêm: nếu danh mục hệ thống không có mục nào khớp, lớp trưởng được đề xuất danh mục mới (không bắt buộc chọn từ danh sách có sẵn); giáo viên khi duyệt phải sửa được nội dung cho chuẩn, hoặc gắn vào danh mục có sẵn gần đúng, hoặc tạo hẳn danh mục mới ngay tại chỗ trước khi tính điểm. | **Migration** `20260725000200_de_xuat_danh_muc_moi_tu_lop_truong.sql`: `de_xuat_ghi_nhan.ma_danh_muc` đổi thành nullable, thêm cột `de_xuat_nhom` (CC/VS/NN/KL/KT, chỉ có giá trị khi `ma_danh_muc` là null); RPC `gui_de_xuat_ghi_nhan` thêm tham số `p_de_xuat_nhom`, chấp nhận `ma_danh_muc` null nếu có `de_xuat_nhom` + nội dung mô tả. **`StudentProfilePage.tsx`**: dropdown danh mục thêm lựa chọn "➕ Không có, đề xuất danh mục mới" — chọn xong hiện thêm dropdown Nhóm, nội dung trở thành bắt buộc (mô tả cho giáo viên hiểu đề xuất là gì). **`SupabaseDataSource.approveDeXuatGhiNhan`** đổi sang nhận `overrides: { maDanhMuc?, noiDung? }` — ghi `ghi_nhan` theo giá trị cuối cùng do giáo viên chọn/sửa (không còn bắt buộc trùng khớp dữ liệu gốc học sinh gửi), đồng thời cập nhật lại `ma_danh_muc`/`noi_dung` vào chính dòng `de_xuat_ghi_nhan` để lưu đúng những gì đã duyệt. **`RecordEntryPage.tsx`**: mỗi đề xuất giờ là `ProposalReviewCard` — dropdown chọn danh mục có sẵn (sửa được kể cả khi đề xuất gốc đã có mã), nút "Tạo danh mục mới từ đề xuất này" mở mini-form (mã, nhóm, tên, điểm, nghiêm trọng) gọi `addPointCatalogItem` rồi tự chọn luôn mã mới tạo, ô nội dung sửa được trước khi Duyệt. Danh mục vừa tạo được thêm ngay vào `state.catalog` qua callback `onCatalogCreated` để các đề xuất khác trong cùng phiên cũng thấy mã mới đó. Chưa tự bấm UI thật để xác nhận |
| C150 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Thêm 3 việc theo yêu cầu: (1) lịch sử đề xuất của chính lớp trưởng, sửa/xoá được khi còn `cho_duyet`; (2) dropdown chọn ngày xảy ra vì lớp trưởng có thể nhập bù vào cuối tuần (Thứ Bảy/Chủ Nhật không thuộc tuần cấu hình nào); (3) nút thông báo cho giáo viên, đếm số đề xuất chờ duyệt trên biểu tượng. | **Migration** `20260725000300_lich_su_de_xuat_va_ngay.sql`: thêm cột `de_xuat_ghi_nhan.ngay` (mặc định `current_date`); `gui_de_xuat_ghi_nhan` thêm tham số `p_ngay`; RPC mới `lay_lich_su_de_xuat_lop_truong` (trả toàn bộ đề xuất do đúng lớp trưởng đó gửi, mọi trạng thái), `sua_de_xuat_ghi_nhan` và `xoa_de_xuat_ghi_nhan` (chỉ cho phép khi `ma_hs_de_xuat` khớp đúng lớp trưởng xác thực qua token+PIN và `trang_thai = cho_duyet`). `SupabaseDataSource.approveDeXuatGhiNhan` dùng `proposal.ngay` (ngày lớp trưởng chọn) thay vì luôn lấy ngày hôm nay khi tạo `ghi_nhan` thật. **`StudentProfilePage.tsx`**: form gửi đề xuất thêm input ngày (`max` = hôm nay); thêm khối "Lịch sử đề xuất của tôi" liệt kê mọi đề xuất đã gửi kèm trạng thái (Chờ duyệt/Đã duyệt/Bị từ chối), đề xuất `cho_duyet` có nút Sửa (mở lại đúng các trường đã gửi để sửa) và Xoá (có `window.confirm`). **`Layout.tsx`**: thêm nút "🔔 Thông báo" trỏ sang `/ghi-nhan`, đếm số đề xuất `cho_duyet` qua `getDeXuatGhiNhan()`, refetch mỗi khi đổi trang để cập nhật badge sau khi giáo viên duyệt/từ chối. Chưa tự bấm UI thật để xác nhận |
| C151 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | 3 việc theo yêu cầu: (1) trang Ghi nhận không tự chọn sẵn học sinh đầu tiên nữa; (2) Tổng quan giáo viên xoá được ghi nhận vi phạm/tích cực trực tiếp, không phải mở hồ sơ học sinh; (3) nút Thông báo bấm vào đọc được danh sách ngay (dropdown), bấm vào từng đề xuất thì điều hướng đến hồ sơ học sinh liên quan, có nút Duyệt/Từ chối nhanh ngay trong dropdown. | **`RecordEntryPage.tsx`**: bỏ dòng `setSelectedStudentIds(students[0] ? [students[0].ma_hs] : [])`, giữ nguyên `useState<string[]>([])` mặc định rỗng. **`DashboardPage.tsx`**: thêm `deleteRecord`/`deletingRecordId` theo đúng pattern đã dùng ở `TeacherStudentDetailPage.tsx` (`window.confirm` → `dataSource.deleteRecord` → lọc khỏi `state.records`); gắn nút "Xoá" vào 2 chỗ hiện ghi nhận cá nhân có `ma_ghi_nhan`: khối "Nhật ký theo ngày" và bảng "Xem theo Nhóm vi phạm" (`GroupViolationView` nhận thêm prop `onDeleteRecord`/`deletingRecordId`). Không đổi khối "Sự kiện của lớp/tổ" vì đó là luồng xử lý sự kiện tập thể riêng, xoá thẳng không phù hợp. **`Layout.tsx`**: đổi nút Thông báo từ `Link` sang `button` mở dropdown tại chỗ, tải kèm `getStudents()`/`getPointCatalog()` để hiện tên học sinh + tên danh mục ngay trong danh sách; mỗi dòng bấm vào điều hướng sang `/quan-ly/hoc-sinh/:ma_hs` (hoặc `/ghi-nhan` nếu là đề xuất danh mục mới, vì cần vào đó mới xử lý được), có nút Duyệt/Từ chối ngay trong dropdown cho đề xuất đã có `ma_danh_muc` sẵn (gọi thẳng `approveDeXuatGhiNhan`/`rejectDeXuatGhiNhan`, không cần override vì đã có danh mục). Chưa tự bấm UI thật để xác nhận |
| C152 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | 3 việc theo yêu cầu: (1) form đề xuất của lớp trưởng thêm Tiết/Môn (không bắt buộc), người ghi vẫn tự động theo chức vụ, không cần chọn; (2) `nguoi_ghi` khi duyệt đề xuất đọc đúng `chuc_vu` thật của lớp trưởng trong `ban_can_su` thay vì hardcode chữ "Lớp trưởng"; (3) chế độ giáo viên xem hồ sơ học sinh thấy được lịch sử đề xuất liên quan đến học sinh đó, sửa/xoá trực tiếp không cần PIN. | **Migration** `20260725000400_them_tiet_mon_de_xuat_ghi_nhan.sql`: thêm cột `de_xuat_ghi_nhan.tiet`, `mon_hoc`; `gui_de_xuat_ghi_nhan`/`sua_de_xuat_ghi_nhan` thêm tham số `p_tiet`/`p_mon_hoc`. **`SupabaseDataSource.approveDeXuatGhiNhan`**: tra `ban_can_su` theo `ma_hs_de_xuat` để lấy `chuc_vu` thật ghép vào `nguoi_ghi` (fallback "Lớp trưởng" nếu không tìm thấy), truyền `proposal.tiet`/`proposal.mon_hoc` vào `ghi_nhan` tạo mới thay vì luôn `null`. `getDeXuatGhiNhan` nhận thêm `options.maHs` để lọc theo học sinh. Thêm `updateDeXuatGhiNhanByTeacher`/`deleteDeXuatGhiNhanByTeacher` (gọi thẳng bảng `de_xuat_ghi_nhan` vì RLS đã cho `authenticated` full quyền, không cần RPC/PIN). **`StudentProfilePage.tsx`**: form gửi đề xuất và form sửa trong lịch sử đều thêm 2 ô Tiết/Môn (input thường, không bắt buộc), thêm dòng chú thích "Người ghi sẽ tự động ghi theo chức vụ". **`TeacherStudentDetailPage.tsx`**: thêm khối "Lịch sử đề xuất ghi nhận cho học sinh này" (load qua `getDeXuatGhiNhan({ maHs })`), mỗi dòng có nút Sửa (danh mục/nhóm mới/nội dung/ngày/tiết/môn)/Xoá dùng thẳng API giáo viên, không giới hạn theo trạng thái (có ghi chú khi đề xuất đã xử lý). Chưa tự bấm UI thật để xác nhận |
| C153 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo layout vỡ lệch trên di động ở tab Ghi nhận/Điểm danh/trang HS; hỏi lại thì chỉ rõ cụ thể: tab Sĩ số và Liên lạc PH bị tràn email ra ngoài khung. | Nguyên nhân: `nguoi_lien_lac` lưu email đăng nhập Supabase của giáo viên (chuỗi dài không có khoảng trắng) và được render trong `<p>` không cho phép ngắt dòng, tràn ngang trên màn hình hẹp. **`ParentContactHistoryPage.tsx`**: đổi `shrink-0` thành `min-w-0` trên div bọc, thêm `min-w-0 wrap-break-word` vào `<p>` hiện `nguoi_lien_lac`. **`TeacherStudentDetailPage.tsx`**: thêm `min-w-0 wrap-break-word` vào cả 2 `<p>` trong khối "Liên lạc phụ huynh" (ngày/buổi/hình thức và thời gian/người liên lạc) để chúng co giãn đúng trong `sm:flex-row`. Chưa xác nhận được cụ thể phần Ghi nhận/trang HS còn lỗi gì khác — đang chờ người dùng mô tả/gửi ảnh thêm nếu vẫn còn vỡ lệch sau bản này. |
| C154 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu sắp xếp lại nội dung trong khối `rounded-md border border-slate-200 p-3` (thẻ lượt liên lạc phụ huynh) theo dòng để tối ưu cho di động, thay vì chia 2 cột `sm:flex-row` (dễ chật vì `--breakpoint-sm` = 375px kích hoạt gần như mọi điện thoại). | **`ParentContactHistoryPage.tsx`**: bỏ layout `flex flex-col gap-1 sm:flex-row sm:items-start sm:justify-between` (tên/ngày một bên, thời gian/người liên lạc một bên), thay bằng `space-y-1` xếp dọc 3 dòng riêng (tên, ngày·buổi·hình thức, thời gian·người liên lạc); nút Sửa/Xoá chuyển xuống hàng riêng bên dưới (`mt-2 flex gap-2`, mỗi nút `flex-1 sm:flex-none` để chiếm đều chiều ngang trên điện thoại, không còn ép chung hàng với dòng text dài). **`TeacherStudentDetailPage.tsx`**: áp dụng đúng cách xếp dọc tương tự cho khối "Liên lạc phụ huynh" (không có nút nên chỉ bỏ `sm:flex-row`, dùng `space-y-1`). |
| C155 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu dịch khối nút `mt-2 flex gap-2` (Sửa/Xoá) trong thẻ liên lạc phụ huynh xuống dưới phần nội dung, thay vì nằm ngay trên nó. | **`ParentContactHistoryPage.tsx`**: chuyển khối nút Sửa/Xoá vào bên trong nhánh `else` của `editingId === item.id`, đặt sau `<p>{item.noi_dung}</p>` (bọc chung bằng fragment `<>...</>`) — khi đang sửa vẫn chỉ hiện form sửa như cũ, không hiện nút Sửa/Xoá; khi không sửa thì thứ tự hiển thị giờ là: thông tin (tên/ngày/thời gian) → nội dung → nút Sửa/Xoá. |
| C156 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu menu tab ở header ghim (sticky) khi cuộn xuống, và trên di động ẩn danh sách tab vào nút "..." — bấm vào mới hiện ra, để tối ưu không gian màn hình nhỏ; sau đó góp ý thêm rằng cả khối nút Thông báo/Tải mẫu phiếu/Đăng xuất/email xếp chồng dọc luôn hiện ra trên di động nhìn thô, cần gọn lại theo chuẩn mobile. | **`Layout.tsx`**: `<header>` thêm `sticky top-0 z-40` để dính lại khi cuộn. Tách phần render danh sách tab ra component `NavLinkList` (nhận `orientation: 'row' | 'col'` để tái dùng cho cả 2 layout) tránh lặp code. Đổi hẳn cấu trúc header từ 2 hàng `flex-col md:flex-row` sang 1 thanh app-bar `items-center justify-between` duy nhất: bên trái là brand, bên phải là 1 hàng ngang gồm nút Thông báo (luôn hiện, chữ "Thông báo" ẩn dưới `sm:` chỉ còn icon 🔔 cho gọn), rồi đến `nav`/`Tải mẫu phiếu`/`Đăng xuất`/email chỉ hiện từ `md:` (`hidden md:flex`), và cuối cùng nút toggle `⋯ {currentNavLabel}` chỉ hiện dưới `md:hidden`. Bấm nút `⋯` mở 1 dropdown tuyệt đối duy nhất (theo đúng pattern dropdown Thông báo có sẵn) gộp cả tab điều hướng (`NavLinkList orientation="col"`) lẫn 2 nút Tải mẫu phiếu/Đăng xuất và dòng email — tất cả gọn vào 1 nút thay vì xếp chồng 4-5 khối riêng biệt luôn hiện như trước; `navOpen` tự đóng khi đổi `pathname` hoặc sau khi bấm 1 mục trong menu. Từ `md:` trở lên hành vi giữ nguyên như cũ (mọi thứ hiện ngang, không có nút `⋯`). |
| C157 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Sau C156, người dùng báo dropdown Thông báo (`w-80`) bị lệch khỏi màn hình trên di động. | Nguyên nhân: dropdown dùng `absolute right-0` neo theo `div` bọc riêng nút 🔔 — trước C156 div đó chiếm trọn bề ngang container (`flex-col` xếp dọc) nên `right-0` trùng đúng mép phải màn hình; sau khi gộp 🔔 vào chung 1 hàng ngang với nút `⋯`, div bọc 🔔 chỉ còn rộng bằng chính nút đó và không còn là phần tử phải-cùng-hàng, khiến điểm neo lệch vào giữa màn hình rồi dropdown 320px tràn ra ngoài. **`Layout.tsx`**: đổi dropdown Thông báo sang `fixed inset-x-3 top-16 max-h-[70vh]` trên di động (bám theo viewport, không phụ thuộc vị trí neo) và chỉ quay lại `md:absolute md:right-0 md:top-12 md:w-80 md:max-h-96` từ `md:` (768px) trở lên — dùng `md:` chứ không phải `sm:` vì dự án đã đặt `--breakpoint-sm` = 375px (kích hoạt trên hầu hết điện thoại thật), chỉ `md:` mới đảm bảo đây là màn hình đủ rộng để neo tuyệt đối an toàn. Dropdown menu `⋯` (`w-56`) không đổi vì nó vẫn là phần tử cuối cùng bên phải hàng nên neo đúng mép màn hình. |
| C158 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu 2 dropdown (Thông báo và menu `⋯`) tự đóng lại khi bấm/thao tác ra chỗ khác, thay vì phải bấm lại đúng nút để đóng. | **`Layout.tsx`**: thêm `notifRef`/`navRef` (`useRef<HTMLDivElement>`) gắn vào đúng 2 `div` bọc từng dropdown; thêm 1 `useEffect` lắng nghe `pointerdown` trên `document` (chỉ đăng ký khi có ít nhất 1 trong 2 đang mở, gỡ khi cả 2 đóng để tránh listener thừa) — nếu điểm bấm nằm ngoài `notifRef`/`navRef` tương ứng thì tự `setNotifOpen(false)`/`setNavOpen(false)`. Dùng `pointerdown` thay vì `click` để đóng sớm hơn (trước khi phần tử bên dưới nhận sự kiện click, tránh trường hợp bấm xuyên qua dropdown vào nút khác). Không ảnh hưởng các nút Sửa/Xoá/Duyệt/Từ chối bên trong dropdown vì chúng nằm trong cùng `ref`, `contains(target)` vẫn đúng dù dropdown Thông báo dùng `position: fixed` (fixed chỉ đổi vị trí hiển thị, không đổi quan hệ cha-con trong DOM). |
| C159 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo dropdown Thông báo vẫn tràn trên giao diện desktop (C157 chỉ mới sửa cho di động). | Nguyên nhân giống hệt C157 nhưng xảy ra ở nhánh `md:` trở lên: `div` bọc riêng nút 🔔 vẫn không phải phần tử cuối/phải-nhất của hàng trên desktop (sau nó còn `nav` ngang + khối Tải mẫu phiếu/Đăng xuất/email), nên `right-0` neo theo div đó vẫn lệch sang trái so với mép phải thật, khiến dropdown 320px tràn/che lên nav và các nút phía sau thay vì nằm gọn sát mép phải. **`Layout.tsx`**: sửa tận gốc thay vì vá theo từng breakpoint — chuyển `relative` từ `div` bọc riêng nút 🔔 sang `div` bọc TOÀN BỘ cụm bên phải header (`flex items-center gap-2` → `relative flex items-center gap-2`), vì CSS `absolute` luôn neo theo tổ tiên `position` gần nhất bất kể có nằm lồng trong div trung gian không-positioned hay không — nhờ đó dropdown tự động neo đúng mép phải thật của hàng header trên MỌI kích thước màn hình, không cần tách `fixed`/`md:absolute` như C157 nữa. Bỏ hẳn kiểu `fixed inset-x-3` của C157, quay về `absolute right-0 top-12` đơn giản, chỉ thêm `max-w-[calc(100vw-1.5rem)]` làm lưới an toàn cho màn hình cực hẹp (dưới 320px). |
| C160 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thiết kế lại menu header giống Facebook: ẩn gọn, chỉ hiện khi bấm — áp dụng cho MỌI kích thước màn hình, không riêng di động (trước đó desktop vẫn luôn hiện đầy đủ thanh tab + nút Tải mẫu phiếu/Đăng xuất/email). | **`Layout.tsx`**: bỏ hẳn thanh tab ngang + khối nút luôn hiện trên desktop (từng có `hidden md:flex`); header giờ chỉ còn Brand + 2 nút icon tròn (`h-10 w-10 rounded-full`) không đổi theo breakpoint: 🔔 Thông báo (bỏ chữ "Thông báo", chỉ còn icon + `title` tooltip) và ☰ Menu (thay hẳn nút `⋯ {currentNavLabel}` cũ). Bấm ☰ mở 1 dropdown duy nhất (không còn tách bản mobile/desktop) gộp cả tab điều hướng, Tải mẫu phiếu, Đăng xuất, email — dùng chung ở mọi kích thước màn hình. Đơn giản hoá `NavLinkList` bỏ tham số `orientation` (chỉ còn 1 kiểu dọc) vì không còn nơi nào cần kiểu hàng ngang nữa. |
| C161 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn màu nền toàn bộ giao diện đậm lên (nền trang + card/section + header), vì bảng màu pastel hiện tại (`bg-*-50`) quá nhạt. | Bump cơ học toàn bộ `bg-{màu}-50` (slate/blue/indigo/cyan/emerald/amber/rose/orange/teal/violet/sky/red) → `bg-{màu}-100` bằng `sed -E` trên 12 file feature đang dùng màu pastel (khớp ranh giới từ `\b` để không đụng `bg-blue-500`...). Riêng nền canvas cấp trang (không phải card/section) đẩy thêm 1 nấc nữa lên `bg-slate-200`: `Layout.tsx` (wrapper chính `<div className="flex min-h-screen flex-col ...">` và `TeacherAuthShell`), `StudentProfilePage.tsx` (`<main>` trang hồ sơ công khai), `src/index.css` (`body`) — vì các trang xác thực (Dashboard, Ghi nhận, Điểm danh...) đều render qua `<Outlet>` bên trong wrapper này của `Layout.tsx` nên tự động ăn theo, không cần sửa từng trang. Header/footer đổi từ `bg-white` sang `bg-slate-100` (border `slate-200`→`slate-300`) để tạo phân lớp rõ: nội dung trắng < header/footer xám nhạt < nền trang xám đậm hơn. Không đụng tới 237 chỗ `bg-white` khác (bảng dữ liệu, modal, ô input...) vì đó là nội dung cần giữ nền sáng để đọc được, không phải phần "nền/canvas". |
| C162 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | 2 việc: (1) rà toàn bộ list trong app, giới hạn 5 dòng/trang, dài hơn thì phân trang; (2) đổi text footer từ "Trường THCS & THPT Lạc Hồng · Năm học 2025–2026" thành lời giới thiệu dạy lập trình của Thầy Nguyễn Duy Tân. | Thêm `src/components/Pagination.tsx`: hook `usePagination<T>(items, pageSize=5)` (tự reset về trang 1 khi `items.length` đổi, tự kẹp `page` không vượt `totalPages`) + component `Pagination` (nút Trước/Sau, ẩn hẳn nếu chỉ 1 trang). Áp dụng cho các list dữ liệu thật (không đụng dropdown option cố định nhỏ như `STATUS_OPTIONS`/`NHOM_OPTIONS`): **`Layout.tsx`** (danh sách đề xuất trong dropdown Thông báo), **`RecordEntryPage.tsx`** (đề xuất chờ duyệt, 2 `PreviewTable` xem trước/vừa tạo), **`AttendanceManagementPage.tsx`** (danh sách đã điểm danh, ngoại lệ trong tháng, cần liên lạc phụ huynh), **`ParentContactHistoryPage.tsx`** (lịch sử liên lạc đã lọc), **`TeacherStudentDetailPage.tsx`** (bảng ghi nhận, đề xuất lớp trưởng, lịch sử liên lạc PH), **`StudentsPage.tsx`** (bảng học sinh chính), **`CatalogPage.tsx`** (bảng danh mục, bảng ghi nhận theo danh mục trong modal), **`ImportPage.tsx`** (chỉ bảng log import — không đụng các bước wizard khớp dữ liệu vì phân trang sẽ che mất dòng cần xử lý), **`StudentProfilePage.tsx`** (lịch sử ghi nhận phân trang theo *nhóm tuần* thay vì từng dòng — giữ nguyên UX gộp theo tuần; lịch sử đề xuất của lớp trưởng). Chưa làm `DashboardPage.tsx` (nhiều list lồng nhau: bảng điểm tuần, nhật ký ngày, các modal drill-down, xem theo nhóm vi phạm) vì file quá lớn/phức tạp, cần một lượt riêng để không làm hỏng logic tính điểm — sẽ hỏi người dùng có muốn tiếp tục không. **`Layout.tsx`** footer: đổi text sang lời giới thiệu Thầy Nguyễn Duy Tân (sửa "chuyện" → "chuyên" cho đúng ngữ pháp, giữ nguyên phần còn lại). |

| C163 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo tab Tổng quan (Dashboard) chưa được phân trang ở C162; đồng thời yêu cầu bổ sung nút nhảy nhanh về trang 1 và trang cuối cho component `Pagination`. | **`Pagination.tsx`**: thêm 2 nút icon `«`/`»` (nhảy thẳng trang 1/trang cuối) hai bên nút "← Trước"/"Sau →"; hook `usePagination` trả thêm `pageSize`/`startIndex` để nơi gọi tự tính đúng số thứ tự (STT) theo trang thay vì luôn hiện lại 1-5 mỗi trang. **`DashboardPage.tsx`**: phân trang toàn bộ list còn thiếu — bảng điểm thi đua `sortedScores` (STT tính lại theo `scoresPage.startIndex`), "Sự kiện của lớp/tổ" `collectiveEvents`, "Nhật ký theo ngày" `dailyLogs`, bảng "Xem theo Nhóm vi phạm" `visibleRows` (trong `GroupViolationView`), và cả 4 nhánh của modal chi tiết (`OverviewDrillDownPanel`: attention/records/events/students — mỗi nhánh 1 hook `usePagination` riêng vì `drillDown.items` là union type theo `kind`, không thể narrow rồi mới gọi hook do luật hook phải gọi vô điều kiện; nhánh không khớp `kind` hiện tại nhận mảng rỗng nên `Pagination` tự ẩn, không xung đột). Cố tình không phân trang `TeamInfoPanel` (danh sách thành viên tổ — cỡ tổ cố định nhỏ; lịch sử sự kiện tổ trực đã tự giới hạn `slice(0, 5)` như một panel "gần đây" gọn, không phải list duyệt đầy đủ). |
| C164 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng mở file `tao csdl/huong-dan-pwa.md` (hướng dẫn tự soạn trước đó) và bảo đọc rồi làm: chuyển QLHS thành PWA (cài lên màn hình chính, cache để xem offline, tải nhanh hơn lần sau), không đổi backend/routing/nghiệp vụ. | Hướng dẫn gốc viết trước khi migrate khỏi Apps Script nên phần `runtimeCaching` mẫu trỏ vào `script.google.com` — đã lỗi thời từ C132 (backend giờ là Supabase thuần, xác nhận qua `src/lib/supabaseClient.ts`); đổi pattern sang `https://*.supabase.co/*`, thêm `method: 'GET'` để chỉ cache các lượt đọc PostgREST, không cache nhầm POST/PATCH/DELETE (ghi dữ liệu). Cài `vite-plugin-pwa`; cấu hình trong `vite.config.ts` với `start_url`/`scope` khớp `base: '/qlhs/'` sẵn có. Chưa có logo nên tạo tạm 4 icon PNG nền màu xanh `#1e40af` (khớp `theme_color`) bằng script Node viết tay (tự dựng PNG qua `zlib.deflateSync` + CRC32, không cần thư viện ảnh) — đặt trong `public/icons/` (thư mục `public/` cũng chưa từng có, tạo mới): `icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `apple-touch-icon.png`. Thêm 5 thẻ meta iOS vào `index.html` theo đúng hướng dẫn. Đã xác minh được: `npm run build` sinh đủ `manifest.webmanifest`, `sw.js`, `workbox-*.js`, `registerSW.js`; `npm run preview` phục vụ đúng `index.html`/`manifest.webmanifest`/`sw.js`/icon đều trả 200; `npm test` vẫn pass. **Chưa xác minh được** (cần người dùng tự làm, agent không có trình duyệt thật để kiểm tra): checklist DevTools Application (Manifest/Service Workers không lỗi), icon "Cài đặt" trên thanh địa chỉ, cài trên Android/iPhone thật, tắt mạng reload xem còn vào được không, và điểm Lighthouse PWA — đề nghị người dùng làm theo đúng checklist trong `huong-dan-pwa.md` sau khi deploy rồi phản hồi lại. Icon hiện chỉ là ô màu xanh đặc, chưa có logo/chữ "QLHS" — nên thay bằng icon thật khi có logo chính thức. |
| C165 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng đã chuẩn bị sẵn icon thật (nhân vật "Thầy Tân") trong `tao csdl/icon/` — 4 file đúng tên/kích thước cộng 1 file gốc `retro_master_1024.png`. | Copy 4 file `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` từ `tao csdl/icon/` đè lên bản placeholder (ô màu xanh đặc) trong `public/icons/` của C164 — đúng kích thước yêu cầu, không cần resize. `npm run build` xác nhận cả 4 icon được copy đúng vào `dist/icons/`, `npm test` vẫn pass. Chưa tự kiểm tra icon maskable có nằm gọn trong vùng an toàn 80% hay không (không có công cụ hình ảnh trong môi trường agent) — đề nghị người dùng tự soi lại bằng maskable.app/editor nếu thấy icon bị cắt góc khi cài trên Android thật. |
| C166 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm các tab dạng icon cố định dưới màn hình di động (kiểu bottom tab bar app thật), tự ẩn khi cuộn xuống và hiện lại khi cuộn lên. | **`Layout.tsx`**: thêm `icon` (emoji) vào từng phần tử `navItems`. Thêm state `bottomBarVisible` + `lastScrollY` (ref) và 1 `useEffect` lắng nghe sự kiện `scroll` trên `window` (passive) — cuộn xuống quá 8px thì ẩn, cuộn lên quá 8px thì hiện, riêng khi gần đầu trang (`scrollY < 24`) luôn hiện. Thêm `<nav>` cố định `fixed inset-x-0 bottom-0 md:hidden` liệt kê toàn bộ 8 tab dạng icon+nhãn ngắn, cuộn ngang được (`overflow-x-auto`) vì 8 tab không vừa 1 màn hình hẹp, tab đang active tô màu xanh; ẩn/hiện bằng `translate-y-full`/`translate-y-0` (transition mượt) thay vì gỡ khỏi DOM để tránh giật layout. `<main>` thêm `pb-20 md:pb-6` để nội dung cuối trang không bị thanh tab che khuất trên di động. Không đụng tới nút `☰ Menu` ở header (C160) — vẫn giữ nguyên vai trò menu đầy đủ + Tải mẫu phiếu/Đăng xuất/email, thanh tab dưới chỉ là lối tắt điều hướng nhanh, có trùng lặp nội dung với menu ☰ nhưng là chủ đích (mẫu UI phổ biến: bottom bar cho điều hướng nhanh, menu đầy đủ cho hành động phụ). |
| C167 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo thanh tab dưới (C166) nằm sát cạnh dưới màn hình, dễ chạm trúng vùng vuốt chuyển ứng dụng (home indicator gesture) của iPhone không có nút Home. | Nguyên nhân: thanh tab dùng `bottom-0` chạm thẳng mép an toàn của thiết bị, và `index.html` chưa khai báo `viewport-fit=cover` nên `env(safe-area-inset-bottom)` luôn trả về 0 (khai báo là điều kiện bắt buộc để giá trị này có tác dụng trên iOS Safari). **`index.html`**: thêm `viewport-fit=cover` vào thẻ `viewport`. **`Layout.tsx`**: khối icon bên trong thanh tab đổi `py-1` thành `pt-1 pb-[calc(0.5rem+env(safe-area-inset-bottom))]` — chừa thêm đúng khoảng vùng an toàn của thiết bị (0 trên Android/desktop, ~34px trên iPhone có home indicator) cộng thêm 0.5rem đệm cho thoáng, đẩy hẳn vùng bấm icon lên cao hơn khỏi dải vuốt cử chỉ; `<main>` cũng đổi `pb-20` thành `pb-[calc(5rem+env(safe-area-inset-bottom))]` để nội dung cuối trang không bị thanh tab (nay cao hơn) che khuất. Đã build kiểm tra class `safe-area-inset-bottom` có xuất hiện đúng trong CSS bundle sinh ra; `tsc`/`npm run build`/`npm test` đều pass. Chưa tự đo được khoảng cách thực tế trên iPhone thật (không có thiết bị/trình duyệt thật trong môi trường agent) — cần người dùng xác nhận lại trên iPhone sau khi deploy. |
| C168 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo header đang bị tai thỏ (notch/Dynamic Island) iPhone che khuất khi mở app dạng PWA cài đặt, muốn dịch header xuống. | Nguyên nhân giống hệt thanh tab dưới ở C167 nhưng ở cạnh trên: header `sticky top-0` chưa trừ vùng an toàn phía trên (chỉ lộ ra khi chạy standalone/full-screen sau khi cài PWA — Safari mở tab thường tự chừa chỗ, nhưng chế độ `display: standalone` thì không). **`Layout.tsx`**: thêm `pt-[env(safe-area-inset-top)]` vào chính thẻ `<header>` (ngoài `div` nội dung có `py-3` sẵn) — nền header vẫn phủ lên tới đỉnh màn hình (không hở màu nền trang phía sau tai thỏ), còn nội dung (logo/nút) tự động đẩy xuống dưới vùng tai thỏ. Cùng cơ chế `env(safe-area-inset-*)` đã bật từ C167 (`viewport-fit=cover` trong `index.html`), không cần thêm cấu hình mới. Build/test đều pass. Chưa tự đo được trên iPhone thật có notch/Dynamic Island (không có thiết bị/trình duyệt thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy, đặc biệt sau khi cài PWA lên màn hình chính (chế độ standalone). |
| C169 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm tính năng kéo màn hình xuống để làm mới dữ liệu (pull-to-refresh) kiểu Facebook, đặc biệt hữu ích khi mở app ở chế độ PWA cài đặt (không có pull-to-refresh gốc của trình duyệt). | Thêm `src/components/PullToRefresh.tsx`: theo dõi `touchstart`/`touchmove`/`touchend` trên `document` — chỉ bắt đầu kéo khi đang ở đúng đỉnh trang (`window.scrollY === 0`), kéo xuống dùng hệ số cản `RESISTANCE = 0.5` (cảm giác co giãn tự nhiên, không dịch 1:1 theo tay), giới hạn tối đa `MAX_PULL = 110px`; qua ngưỡng `PULL_THRESHOLD = 70px` mới nhả tay thì kích hoạt làm mới. Vì app có rất nhiều trang với luồng tải dữ liệu riêng (Dashboard, Ghi nhận, Điểm danh, Học sinh...), cách làm mới đơn giản và chắc chắn đúng ở MỌI trang mà không cần nối dây riêng từng nơi là gọi thẳng `window.location.reload()` — tải lại toàn trang, đi qua đúng service worker `NetworkFirst` cho API Supabase (đã cấu hình ở C164) nên vẫn ưu tiên lấy dữ liệu mới nhất khi có mạng. Chỉ gọi `event.preventDefault()` (nếu `cancelable`) khi `touchmove` đã xác nhận đang kéo xuống ở đỉnh trang, để tránh chặn nhầm việc cuộn trang bình thường. Có chỉ báo trực quan phía trên nội dung: mũi tên xoay dần theo % kéo, đổi thành icon quay tròn + chữ "Đang làm mới..." khi vượt ngưỡng. Gắn vào `Layout.tsx`, bọc quanh `<Outlet />` bên trong `<main>` nên áp dụng cho mọi trang đã đăng nhập cùng lúc, không cần sửa từng trang. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác kéo thật trên màn cảm ứng để kiểm tra cảm giác/độ nhạy (không có thiết bị cảm ứng thật trong môi trường agent) — cần người dùng tự thử trên điện thoại thật, đặc biệt xem có xung đột với cử chỉ vuốt xuống mở Trung tâm Thông báo trên đầu màn hình iPhone hay không. |
| C170 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn trang link hồ sơ học sinh (public, không qua `Layout.tsx`) có menu ẩn dạng "☰" giống style header trang chủ giáo viên (C160), cộng thêm thanh icon chọn mục cạnh dưới màn hình di động (giống C166), lưu tâm vùng tai thỏ và cử chỉ vuốt cạnh dưới iPhone (giống C167/C168). | **`StudentProfilePage.tsx`**: thêm `icon` (emoji) vào từng phần tử `PROFILE_SECTIONS` (🧾 Tóm tắt, ⭐ Ghi nhận, 🕘 Lịch sử, 📊 Điểm tuần, 👤 Cá nhân). Thay khối tiêu đề tĩnh + card `ProfileSectionNav` (luôn hiện 5 nút bấm dạng pill) bằng 1 thanh header `sticky top-0` riêng cho trang này (không dùng chung `Layout.tsx` vì đây là route public không qua layout xác thực) — brand bên trái, nút icon tròn "☰" bên phải mở dropdown liệt kê đúng 5 mục cũ (đóng khi bấm ra ngoài qua `menuRef` + `pointerdown` listener, cùng pattern với C158); xoá hẳn component `ProfileSectionNav` cũ vì không còn nơi nào gọi. Thêm `<nav>` cố định `fixed inset-x-0 bottom-0 md:hidden` bên dưới cùng, y hệt kiểu bottom tab bar ở `Layout.tsx` (C166): icon lớn + nhãn nhỏ cho cả 5 mục, tô xanh mục đang khớp `activeTab`, cuộn ngang được. Áp dụng luôn 2 bản vá an toàn thiết bị đã có: header thêm `pt-[env(safe-area-inset-top)]` (tránh tai thỏ/Dynamic Island, như C168), thanh tab dưới thêm `pb-[calc(0.5rem+env(safe-area-inset-bottom))]` (tránh dải cử chỉ chuyển ứng dụng iPhone, như C167), `<main>` thêm `pb-[calc(5rem+env(safe-area-inset-bottom))]` để nội dung không bị thanh tab che. Không đụng tính năng pull-to-refresh (C169) vì trang này không nằm trong `Layout.tsx`. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật trên iPhone/Android để kiểm tra bố cục và vùng an toàn (không có thiết bị thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy. |
| C171 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn trang link hồ sơ học sinh cũng có pull-to-refresh như C169 — trang này không đi qua `Layout.tsx` nên chưa tự có tính năng đó. | **`StudentProfilePage.tsx`**: import component `PullToRefresh` có sẵn từ C169 (`src/components/PullToRefresh.tsx`, không viết lại logic), bọc quanh `<section>` nội dung chính (giữ header `sticky` và thanh tab dưới `fixed` ở ngoài, không bọc — 2 khối đó không cần cuộn/kéo). Cùng cơ chế `window.location.reload()` khi kéo qua ngưỡng 70px như ở `Layout.tsx`. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác kéo thật trên thiết bị cảm ứng (không có trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy. |

| C172 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn form đề xuất của lớp trưởng (`rounded-lg border border-teal-300 bg-teal-100 p-4 shadow-sm` trong trang link hồ sơ) hỗ trợ chọn nhiều học sinh và nhiều danh mục cùng lúc (tránh phải lặp thao tác từng cặp học sinh-danh mục), và đổi cách nhập từ "dàn trải hết các trường cùng lúc" sang luồng từng bước có Tiếp theo/Quay lại để tránh bỏ sót trường. | **`StudentProfilePage.tsx`** (`LopTruongPanel`): đổi `selectedMaHs`/`selectedCatalog` (string đơn) thành `selectedStudents`/`selectedCatalogCodes` (mảng, chọn qua checkbox list `max-h-64 overflow-y-auto`) + `includeNewCategory` (checkbox riêng cho "đề xuất danh mục mới", tách khỏi việc chọn nhiều danh mục có sẵn). Thêm state `step: 'students' | 'catalog' | 'details' | 'review'` điều khiển 1 wizard 4 bước (chỉ hiện đúng 1 bước tại 1 thời điểm, có thanh chỉ số bước + nút "← Quay lại"/"Tiếp theo →", validate bắt buộc mỗi bước trước khi qua bước sau: bước 1 cần ≥1 học sinh, bước 2 cần ≥1 danh mục hoặc bật đề xuất mới, bước 3 nếu bật đề xuất mới thì bắt buộc nội dung). Bước 4 (Xác nhận) hiện tổng số đề xuất sẽ tạo = số học sinh × số danh mục (kể cả danh mục mới, tính +1), liệt kê tên học sinh/danh mục đã chọn, rồi bấm "Gửi N đề xuất" mới thật sự gọi API — hàm `submitAll` dựng mảng "combos" (mọi cặp học sinh×danh mục), gọi `dataSource.submitDeXuatGhiNhan` tuần tự cho từng combo (không dùng `Promise.all` để tránh làm nghẽn RPC Supabase khi số lượng lớn), có `submitProgress { done, total }` hiện tiến độ, đếm số lỗi để báo "Gửi được X/Y đề xuất" nếu có lỗi thay vì chỉ báo chung chung. Sau khi gửi xong reset toàn bộ state về bước 1. Không đụng `ProposalHistoryItem` (form sửa lại 1 đề xuất đã gửi) vì đó vẫn đúng là sửa 1 học sinh + 1 danh mục, không cần đổi. `tsc` (có bật `noUnusedLocals`)/`npm run build`/`npm test` đều pass, xác nhận không còn sót state single-select cũ. Chưa tự thao tác thật để kiểm tra cảm giác luồng 4 bước trên di động (không có thiết bị thật trong môi trường agent) — cần người dùng tự thử và góp ý nếu luồng bước chưa hợp lý. |
| C173 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn bước 4 (Xác nhận gửi) của wizard đề xuất lớp trưởng (C172) hiện rõ nội dung đã chọn theo từng học sinh cụ thể, không bắt học sinh phải tự suy luận từ con số "N học sinh × M danh mục" và 2 dòng liệt kê tên tách rời (dễ nhầm ai bị áp mục nào khi có nhiều học sinh/danh mục). | **`StudentProfilePage.tsx`** (`LopTruongPanel`, bước `review`): bỏ cách hiện phép tính trừu tượng + 2 dòng liệt kê tên tách rời, thay bằng danh sách từng học sinh — mỗi học sinh 1 khối riêng ghi rõ tên, bên dưới là `<ul>` liệt kê đúng các danh mục (mã + tên đầy đủ + điểm, không chỉ mã) sẽ áp cho riêng người đó (kể cả dòng "➕ Đề xuất danh mục mới · nhóm ..." nếu có bật, tra tên nhóm qua `NHOM_OPTIONS` thay vì hiện code thô). Vì hiện tại mọi học sinh được chọn đều nhận đúng cùng 1 tập danh mục (múc chọn ở bước 2 là chung, chưa hỗ trợ chọn khác nhau theo từng người), danh sách con lặp lại giống nhau cho mỗi học sinh — nhưng hiện tường minh dạng "học sinh A: mục X, mục Y" thay vì bắt tự suy ra từ phép nhân, đúng yêu cầu "không cần đoán hay suy luận". Thêm khối riêng hiện rõ "Áp dụng cho tất cả: ngày/tiết/môn" và dòng Nội dung (in nghiêng nếu có nhập, hoặc ghi chú rõ "chưa nhập, mỗi đề xuất sẽ tự lấy theo tên danh mục tương ứng" nếu để trống — đúng hành vi thật của `approveDeXuatGhiNhan` phía giáo viên duyệt, không phải suy đoán). `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật để xem giao diện có quá dài khi chọn nhiều học sinh/danh mục hay không (không có thiết bị thật trong môi trường agent) — cần người dùng tự thử với số lượng lớn (VD 10 học sinh) để góp ý nếu cần thu gọn thêm. |
| C174 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn Tiết/Môn trong form đề xuất lớp trưởng chuyển từ ô nhập tự do sang dropdown cố định: Tiết = 1-8 hoặc "Giờ ra chơi"; Môn = Toán, Lý, Địa, Văn, TVAV, ANH, KTPL, GDTC, Sinh hoạt dưới cờ (SHDC), Sinh hoạt chủ nhiệm (SHCN) — mỗi dropdown chỉ chọn được 1 giá trị. | **`StudentProfilePage.tsx`**: thêm hằng `TIET_OPTIONS` (mảng '1'..'8' + 'Giờ ra chơi') và `MON_OPTIONS` (10 mã môn đúng thứ tự người dùng liệt kê) + `MON_LABELS` (map riêng cho SHDC/SHCN hiện tên đầy đủ trong ngoặc, các môn còn lại hiện nguyên mã). Đổi cả 2 chỗ đang dùng `<input>` tự do cho tiết/môn thành `<select>`: form gửi đề xuất mới (bước 3 "Thông tin chung" của wizard) và form sửa đề xuất trong lịch sử (`ProposalHistoryItem`). Giá trị lưu vẫn là string tự do như cũ (cột `tiet`/`mon_hoc` không đổi kiểu dữ liệu), chỉ giới hạn UI nhập liệu còn đúng các lựa chọn hợp lệ, tránh gõ sai chính tả/định dạng tiết-môn không thống nhất giữa các lần nhập. `tsc`/`npm run build`/`npm test` đều pass. Đề xuất cũ đã lưu trước đây (nếu có tiết/môn dạng tự do khác các lựa chọn mới) khi mở form sửa sẽ không khớp lựa chọn nào trong dropdown — cần người dùng lưu ý khi sửa đề xuất cũ, có thể cần chọn lại đúng giá trị. |
| C175 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo trang xem học sinh (`TeacherStudentDetailPage.tsx`) có nhiều khối div lồng nhau bị chèn chữ/xuống dòng sai trên di động, nêu ví dụ khối `rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm` (thẻ tiêu đề hồ sơ), yêu cầu rà lại toàn trang và chọn cách làm tốt nhất cho di động. | Nguyên nhân gốc trùng với các lỗi header đã sửa trước đây (C157/C159): dự án đặt `--breakpoint-sm: 375px` (từ C008) nên mọi `sm:flex-row` chuyển sang xếp ngang ngay trên điện thoại thật (390-430px) thay vì đợi đến máy tính bảng, cộng thêm khối chứa text bên trái không có `min-w-0` nên trình duyệt không cho phép nó co lại/ngắt dòng đúng cách trong flex row (mặc định `min-width: auto` của flex item chặn wrap). Sửa tận gốc bằng 2 nguyên tắc áp dụng nhất quán cho mọi vị trí xếp "khối tiêu đề dài + 1 nút/badge ngắn cạnh bên" trong file: (1) đổi `sm:flex-row sm:items-*` thành `md:flex-row md:items-*` (mốc `md` 768px không bị override, mới thật sự "đủ rộng để xếp ngang"); (2) thêm `min-w-0` vào khối text bên trái và `shrink-0` vào nút/badge bên phải để 2 bên chia không gian đúng thay vì bên trái bị đẩy tràn. Áp dụng cho: tiêu đề trang (brand + nút "Xem link học sinh"/"Về danh sách"), thẻ tiêu đề hồ sơ học sinh (tên/avatar cạnh khối STT+Ghi nhận — khối STT+Ghi nhận đổi từ `min-w-40` cố định sang `w-full md:w-40` để chiếm trọn hàng riêng khi xếp dọc trên di động, gọn hơn hẳn khối nhỏ bị bóp), 3 tiêu đề section ("Ghi nhận của học sinh", "Liên lạc phụ huynh", "Chỉnh sửa thông tin" đều có nút hành động cạnh bên), khối đề xuất lớp trưởng (`TeacherProposalItem`, thêm `wrap-break-word` vào các dòng text bên cạnh badge trạng thái), và `PhoneRow` (tên phụ huynh dài cạnh số điện thoại). Không đụng các bảng dữ liệu (ghi nhận) vì đã dùng đúng `overflow-x-auto` (cuộn ngang) từ trước — đó là cách xử lý bảng chuẩn, khác với lỗi "flex bị bóp chữ" đang sửa. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật trên điện thoại để kiểm tra bố cục mới (không có thiết bị thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy, đặc biệt các thẻ có tên phụ huynh/nội dung dài. |
| C176 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng gửi ảnh chụp cho thấy tên học sinh vẫn bị cắt "Vũ Trần Khá..." trong thẻ tiêu đề hồ sơ (`rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm`) dù đã báo đã sửa ở C175 — vì C175 mới chỉ commit local, chưa `push` nên bản đang chạy thật vẫn là code cũ. | Đã push C170-C175 lên `origin/master`. Đồng thời làm chắc thêm 1 lớp phòng ngừa tại đúng chỗ người dùng chụp ảnh: `<h3>` tên học sinh trong thẻ tiêu đề đổi từ `truncate` (cắt + "...", luôn mất chữ khi hẹp bất kể nguyên nhân gì) sang `wrap-break-word` (cho xuống dòng khi cần, không bao giờ mất chữ) + cỡ chữ giảm `text-xl` trên di động, giữ `sm:text-2xl` từ tablet trở lên — tên học sinh dù dài cỡ nào cũng luôn đọc được đầy đủ, chỉ đánh đổi bằng việc thẻ có thể cao thêm 1 dòng khi tên rất dài, chấp nhận được vì thà xuống dòng còn hơn mất thông tin tên học sinh. `tsc`/`npm run build`/`npm test` đều pass. |
| C177 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Ảnh chụp cho thấy tên vẫn bị cắt ở khối `rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm` — hoá ra đây là component `StudentProfileHeader` trong `StudentProfilePage.tsx` (trang link công khai học sinh xem), KHÔNG phải `TeacherStudentDetailPage.tsx` mà C175/C176 đã sửa (2 file trùng tên class ngẫu nhiên). Người dùng yêu cầu bố cục lại hẳn layout thay vì tiếp tục vá vì bề ngang di động quá hạn chế. | **`StudentProfilePage.tsx`**: sửa đúng `StudentProfileHeader` — layout cũ `flex items-center gap-3` không có nhánh xếp dọc nào cả (không phải lỗi breakpoint mà là hoàn toàn thiếu responsive), nên tên luôn bị bóp bởi avatar + khối "N ghi nhận" cố định bên phải. Bố cục lại: `flex-col md:flex-row md:items-center md:justify-between` — trên di động, avatar+tên nằm 1 hàng riêng (được toàn bộ bề ngang thẻ), khối số liệu "N ghi nhận" xuống hàng dưới hiện dạng ngang gọn (`flex items-center gap-4`) thay vì ép chung 1 hàng với tên; từ `md:` trở lên khôi phục đúng bố cục ngang cũ. Tên đổi từ `truncate` sang `wrap-break-word` (không bao giờ mất chữ). Nhân dịp rà toàn bộ file (đúng như người dùng yêu cầu "rất nhiều hiện tượng này trong trang đó"), sửa thêm 6 vị trí cùng lỗi: `ProfileSectionHeader` (tiêu đề + nút Mở rộng/Thu gọn dùng chung cho mọi section), `FeaturedRecords`/`RecordHistory` (tiêu đề dài + `ImpactSummary`), `ScoreSummary` (tiêu đề + hộp xếp loại), `ProfileCard` (tên ở tab Cá nhân), `ProposalHistoryItem` (tên/nội dung đề xuất cạnh badge trạng thái) — đều theo cùng công thức: đổi `sm:` → `md:` cho các cặp tiêu đề dài + khối cố định bên cạnh (băng qua đúng ngưỡng `--breakpoint-sm: 375px` bị override từ C008), thêm `min-w-0`/`wrap-break-word` vào khối text, `shrink-0` vào khối cố định. `tsc`/`npm run build`/`npm test` đều pass. |
| C178 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn bố cục lại lưới điểm 5 cột (`grid gap-px bg-amber-200 sm:grid-cols-5` trong `ScoreSummary`, tab "Điểm tuần" của trang thông tin học sinh) cho hợp di động — 5 cột (Chuyên cần/Vệ sinh/Nề nếp/Kỷ luật/Học tập) nhồi vào 1 hàng ngay từ `sm:` (375px, kích hoạt trên hầu hết điện thoại thật) khiến mỗi cột chỉ còn ~70-80px, nhãn và số bị bóp. | **`StudentProfilePage.tsx`** (`ScoreSummary`): đổi `sm:grid-cols-5` (kích hoạt quá sớm trên điện thoại) thành `grid-cols-2 md:grid-cols-5` — mặc định (mọi điện thoại) xếp 2 cột/hàng (5 mục thành 2-2-1, mục thứ 5 tự xuống hàng riêng, thoáng hơn hẳn 5 cột chật hay 1 cột dài lê thê), chỉ về đúng 5 cột 1 hàng khi màn hình đủ rộng thật (`md:` 768px). Giảm nhẹ `p-4` → `p-3 sm:p-4` và cỡ chữ số điểm `text-2xl` → `text-xl sm:text-2xl` để mỗi ô 2-cột trên điện thoại nhỏ (ví dụ 360px) vẫn đủ chỗ, tránh số/nhãn tràn ra ngoài ô. `tsc`/`npm run build`/`npm test` đều pass. |
| C179 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thiết kế lại nút "Thu gọn"/"Mở rộng" (`inline-flex h-9 shrink-0 items-center justify-center rounded-md border border-slate-300 bg-white px-3 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-100`) trong `ProfileSectionHeader` của trang thông tin học sinh thành 1 icon nhỏ ở góc thay vì chữ nhìn thô — làm trước 1 chỗ, nếu ổn thì triển khai cho tất cả các nơi dùng cùng mẫu nút "Thu gọn"/"Mở rộng" trong app. | **`StudentProfilePage.tsx`** (`ProfileSectionHeader`): đổi nút chữ thành nút tròn nhỏ `h-8 w-8 rounded-full` chỉ chứa icon mũi tên "▾" (không còn chữ "Thu gọn"/"Mở rộng"), xoay 180° (`rotate-180`) khi đang mở để chỉ hướng "thu gọn lại", giữ nguyên hướng khi đang gọn để chỉ hướng "mở ra" — vẫn giữ `aria-label`/`aria-expanded` để đọc được bằng screen reader dù không còn chữ hiển thị. Bố cục tiêu đề đổi từ `flex-col md:flex-row` (từng phải xếp dọc trên di động vì nút chữ dài) về lại 1 hàng ngang cố định `flex items-center justify-between` — vì nút icon giờ rất nhỏ (32px), không còn cần xếp dọc để tránh chèn tiêu đề nữa, gọn hơn hẳn. `tsc`/`npm run build`/`npm test` đều pass. Các nơi khác đang dùng nút chữ "Thu gọn"/"Mở rộng" tương tự (`DashboardPage.tsx` — 3 chỗ: `SectionHeader` dùng chung, khối điểm thi đua, `GroupViolationView`; `StudentsPage.tsx` — nút thu gọn danh sách chính và nút "Chi tiết"/"Thu gọn" theo từng dòng) CHƯA đổi — đang chờ người dùng xác nhận thiết kế icon này ổn rồi mới triển khai đồng loạt như đã hẹn. |
| C180 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Sau C179, người dùng thấy `ProfileSectionHeader` (`flex items-center justify-between gap-2`) nhìn chưa nổi bật, không phân biệt được với vùng nội dung phía dưới; sau khi thử bản nền trắng viền xám, người dùng góp ý luôn muốn nền màu đậm hẳn cho thật nổi bật thay vì chỉ viền nhạt. | **`StudentProfilePage.tsx`** (`ProfileSectionHeader`): đổi hẳn thành thanh tiêu đề nền xanh đậm đặc `rounded-lg bg-blue-700 px-4 py-2.5 shadow-sm`, chữ tiêu đề đổi sang `text-white` cho tương phản rõ — dùng chung 1 màu xanh brand cho mọi section bất kể section đó bên dưới màu gì (sky/blue/emerald/amber/violet), nổi bật tách biệt hẳn khỏi nền `bg-slate-200` của trang. Nút icon tròn đổi thành `bg-white/20` (nền trắng bán trong suốt) + `text-white`, nổi rõ trên nền xanh đậm mà không cần thêm viền. `tsc`/`npm run build`/`npm test` đều pass. |
| C181 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng duyệt mẫu icon+nền đậm ở C179/C180, yêu cầu áp dụng cho tất cả các chỗ "Thu gọn"/"Mở rộng" còn lại trong app, màu nền lấy theo tông màu riêng của từng section cho hợp gu (không dùng chung 1 màu xanh). | **`DashboardPage.tsx`**: thêm `SectionTone` + `SECTION_TONE_BAR` (map tone → `bg-{màu}-700`, các tông: slate/sky/cyan/indigo/violet/amber/emerald). `SectionHeader` (component dùng chung 5 section: Tóm tắt nhanh=slate, Bộ lọc thời gian=sky, Thống kê tổng quan=cyan, Sự kiện của lớp/tổ=amber, Nhật ký theo ngày=emerald) đổi hẳn cấu trúc: thanh tiêu đề nền đậm theo tone + nút icon tròn thay chữ, mô tả dài tách xuống dưới thanh màu (không nhét chung vào nền đậm vì mô tả nhiều dòng). Khối "Điểm thi đua học sinh" (indigo) và `GroupViolationView`/"Xem theo Nhóm vi phạm" (violet) không dùng chung `SectionHeader` nên sửa tay riêng theo đúng công thức, phần checkbox "Hiện cả học sinh không vi phạm" tách khỏi thanh màu, đặt xuống dòng riêng bên dưới mô tả. **`StudentsPage.tsx`**: header "Danh sách học sinh (N)" đổi tương tự, tone slate (card trung tính). Cố tình KHÔNG đổi nút "Chi tiết"/"Thu gọn" theo từng dòng học sinh trong bảng — đây là action nhỏ cấp hàng bảng giống hệt các nút "Sửa"/"Xoá" cạnh nó, không phải thanh tiêu đề section, đổi riêng nó sẽ lệch tông với các nút hàng khác. `tsc`/`npm run build`/`npm test` đều pass. |
| C182 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm hệ thống đăng nhập/đăng xuất cho trang công khai hồ sơ học sinh (`/hs/:token`), dựa trên SĐT 1 của học sinh: username = SĐT 1, mật khẩu = 3 số cuối của chính SĐT 1 đó. | Xác thực làm ở tầng server (Postgres `security definer`), không chỉ chặn trên UI — vì RPC `lay_ho_so_cong_khai` cũ chỉ cần đúng `token` là trả toàn bộ dữ liệu, nếu chỉ thêm màn hình đăng nhập ở React thì dữ liệu vẫn lộ qua network request đầu tiên. **Migration mới** `20260725000500_dang_nhap_ho_so_cong_khai.sql`: drop hàm cũ `lay_ho_so_cong_khai(text)`, tạo lại với 3 tham số `(p_token, p_sdt, p_mat_khau)` — chuẩn hoá cả số nhập lẫn số lưu (`regexp_replace(..., '\D', '', 'g')`, chỉ giữ chữ số) trước khi so sánh để tránh sai lệch do khoảng trắng/dấu gạch ngang; so `p_sdt` với `sdt_1` và `p_mat_khau` với `right(sdt_1, 3)`, sai 1 trong 2 đều trả rỗng (không lộ sai ở đâu, đúng pattern PIN lớp trưởng đã dùng). Vẫn giữ nguyên việc KHÔNG trả `sdt_1`/`sdt_2` thật trong kết quả (bảo mật số điện thoại phụ huynh) — số điện thoại chỉ dùng để xác thực, không hiển thị lại. **`DataSource.ts`/`SupabaseDataSource.ts`**: `getPublicStudentProfile(token)` đổi thành `getPublicStudentProfile(token, sdt, matKhau)`; xoá `getStudentByToken` (không còn ai gọi, và không thể giữ nguyên logic không-cần-mật-khẩu nữa). **`StudentProfilePage.tsx`**: thêm state `'login'` vào `ProfileState`, hiện form đăng nhập (SĐT + 3 số mật khẩu) khi chưa xác thực; đăng nhập thành công lưu `{ sdt, matKhau }` vào `sessionStorage` (khoá riêng theo từng `token`, tự xoá khi đóng tab/trình duyệt — không dùng `localStorage` để tránh đăng nhập "vĩnh viễn" trên máy dùng chung) để refresh trang trong cùng phiên không phải nhập lại; mở lại link sau khi đóng trình duyệt thì phải đăng nhập lại. Thêm nút "Đăng xuất" trong menu ☰ sẵn có (xoá `sessionStorage`, quay lại form đăng nhập). Nút ☰ chỉ hiện khi đã đăng nhập thành công. **Lưu ý quan trọng**: học sinh nào chưa có SĐT 1 trong hệ thống sẽ KHÔNG đăng nhập được vào chính hồ sơ của mình nữa (trước đây chỉ cần link là xem được) — cần rà lại toàn bộ 36 học sinh đã có đủ SĐT 1 chưa trước khi áp dụng migration này lên Supabase thật, nếu thiếu phải bổ sung trước. `tsc`/`npm run build`/`npm test` đều pass. Chưa test được luồng đăng nhập/đăng xuất thật (cần áp dụng migration lên Supabase trước, agent không có trình duyệt thật để kiểm tra) — cần người dùng dán migration vào Supabase SQL Editor rồi tự thử đăng nhập bằng SĐT thật của 1 học sinh. |
| C183 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Người dùng muốn mở rộng quyền "Nhập đề xuất ghi nhận cho lớp" (gửi đề xuất ghi nhận qua link hồ sơ công khai) — hiện chỉ dành cho học sinh giữ chức vụ "Lớp trưởng" — để áp dụng thêm cho "Lớp phó học tập". | Rà bằng `grep` xác nhận chuỗi `'Lớp trưởng'` chỉ xuất hiện đúng 1 lần trong toàn bộ migration SQL, nằm trong mệnh đề `where` của hàm `_qlhs_lop_truong_ma_hs` (helper xác thực token+PIN dùng chung cho mọi RPC liên quan: `xac_thuc_pin_lop_truong`, `lay_du_lieu_lop_truong`, `gui_de_xuat_ghi_nhan`, `sua_de_xuat_ghi_nhan`, `xoa_de_xuat_ghi_nhan`, `lay_lich_su_de_xuat_lop_truong`) — nên chỉ cần sửa đúng 1 chỗ là quyền mới lan ra toàn bộ 6 RPC mà không phải sửa từng cái. **Migration mới** `20260726000100_mo_rong_quyen_lop_pho_hoc_tap.sql`: `create or replace function _qlhs_lop_truong_ma_hs` giữ nguyên chữ ký, chỉ đổi điều kiện `bcs.chuc_vu = 'Lớp trưởng'` thành `bcs.chuc_vu in ('Lớp trưởng', 'Lớp phó học tập')`. **`StudentProfilePage.tsx`**: đổi hằng số `LOP_TRUONG_ROLE` (dùng đúng 1 lần, để gate hiển thị `<LopTruongPanel>`) thành mảng `LOP_TRUONG_ELIGIBLE_ROLES = ['Lớp trưởng', 'Lớp phó học tập']`, điều kiện hiển thị đổi từ `state.role === LOP_TRUONG_ROLE` sang `LOP_TRUONG_ELIGIBLE_ROLES.includes(state.role)`. **`TeacherStudentDetailPage.tsx`**: giữ nguyên `LOP_TRUONG_ROLE` (còn dùng ở chỗ khác trong file) và thêm mới `LOP_TRUONG_ELIGIBLE_ROLES`, áp dụng ở 3 chỗ — logic lưu `ma_pin` trong `saveRole()` (chỉ giữ/lưu PIN khi chức vụ chọn nằm trong danh sách được phép, thay vì chỉ đúng "Lớp trưởng"), đoạn mô tả hướng dẫn dưới tiêu đề "Chức vụ trong lớp" (cập nhật nêu cả 2 chức vụ), và điều kiện hiện ô nhập "Mã PIN" trong form gán chức vụ (hiện cả khi chọn "Lớp phó học tập", không chỉ "Lớp trưởng"). `tsc`/`npm run build`/`npm test` (10/10) đều pass. Migration mới cần người dùng dán vào Supabase Dashboard SQL Editor thì quyền mới có hiệu lực thật — trước đó "Lớp phó học tập" dù đăng nhập đúng SĐT/mật khẩu vẫn sẽ không thấy được form gửi đề xuất vì RPC nền chưa cập nhật. |
| C184 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Sau C183, người dùng chỉ ra nhãn tĩnh "Dành cho lớp trưởng" trong panel gửi đề xuất, đối chiếu với ví dụ `{role}` động đã có sẵn ở thẻ tiêu đề hồ sơ — muốn nhãn hiển thị đúng chức vụ thật của học sinh đang đăng nhập, không phải chuỗi liệt kê tĩnh. | `StudentProfilePage.tsx`: `LopTruongPanel` nhận thêm prop `role`, 2 nhãn "Dành cho ..." và dòng chú thích "tự động ghi theo chức vụ của bạn" đổi từ chuỗi tĩnh sang hiển thị `{role}` động — khớp đúng chức vụ thật (Lớp trưởng hoặc Lớp phó học tập) của người đang dùng form. Giữ nguyên nhãn liệt kê tĩnh ở `TeacherStudentDetailPage.tsx`/`RecordEntryPage.tsx` vì đó là màn hình giáo viên xem tổng hợp đề xuất từ nhiều học sinh khác chức vụ, không có 1 giá trị role duy nhất để hiển thị động. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C185 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Người dùng chỉ ra số điện thoại hiển thị bị thiếu số 0 đầu (ví dụ `<a href="tel:966936362">966936362</a>` ở khối SĐT 1) và lưu ý tương tự vì SĐT 1 cũng dùng làm "username" đăng nhập trang công khai (C182); sau đó yêu cầu thêm bỏ placeholder mẫu trong 2 ô nhập của form đăng nhập. | Xác định đây là lỗi DỮ LIỆU (nhập/import từng làm mất số 0 đầu), không phải lỗi hiển thị — cột `sdt_1`/`sdt_2`/`phu_huynh.sdt` đều là `text` nên Postgres không tự mất số 0. Vá hiển thị sẽ gây lệch với đăng nhập thật (RPC so khớp đúng chuỗi lưu, vẫn thiếu số 0), nên sửa tận gốc bằng migration mới `20260728000100_them_so_0_dau_sdt.sql`: `update` 3 cột thêm `'0' ||` vào các giá trị khớp đúng mẫu 9 chữ số liền chưa có số 0 đầu (`^[1-9][0-9]{8}$`), không đụng định dạng khác để tránh sửa nhầm. Không cần đổi mã nguồn vì hiển thị/đăng nhập chỉ dùng đúng giá trị lưu trong CSDL; mật khẩu (3 số cuối) không đổi vì không ảnh hưởng. `StudentProfilePage.tsx`: bỏ `placeholder` ("VD: 0912345678", "VD: 678") ở 2 ô nhập form đăng nhập. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng**: đây là migration `UPDATE` dữ liệu thật — cần người dùng tự kiểm tra trước khi chạy trên Supabase Dashboard SQL Editor (có thể `select` thử theo đúng điều kiện regex để xem trước các dòng sẽ đổi); học sinh đã đăng nhập trước đó bằng số thiếu số 0 (lưu `sessionStorage`) sẽ cần đăng nhập lại bằng số đã đủ số 0 sau khi migration chạy. |
| C186 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Người dùng yêu cầu thêm validation cho 2 ô nhập (SĐT/mật khẩu) của form đăng nhập trang công khai hồ sơ. | `StudentProfilePage.tsx`: thêm hằng regex `SDT_PATTERN = /^0\d{9}$/` (đúng 10 chữ số, bắt đầu bằng số 0 — khớp định dạng chuẩn sau khi dữ liệu đã được sửa ở C185) và `MAT_KHAU_PATTERN = /^\d{3}$/` (đúng 3 chữ số). `handleLoginSubmit` kiểm tra định dạng trước khi gọi API: sai định dạng SĐT báo "Số điện thoại phải gồm đúng 10 chữ số, bắt đầu bằng số 0", sai định dạng mật khẩu báo "Mật khẩu phải gồm đúng 3 chữ số" — tránh gọi RPC vô ích với input chắc chắn sai định dạng, và giúp học sinh biết ngay lỗi ở đâu thay vì chỉ nhận thông báo chung "sai số điện thoại hoặc mật khẩu" sau khi gọi API. 2 ô nhập cũng lọc bỏ ký tự không phải chữ số ngay khi gõ (`.replace(/\D/g, '')`) và xoá thông báo lỗi cũ mỗi khi người dùng sửa lại (`setLoginError(null)` trong `onChange`), thêm `maxLength={10}` cho ô SĐT (mật khẩu đã có `maxLength={3}` từ trước). `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C187 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Người dùng báo trên di động, bấm nút "Thêm danh mục" (trang Danh mục) thì "hiện lên nhưng không thấy layout đâu" — modal mở nhưng không nhìn thấy nội dung. | Tìm ra lỗi gốc nằm ở `PullToRefresh.tsx` chứ không phải ở `CatalogPage.tsx`: div bọc `children` trong `PullToRefresh` luôn gán inline `style={{ transform: ... }}` kể cả khi không kéo gì (`pullDistance === 0` thì vẫn là `translateY(0px)`, không phải bỏ hẳn thuộc tính) — theo đúng chuẩn CSS, BẤT KỲ giá trị `transform` nào khác `none` trên một phần tử cha đều biến phần tử đó thành "containing block" mới cho mọi phần tử con dùng `position: fixed`. Vì `Layout.tsx` bọc toàn bộ nội dung trang giáo viên bằng `PullToRefresh`, mọi modal dùng `fixed inset-0` bên trong (rà bằng `grep` thấy ở `CatalogPage.tsx`, `StudentsPage.tsx`, `DashboardPage.tsx`, `ImportPage.tsx`, `AttendanceManagementPage.tsx` — cả 5 trang có modal kiểu này) đều bị "giam" bên trong div đó thay vì được định vị theo viewport thật: modal `flex items-center justify-center` sẽ canh giữa theo chiều cao của TOÀN BỘ nội dung trang (có thể rất cao do cuộn), khiến modal bị đẩy lệch xuống rất xa, nằm ngoài vùng nhìn thấy hiện tại của màn hình — đúng với hiện tượng "bấm vào hiện lên (nền tối `bg-slate-950/50` phủ kín) nhưng không thấy modal đâu" mà không cần cuộn xa mới thấy. Sửa tận gốc bằng 1 dòng: đổi `style={{ transform: refreshing ? undefined : ... }}` (luôn có `transform`) thành `style={!refreshing && pullDistance > 0 ? { transform: ... } : undefined}` — chỉ gắn `transform` khi thực sự đang kéo (`pullDistance > 0`), lúc bình thường (đứng yên) không có thuộc tính `transform` nào cả, khôi phục đúng hành vi `position: fixed` định vị theo viewport thật cho mọi modal trong toàn bộ 5 trang bị ảnh hưởng cùng lúc — không cần sửa riêng từng trang. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C188 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Sau C187, người dùng báo tiếp: các ô nhập/nút trong modal bị "vỡ layout lệch khỏi màn hình", cuộn xuống vẫn còn nội dung nhưng không cuộn tới được. | Lỗi thứ 2, độc lập với C187, nằm ngay trong cấu trúc modal: khối modal dùng `fixed inset-0 flex items-center justify-center` (không có `overflow-y-auto` ở lớp ngoài cùng) + hộp trắng bên trong giới hạn `max-h-[90vh] overflow-hidden`, phần nội dung cuộn riêng bằng `max-h-[calc(90vh-150px)] overflow-y-auto` — khi modal có nhiều trường (đặc biệt form Thêm/Sửa danh mục, Thêm/Sửa học sinh có nhiều ô) cộng thêm bàn phím ảo che bớt màn hình trên di động, tổng chiều cao thật cần thiết vượt quá chiều cao khả dụng, nhưng lớp ngoài `fixed` không có cách nào cuộn để lộ phần bị che (header/nút cuối form có thể bị đẩy ra ngoài vùng nhìn thấy vĩnh viễn) — đúng với mô tả "vỡ lệch, cuộn xuống không hết". Đối chiếu thấy `AttendanceManagementPage.tsx` đã có sẵn cách làm đúng cho đúng vấn đề này: lớp ngoài `fixed inset-0 ... items-start justify-center overflow-y-auto ... sm:items-center` (căn trên cùng + cho cuộn toàn bộ nền tối trên di động, chỉ căn giữa khi màn hình đủ cao từ `sm:`), hộp trắng dùng `my-8 ... sm:my-0` thay vì `max-h`/`overflow-hidden` cứng. Áp dụng đúng công thức này cho toàn bộ 6 modal còn lại dùng chung mẫu cũ bị lỗi: `CatalogPage.tsx` (Thêm/sửa danh mục), `StudentsPage.tsx` (Thêm/sửa học sinh), `ImportPage.tsx` (3 modal: tạo danh mục từ đề xuất AI, tạo danh mục thiếu từ JSON, tạo học sinh mới từ import), và `DashboardPage.tsx` (`OverviewDrillDownModal` dùng chung cho các bảng xem chi tiết) — bỏ hẳn `max-h-[90vh] overflow-hidden` ở hộp trắng và `max-h-[calc(90vh-150px)] overflow-y-auto` ở khối nội dung bên trong, để header/nội dung/nút bấm cuộn tự nhiên CÙNG NHAU theo đúng 1 lớp cuộn duy nhất (nền tối `overflow-y-auto`) thay vì 2 lớp cuộn lồng nhau dễ gây kẹt cử chỉ vuốt trên di động — đảm bảo luôn cuộn tới được mọi phần của form kể cả khi bàn phím ảo che một phần màn hình. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C189 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Sau C188, người dùng gửi ảnh chụp cho thấy modal vẫn "vỡ" (nút "Gợi ý xử lý" trong khối vàng bị bóp chữ xuống 3 dòng) và "kéo xuống hết nội dung nhưng không cuộn lên trên được". | 2 lỗi độc lập. (1) Bóp chữ nút: khối gợi ý xử lý dùng `flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between` — đúng lỗi quen thuộc của dự án (`--breakpoint-sm: 375px` bị override từ C008 khiến `sm:flex-row` kích hoạt ngay trên điện thoại thật), xếp đoạn text dài + nút "Gợi ý xử lý" chung 1 hàng quá sớm khiến nút bị bóp còn rất ít chỗ, chữ tự xuống dòng theo từng từ. Sửa theo đúng công thức đã dùng khắp dự án: `sm:` → `md:`, thêm `min-w-0` cho đoạn text, `shrink-0 whitespace-nowrap` cho nút. (2) "Không cuộn lên trên được" — lỗi nghiêm trọng hơn, nằm ở `PullToRefresh.tsx` chứ không phải ở modal: cơ chế kéo-làm-mới gắn listener `touchstart`/`touchmove` ở cấp `document` cho toàn app, chỉ kiểm tra `window.scrollY > 0` để biết có nên "nhường" cử chỉ vuốt cho việc cuộn trang bình thường hay không — nhưng khi modal đang mở, phần cuộn thật sự diễn ra bên trong div `overflow-y-auto` riêng của modal (từ C188), còn `window.scrollY` (cuộn của toàn trang phía sau modal) vẫn luôn là 0 suốt thời gian đó. Hệ quả: bất kỳ lúc nào người dùng vuốt xuống (để cuộn NGƯỢC LÊN xem nội dung phía trên trong modal) đều bị `PullToRefresh` hiểu nhầm là đang "kéo để làm mới trang", tự động gọi `event.preventDefault()` chặn đứng cử chỉu cuộn thật của modal — đúng khớp mô tả "cuộn xuống được (vuốt lên) nhưng không cuộn lên được (vuốt xuống)". Sửa tận gốc: thay điều kiện `window.scrollY > 0` bằng hàm mới `getScrollTop(event.target)` — tự dò tìm phần tử cha gần nhất có thể cuộn thật sự (`overflowY: auto/scroll` và `scrollHeight > clientHeight`) tính từ đúng vị trí ngón tay đang chạm, rồi kiểm tra `scrollTop` của CHÍNH phần tử đó (modal hay trang chính đều tự động nhận diện đúng); nếu phần tử đó đang cuộn dở (`scrollTop > 0`) thì đứng yên, nhường hoàn toàn cho cuộn tự nhiên của trình duyệt — không cần biết trước đó là trang hay modal, tổng quát cho mọi vùng cuộn lồng nhau trong tương lai chứ không vá riêng từng modal. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C190 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Sau 3 lần vá liên tiếp (C187-C189) vẫn còn lỗi hiển thị, người dùng yêu cầu thiết kế lại HOÀN TOÀN layout modal "Thêm danh mục", không vá tiếp nữa. | Bỏ hẳn cấu trúc modal kiểu "hộp cố định + 2 lớp cuộn lồng nhau" từng gây lỗi, thay bằng mẫu chuẩn "sheet toàn màn hình trên di động, dialog giữa màn hình trên desktop": khung ngoài `fixed inset-0` chỉ bật `md:flex md:items-center md:justify-center md:p-4` (dưới `md` không flex/center gì cả, chiếm trọn màn hình); khung form đổi thành `flex h-full w-full flex-col` (dưới `md`, chiếm đúng 100% chiều cao/rộng màn hình thật) và `md:h-auto md:max-h-[85vh] md:max-w-4xl md:rounded-lg md:shadow-xl` (desktop mới co lại thành hộp giữa màn hình). Nhờ `flex-col` với header/footer là `shrink-0` và chỉ đúng 1 khối giữa `min-h-0 flex-1 overflow-y-auto`, CHỈ có duy nhất 1 vùng cuộn cho toàn bộ modal (không còn modal lồng trong nền cuộn lồng nhau như C188/C189) — header (tiêu đề + nút "✕" đóng, thêm padding-top an toàn `env(safe-area-inset-top)` cho tai thỏ) và footer (nút Huỷ/Lưu, thêm padding-bottom an toàn `env(safe-area-inset-bottom)` cho thanh cử chỉ) luôn cố định, không bao giờ bị cuộn mất khỏi màn hình dù nội dung dài cỡ nào. Đồng thời tổ chức lại nội dung thành các thẻ (section) rõ ràng theo nhóm chức năng: "Thông tin cơ bản" (Mã/Nhóm/Nội dung/Điểm/Phạm vi, xếp `sm:grid-cols-2` thay vì nhồi 6 cột), "Mô tả & xử lý" (2 textarea xếp dọc luôn thay vì cố xếp ngang), "Mã xử lý/phạt liên kết" (select + nút "Tạo mã xử lý mới" xếp dọc, nút full-width trên di động), khối gợi ý xử lý (nút full-width trên di động, không còn xếp hàng ngang gây bóp chữ). Nút "Lưu danh mục" ở footer đổi thành `flex-1` (rộng hết cỡ) trên di động để dễ bấm bằng ngón cái, thu về kích thước vừa (`sm:flex-none`) từ tablet trở lên. Nhân dịp thiết kế lại, xoá hẳn 1 khối `<form>` cũ trùng lặp đã bị vô hiệu hoá vĩnh viễn bằng `{false ? (...) : null}` từ trước (code chết, không ai render tới, gây rối khi đọc file) thay vì giữ lại. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C191 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng muốn thêm nút copy nhanh số điện thoại trên giao diện di động, và hiển thị 2 số điện thoại PH ngay tại khối "Cần liên lạc phụ huynh" (trang Điểm danh) để giáo viên bấm gọi luôn tại vị trí nhắc, không cần mở qua trang khác. | Khối "Cần liên lạc phụ huynh" (`AttendanceManagementPage.tsx`) trước đây chỉ hiện tên/ngày/buổi + nút "Ghi nhận"/"Xoá", không có số điện thoại — phải mở sang trang chi tiết học sinh mới gọi được, mất thời gian ngay lúc cần gọi gấp. **`types.ts`**: thêm `sdt_1`/`sdt_2` vào `DiemDanhCanLienLac`. **`SupabaseDataSource.ts`**: `getPendingParentContacts()` đã sẵn join với `this.getStudents()` (có đủ `sdt_1`/`sdt_2`) để lấy tên/tổ/diện — chỉ cần bổ sung 2 dòng lấy thêm `sdt_1`/`sdt_2` từ đúng học sinh đó, không cần đổi RPC hay query mới. **`AttendanceManagementPage.tsx`**: thêm component `PhoneQuickAction` — hiển thị số điện thoại dạng `tel:` link (bấm gọi ngay) kèm 1 nút icon copy riêng biệt bên cạnh (dùng `navigator.clipboard.writeText`, báo "Đã copy SĐT X: ..." qua toast `message` dùng chung đã có sẵn trong trang, tự ẩn sau 2.5s giống mẫu copy link hồ sơ ở `StudentsPage.tsx`) — tách bạch 2 hành động (gọi ngay vs copy để dán sang Zalo/tin nhắn) thay vì ép chung 1 nút. Card mỗi lượt cần liên lạc đổi từ `sm:flex-row sm:items-center sm:justify-between` (tên+nút cùng hàng) sang `flex-col` cố định luôn để có chỗ chèn thêm hàng số điện thoại mà không bị bóp; 2 nút "Ghi nhận"/"Xoá" đổi "Ghi nhận" thành `flex-1 sm:flex-none` (rộng hết cỡ trên di động, dễ bấm bằng ngón cái ngay khi đang cầm máy gọi xong). `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C192 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Sau C191, người dùng báo: chế độ giáo viên xem trang chi tiết học sinh (khối SĐT 1/SĐT 2/số phụ huynh) chưa thấy icon copy — C191 mới làm ở trang Điểm danh. Sau đó yêu cầu đổi icon copy giống icon copy trong các khung trả lời của Claude Code web (icon SVG 2 hình chữ nhật chồng nhau, không phải emoji). | **`TeacherStudentDetailPage.tsx`**: `PhoneRow` (dùng chung cho SĐT 1, SĐT 2, và từng số phụ huynh) thêm nút copy cạnh số điện thoại, tách biệt với link `tel:` bấm gọi — nhấn copy gọi `navigator.clipboard.writeText`, đổi icon tạm thời sang trạng thái "đã copy" trong 2 giây bằng state `copied` cục bộ ngay trong `PhoneRow` (không cần toast chung, vì phản hồi ngay trên icon là đủ rõ ở quy mô 1 dòng số điện thoại). **`src/components/CopyIcon.tsx`** (mới): component icon SVG dùng chung — vẽ tay 2 icon dạng outline quen thuộc trong các nút copy code/câu trả lời (không phải emoji): icon "copy" mặc định (2 hình chữ nhật bo góc chồng nhau) và icon "đã copy" (dấu tích ✓), chuyển đổi qua prop `copied: boolean`. Áp dụng icon mới này cho cả `PhoneRow` (`TeacherStudentDetailPage.tsx`) và `PhoneQuickAction` (`AttendanceManagementPage.tsx`, thay emoji 📋 cũ từ C191) để đồng nhất 1 kiểu icon copy xuyên suốt toàn app thay vì mỗi nơi 1 kiểu khác nhau. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C193 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng cung cấp sẵn tài liệu hướng dẫn `tao csdl/huong-dan-khoa-zoom-mobile.md` (khóa pinch-to-zoom, chặn Safari tự động zoom khi focus input, chặn hiệu ứng rubber-band khi cuộn quá đầu/cuối trang, bỏ độ trễ 300ms khi chạm) và yêu cầu triển khai. | Đọc kỹ `index.html`/`src/index.css` trước khi sửa theo đúng yêu cầu của guide, đồng thời phát hiện 2 điểm cần điều chỉnh so với hướng dẫn gốc cho khớp với codebase thực tế (không áp dụng máy móc): (1) hướng dẫn gốc đề nghị thêm `padding-top`/`padding-bottom: env(safe-area-inset-*)` thẳng vào `body`, nhưng `Layout.tsx` đã tự tính padding an toàn riêng cho từng phần (`header` dùng `pt-[env(safe-area-inset-top)]`, `main` dùng `pb-[calc(5rem+env(safe-area-inset-bottom))]`, thanh tab dưới cũng vậy) — nếu thêm luôn vào `body` sẽ bị CỘNG DỒN 2 lần khoảng đệm, đẩy nội dung xuống/lên sai vị trí; nên CHỈ thêm `padding-left`/`padding-right` cho `body` (chưa nơi nào xử lý, chỉ có tác dụng khi xoay ngang trên máy có tai thỏ, không xung đột với padding trên/dưới đã có). (2) hướng dẫn gốc tự lưu ý "cân nhắc không áp dụng `user-scalable=no` cho route công khai `lay_ho_so_cong_khai` vì phụ huynh lớn tuổi cần phóng to" — xử lý cụ thể bằng cách thêm 1 `useEffect` trong `StudentProfilePage.tsx` (trang `/hs/:token`), khi mount sẽ tạm đổi `<meta name="viewport">` sang bản cho phép zoom bình thường (`width=device-width, initial-scale=1, viewport-fit=cover`, không có `maximum-scale=1`/`user-scalable=no`), và khôi phục lại nội dung gốc (khoá zoom) khi unmount — chỉ đúng trang công khai này được zoom, mọi trang giáo viên khác vẫn khoá. **`index.html`**: viewport mặc định đổi thành có `maximum-scale=1, user-scalable=no` (khoá pinch-zoom cho toàn bộ trang giáo viên). **`src/index.css`**: thêm `input, select, textarea { font-size: 16px }` (chặn Safari tự zoom khi focus ô nhập — do được viết dạng rule thường không nằm trong `@layer` nào nên tự động có độ ưu tiên cao hơn mọi class Tailwind như `text-sm` theo đúng cơ chế cascade layers của Tailwind v4, không cần `!important`); `html, body { height: 100dvh; overscroll-behavior: none }` (chặn hiệu ứng kéo giãn cao su, dùng `dvh` thay `vh` để tính đúng khi thanh địa chỉ ẩn/hiện); `* { touch-action: manipulation }` (bỏ độ trễ 300ms khi chạm, vẫn cho phép cuộn/kéo bình thường, không ảnh hưởng logic chạm tuỳ biến của `PullToRefresh`). Cố tình KHÔNG đổi các chỗ đang dùng `min-h-screen` (Tailwind = `100vh`) trong `Layout.tsx`/`StudentProfilePage.tsx` sang `100dvh` như gợi ý phụ của guide vì không bắt buộc và có rủi ro vỡ layout không cần thiết ở phạm vi thay đổi này. `tsc`/`npm run build`/`npm test` (10/10) đều pass. Đây là thay đổi ảnh hưởng tương tác chạm/zoom trên toàn app — cần người dùng tự kiểm tra lại trên điện thoại thật theo đúng checklist trong `tao csdl/huong-dan-khoa-zoom-mobile.md` (đặc biệt: pinch-zoom không còn hoạt động ở trang giáo viên nhưng vẫn zoom được ở trang hồ sơ công khai; focus vào ô nhập không bị tự zoom; cuộn quá đầu/cuối trang không còn hiệu ứng cao su; các trang chính không bị vỡ layout). |
| C194 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng yêu cầu áp dụng luôn khoá zoom (C193) cho trang xem hồ sơ học sinh công khai `/hs/:token` — huỷ ngoại lệ nới lỏng zoom đã chủ động thêm ở C193 cho đối tượng phụ huynh lớn tuổi. | `StudentProfilePage.tsx`: bỏ hẳn `useEffect` vừa thêm ở C193 (tạm đổi `<meta name="viewport">` sang bản cho phép zoom khi mount, khôi phục khi unmount) — trang công khai giờ dùng chung đúng 1 thẻ viewport khoá zoom (`maximum-scale=1, user-scalable=no`) trong `index.html` như mọi trang giáo viên khác, không còn ngoại lệ theo route nữa. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C195 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng cung cấp đặc tả `bc/08-dac-ta-bao-cao-tuan-thang.md` (do một AI kiến trúc sư khác soạn dựa trên `schema.sql`/`DashboardPage.tsx`/`AttendanceManagementPage.tsx` đã xuất trước đó) và yêu cầu triển khai tính năng Báo cáo Tuần/Tháng: xem nhanh Chuyên cần + Vi phạm nề nếp + Ghi nhận tích cực trong 1 khoảng ngày, xuất ra Word/PDF. | Đọc kỹ đặc tả và code thật trước khi viết, phát hiện tên hàm đặc tả nêu (`isViolationRecord`, `isPositiveRecord`) là hàm cục bộ (không export) trong `DashboardPage.tsx` — thêm `export` cho đúng các hàm này cùng `getRecordTypeLabel`, `formatDate`, `formatDateCompact` để tái dùng nguyên văn thay vì viết lại logic trùng, đúng yêu cầu "tái dùng, không viết lại" của đặc tả. **`src/features/reports/reportData.ts`** (mới): 1 hàm `buildReportData({ tuNgay, denNgay, students, attendanceEntries, records, catalog, contactHistory })` tính toán chung cho cả 2 tab (chỉ khác cách chọn khoảng ngày, không khác cách tính, đúng mục 1 đặc tả) — gộp trạng thái điểm danh 2 buổi/ngày theo ưu tiên vắng không phép > vắng có phép > trễ, tra "đã liên lạc PH?" qua `diem_danh_id` hoặc fallback 3 cột chụp nhanh `ma_hs+ngay+buoi` khi `diem_danh_id` đã null (đúng migration `20260724000300`); nhóm vi phạm theo `ghi_nhan.loai` (không dùng `danh_muc_diem.nhom` vì `ma_danh_muc` có thể null), trong mỗi nhóm gom tiếp theo mã, dòng `ma_danh_muc = null` luôn xếp cuối nhóm dưới tên "Chưa phân loại mã cụ thể" (không tự gán mã); tách sự kiện tập thể/tổ trực (`ma_hs is null`) khỏi bảng vi phạm cá nhân, chỉ đếm riêng 1 dòng tổng số + số đã xử lý. **`src/features/reports/ReportsPage.tsx`** (mới): 2 tab Tuần/Tháng dùng chung 1 khoảng `[tu_ngay, den_ngay]`; tab Tuần tái dùng nguyên `WeekSelector`/`selectDefaultWeek` có sẵn, thêm switch "Tuỳ chỉnh khoảng ngày" ghi đè tuần khi bật; tab Tháng dùng `<input type="month">`, tự tính ngày 1 → ngày cuối tháng. Khu vực preview hiển thị đúng nội dung sẽ có trong file xuất, không phân trang (khác quy ước "5 dòng/trang" đã áp dụng ở các trang khác trong app — cố tình bỏ qua ở đây vì đây là bản xem trước của TOÀN BỘ tài liệu sẽ xuất ra, phân trang preview sẽ gây hiểu lầm "file xuất cũng chỉ có 5 dòng"). **Xuất Word** (`exportWord.ts`, dùng thư viện `docx` mới cài): dựng `Document` với `Heading`/`Paragraph`/`Table` cho từng phần, tải qua `Packer.toBlob` + `<a download>` giống kỹ thuật `downloadPrintableForm.ts` đã có. **Xuất PDF** (`exportPdf.ts`, dùng `jspdf` mới cài): đặc tả yêu cầu nhúng font Unicode base64 qua `addFileToVFS`/`addFont` cho đúng dấu tiếng Việt, nhưng dự án không có sẵn file font thật (`.ttf`) và môi trường agent không truy cập được internet để tải — thay vì nhúng font giả/không hợp lệ (sẽ làm hỏng hoặc mất dấu), chuyển sang dùng `doc.html()` của jsPDF (đã có `html2canvas` kèm sẵn dạng optional dependency) để chụp lại đúng DOM đã dựng (font trình duyệt hiển thị đúng, không mất dấu) rồi nhúng vào PDF dạng ảnh — đánh đổi: chữ trong PDF là ảnh, không bấm chọn/copy/tìm kiếm được như PDF văn bản thật, đã ghi rõ đánh đổi này trong code. Gỡ bỏ `jspdf-autotable` (cài ban đầu theo gợi ý đặc tả nhưng không dùng được vì cùng vướng vấn đề font). **Bundle size**: `docx`+`jspdf`+`html2canvas` làm bundle chính tăng gần gấp đôi (830KB → 1.6MB) nếu import tĩnh — sửa bằng `import()` động ngay trong handler bấm nút xuất ở `ReportsPage.tsx`, tách `docx`/`jspdf`/`html2canvas` thành 3 chunk riêng chỉ tải khi thực sự bấm xuất, đưa bundle chính về gần như cũ (853KB, chỉ tăng ~21KB cho code trang Báo cáo). Thêm route `/bao-cao` (`router.tsx`) + mục menu "🧾 Báo cáo" (`Layout.tsx`, dùng chung mảng `navItems` cho cả menu desktop lẫn thanh tab dưới di động). `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Phạm vi đã làm đúng đặc tả**: chỉ 2 tab Tuần/Tháng, KHÔNG làm Học kỳ/Năm học, KHÔNG gộp sự kiện tập thể vào bảng cá nhân, KHÔNG thêm điểm trung bình lớp. Chưa tự kiểm tra được file Word/PDF xuất ra trên thiết bị thật (không có Word/trình đọc PDF trong môi trường agent để mở lại kiểm tra bố cục) — cần người dùng tự mở thử file xuất từ 1 tuần/tháng có đủ cả 3 loại dữ liệu (vắng, vi phạm, khen thưởng) để xác nhận đúng nội dung và định dạng trước khi dùng thật cho sinh hoạt lớp. |
| C196 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng yêu cầu thêm tính năng chia sẻ file báo cáo (Word/PDF vừa xuất ở C195) trên di động qua các app khác như Zalo/Facebook/Telegram, thay vì chỉ tải về máy. | Thêm `src/features/reports/shareFile.ts` — hàm `shareOrDownloadFile(blob, filename, mimeType, shareTitle)` dùng Web Share API cấp 2 (`navigator.canShare({ files })` + `navigator.share({ files })`, đã có sẵn kiểu dữ liệu chuẩn trong TypeScript DOM lib, không cần khai báo type thủ công): nếu trình duyệt hỗ trợ chia sẻ kèm file (chủ yếu Android Chrome, iOS Safari bản mới — đúng đối tượng "trên di động" người dùng yêu cầu), mở hộp thoại chia sẻ gốc của hệ điều hành cho phép gửi thẳng file vào Zalo/Messenger/Telegram/Drive... mà không cần tải về rồi tự đính kèm tay; nếu người dùng tự huỷ hộp thoại chia sẻ (`DOMException` tên `AbortError`) thì coi là thao tác bình thường, không rơi về tải file. Nếu trình duyệt không hỗ trợ (đa số desktop) thì tự động quay lại đúng cách tải file qua Blob URL + thẻ `<a download>` như cũ, không mất chức năng tải về. **`exportWord.ts`**: đổi `downloadBlob(blob, ...)` cũ thành gọi `shareOrDownloadFile` với mime type chuẩn của `.docx` (`application/vnd.openxmlformats-officedocument.wordprocessingml.document`), xoá hàm `downloadBlob` cục bộ không còn dùng. **`exportPdf.ts`**: đổi callback của `doc.html()` từ gọi thẳng `finishedDoc.save(filename)` (chỉ tải, không có Blob để chia sẻ) sang lấy `pdfDoc.output('blob')` rồi cũng đưa qua `shareOrDownloadFile` với mime type `application/pdf`. Thêm dòng chú thích nhỏ dưới 2 nút xuất trong `ReportsPage.tsx`: "Trên điện thoại, có thể chọn gửi thẳng qua Zalo/Messenger/Telegram." để giáo viên biết tính năng mới mà không cần đổi tên nút (nút vẫn ghi "Xuất Word"/"Xuất PDF" vì hành vi thật (chia sẻ hay tải) tự động tuỳ trình duyệt, đổi tên nút theo 1 hành vi cụ thể sẽ sai trên thiết bị dùng hành vi còn lại). `tsc`/`npm run build`/`npm test` (10/10) đều pass, `shareFile.ts` tách thành chunk riêng rất nhỏ (~0.45KB), không ảnh hưởng kích thước bundle chính hay 2 chunk xuất file đã tách lazy từ C195. **Lưu ý cần kiểm tra trên thiết bị thật**: Web Share API yêu cầu gọi trong đúng "cửa sổ tương tác người dùng" (user activation) tính từ lúc bấm nút — vì việc dựng file Word/PDF là bất đồng bộ (đặc biệt PDF phải đợi `html2canvas` chụp xong DOM, có thể mất 1-2 giây), một số trình duyệt (đặc biệt Safari/iOS ở các bản cũ hơn) có thể từ chối chia sẻ nếu khoảng chờ quá lâu và tự rơi về tải file thay vì mở được hộp thoại chia sẻ — cần người dùng tự thử trên điện thoại thật (cả Android lẫn iPhone nếu có) để xác nhận hộp thoại chia sẻ có mở đúng hay không, đặc biệt với file PDF (chờ lâu hơn Word). Nhân dịp thêm nút, phát hiện thêm dòng chú thích mới khiến khối `flex flex-col gap-3 ... sm:flex-row sm:items-center sm:justify-between` (tiêu đề + khối nút xuất) bị vỡ layout trên di động — cột nút giờ có 2 nút + 1 dòng chú thích xếp `items-end` bên cạnh tiêu đề dài, đúng lỗi quen thuộc của dự án (`sm:` kích hoạt quá sớm ở 375px). Thiết kế lại: đổi `sm:` → `md:` cho hàng ngoài (tiêu đề/khối nút chỉ xếp ngang từ 768px), 2 nút xuất bên trong đổi `w-full sm:w-auto` (rộng hết cỡ khi xếp dọc trên di động hẹp, xếp ngang gọn từ 375px khi đã tách khỏi tiêu đề dài), dòng chú thích thêm `md:text-right` để canh phải khớp khối nút khi lên desktop. `tsc`/`npm run build`/`npm test` (10/10) đều pass.Sau đó người dùng báo lỗi thật khi bấm Xuất PDF: "Attempting to parse an unsupported color function "oklch"" — do `html2canvas` (dùng trong `doc.html()`) không đọc được hàm màu CSS `oklch()`, trong khi nội dung offscreen tự dựng chỉ dùng màu hex inline nhưng lại được gắn thẳng vào `document.body` của app, khiến `html2canvas` đọc phải style kế thừa/tính toán từ `<body>`/`<html>` (Tailwind CSS v4 xuất màu bằng `oklch()` mặc định) rồi báo lỗi. Sửa bằng cách dựng nội dung trong 1 `<iframe>` có `document` rỗng riêng (`iframe.contentDocument`, không nạp CSS của app) thay vì gắn thẳng vào `document.body` — nội dung báo cáo hoàn toàn tách biệt khỏi mọi style Tailwind của app, tránh được lỗi `oklch` tận gốc. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C197 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng cung cấp đặc tả `bc/09-dinh-dang-trinh-bay-bao-cao.md` (bổ sung cho tài liệu 08, chỉ sửa cách trình bày Word/PDF, không đổi số liệu) yêu cầu thêm quốc hiệu, thông tin lớp/GVCN/sĩ số, tiêu đề trang trọng, font Times New Roman, bảng có viền/tô nền, và khối ký tên cuối trang. | **`reportConfig.ts`** (file mới theo đúng mục 1 đặc tả): hằng số `REPORT_CONFIG` (tên trường/lớp/năm học/GVCN/địa điểm ký — `tenGvcn` để `'[Điền tên GVCN]'` làm placeholder, cần giáo viên tự điền tên thật trước khi dùng thật đúng như đặc tả yêu cầu) và type `ReportPresentationMeta` (title/subtitle/soHocSinh) dùng chung cho cả 2 file export. **`DashboardPage.tsx`**: đổi `isActiveStudent(student)` (trước đó luôn so với "hôm nay" cố định) thành `isActiveStudent(student, referenceDate = new Date())` nhận thêm tham số ngày mốc tuỳ chọn rồi export ra — cần thiết vì đặc tả yêu cầu tính "sĩ số tại thời điểm CUỐI KỲ báo cáo", không phải sĩ số hôm nay (báo cáo về 1 tuần/tháng đã qua phải phản ánh đúng sĩ số tại thời điểm đó, ví dụ học sinh mới chuyển đến sau kỳ báo cáo thì không được tính); sửa xong vẫn tương thích ngược 100% với chỗ gọi cũ trong `DashboardPage.tsx` (không truyền tham số 2 thì mặc định vẫn là hôm nay như trước). **`ReportsPage.tsx`**: thêm tính `meta` (title/subtitle "Từ {ngày đủ có thứ} đến {ngày đủ có thứ}"/sĩ số học sinh đang học tính đến đúng `denNgay` của kỳ báo cáo), truyền xuống cả `exportReportToWord`/`exportReportToPdf` thay cho tham số `title` đơn lẻ trước đây. **`exportWord.ts`** (viết lại đáng kể, dùng thư viện `docx`): thêm khối quốc hiệu (2 dòng in đậm căn giữa + 1 dòng gạch ngang ngắn), khối thông tin lớp căn trái (tên trường/lớp+sĩ số+năm học/GVCN), tiêu đề in hoa đậm cỡ 16pt căn giữa + dòng phụ in nghiêng nhỏ hơn; đặt font mặc định toàn tài liệu là Times New Roman 13pt qua `styles.default.document` (áp dụng cho MỌI đoạn văn, không cần lặp lại từng chỗ) — riêng tiêu đề phần ("Phần 1 — ...") và hàng tiêu đề bảng đặt cỡ 14pt đậm theo đúng đặc tả; bảng thêm `borders` (viền đơn xám cho toàn bộ ô, không chỉ viền ngoài) + hàng tiêu đề tô nền `#F2F2F2` đậm, cột số liệu (STT/Số lượt/Ngày/Trạng thái/Mã/Đã liên lạc PH?) căn giữa còn cột văn bản (Họ tên/Nội dung/Tên vi phạm...) căn trái; dòng thẻ tổng hợp đổi từ 1 câu văn thường sang ghép nhiều `TextRun` để in đậm riêng từng con số, số vi phạm nghiêm trọng tô thêm màu đỏ đậm (`C00000`); thêm khối ký tên cuối trang (địa điểm+ngày lập báo cáo LẤY THEO NGÀY GIỜ THẬT LÚC BẤM XUẤT chứ không phải ngày cuối kỳ báo cáo, "GIÁO VIÊN CHỦ NHIỆM", dòng "(Ký và ghi rõ họ tên)", khoảng trống để ký tay, tên GVCN) — tất cả căn phải theo đúng đặc tả. **`exportPdf.ts`**: áp dụng ĐÚNG cùng bố cục letterhead/tiêu đề/khối ký tên như bản Word (không làm PDF sơ sài hơn, đúng yêu cầu mục 5 của đặc tả) — vì PDF của dự án này render qua `doc.html()`/`html2canvas` (không phải qua `jspdf-autotable` như đặc tả giả định, do C195 đã phải đổi hướng vì thiếu font Unicode thật, xem lý do ở C195/PROGRESS.md) nên chỉ cần thêm đúng HTML/CSS tương ứng (font Times New Roman, viền bảng, tô nền header, chữ đỏ đậm cho số nghiêm trọng, khối ký tên căn phải) vào cùng hàm dựng HTML offscreen đã có sẵn — không cần vẽ tay đường gạch ngang bằng canvas, dùng luôn ký tự `──────────` giống bản Word đúng như đặc tả gợi ý khi khó vẽ border thủ công. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng cần xử lý trước khi gửi báo cáo thật ra ngoài lớp**: `tenGvcn` trong `reportConfig.ts` vẫn đang để nguyên `'[Điền tên GVCN]'` — giáo viên PHẢI tự sửa thành tên thật trước khi dùng, nếu không dòng chữ đó sẽ in nguyên văn trong mọi báo cáo xuất ra. Chưa tự mở lại file Word/PDF thật trên thiết bị để so khớp đúng bố cục (không có Word/trình đọc PDF trong môi trường agent) — cần người dùng tự xuất thử 1 báo cáo và kiểm tra letterhead/bảng/khối ký tên trước khi dùng chính thức. |
| C198 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Sau C197, người dùng báo lỗi "Attempting to parse an unsupported color function "oklch"" vẫn còn khi bấm Xuất PDF — hoá ra cách sửa bằng `<iframe>` document rỗng ở C196 không thật sự có tác dụng. | Đọc thẳng mã nguồn `node_modules/jspdf/dist/jspdf.es.js` (hàm `Worker.prototype.toContainer`/`cloneNode`) để tìm đúng nguyên nhân thay vì đoán: `doc.html()` của jsPDF tự `node.cloneNode(false)` (nhân bản) nội dung nguồn rồi bọc vào 1 `<div>` MỚI tạo bằng `document.createElement` (document toàn cục) và gọi thẳng `document.body.appendChild(...)` để dựng canvas render — nghĩa là bất kể container nguồn được đặt ở đâu (kể cả trong `<iframe>` tách biệt như C196 đã làm), bản sao cuối cùng luôn bị gắn thẳng vào `<body>` THẬT của trang, kế thừa `background-color`/`color` của `body { @apply ... bg-slate-200 text-slate-900 }` (Tailwind CSS v4 xuất bằng `oklch()`) — đây mới là nguồn `oklch` thật sự, không phải nội dung tự dựng (vốn đã toàn dùng màu hex inline từ đầu). Bỏ hẳn cách tách iframe (không giải quyết đúng vấn đề, chỉ thêm phức tạp không cần thiết), sửa tận gốc bằng cách tạm ghi đè `document.body.style.backgroundColor`/`color` sang giá trị hex thường (`#ffffff`/`#0f172a`) ngay trước khi gọi `doc.html()`, khôi phục lại giá trị cũ ngay trong khối `finally` (đảm bảo khôi phục dù xuất lỗi hay xuất thành công, không để lại tác dụng phụ nhìn thấy được trên giao diện vì toàn bộ quá trình diễn ra rất nhanh và `body` không re-render lại trong lúc này). Thêm `html2canvas: { backgroundColor: '#ffffff' }` vào tuỳ chọn `doc.html()` để chắc chắn nền canvas cũng không suy luận màu từ nơi khác. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý**: đây là sửa lần 2 cho cùng 1 lỗi — lần sửa ở C196 chưa thực sự đúng nguyên nhân (chỉ dựa trên suy đoán hợp lý nhưng chưa đọc mã nguồn thư viện), lần này đã xác nhận qua đọc trực tiếp code `jspdf.es.js`; vẫn cần người dùng tự bấm Xuất PDF trên trình duyệt thật để xác nhận lỗi `oklch` đã hết hẳn, vì agent không có trình duyệt thật để tự kiểm tra lại. |
| C199 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng điền tên GVCN thật ("Nguyễn Duy Tân") và yêu cầu thêm phần chữ ký của ban cán sự lớp vào báo cáo xuất Word/PDF. | **`reportConfig.ts`**: `tenGvcn` đổi từ placeholder `'[Điền tên GVCN]'` sang tên thật "Nguyễn Duy Tân". Thêm `BAN_CAN_SU_SIGNATURE_ROLES = ['Lớp trưởng', 'Lớp phó học tập', 'Lớp phó lao động', 'Bí thư chi đoàn']` — chỉ chọn 4 chức vụ cấp lớp (bỏ qua "Tổ trưởng"/"Tổ phó" vì đây là chức vụ theo từng tổ, số lượng có thể nhiều và thay đổi theo cách chia tổ, không phải đại diện chung cho cả lớp khi ký xác nhận 1 bản báo cáo) và interface `BanCanSuSignature { chucVu, hoTen }`, thêm field `banCanSuSignatures` vào `ReportPresentationMeta`. **`ReportsPage.tsx`**: thêm `dataSource.getBanCanSu()` vào lượt fetch dữ liệu ban đầu (song song với các fetch khác đã có); tính `banCanSuSignatures` bằng cách tra, với mỗi chức vụ trong `BAN_CAN_SU_SIGNATURE_ROLES`, xem có học sinh nào đang giữ chức vụ đó trong `ban_can_su` không rồi lấy họ tên tương ứng — chức vụ nào chưa có ai giữ thì bỏ qua hẳn (không hiện dòng ký tên trống "chưa có" gây rối bản in). **`exportWord.ts`**: thêm `buildBanCanSuSignatures(meta)` — dựng 1 bảng KHÔNG viền (`NO_TABLE_BORDERS`/`NO_CELL_BORDERS` mới, dùng `BorderStyle.NONE`) chia đều số cột theo số chức vụ có người giữ, mỗi cột: tên chức vụ (đậm) + "(Ký, ghi rõ họ tên)" (nghiêng, nhỏ) + khoảng trống để ký tay + họ tên (đậm) — đặt tiêu đề "XÁC NHẬN CỦA BAN CÁN SỰ LỚP" (đậm, căn giữa) phía trên bảng, chèn ngay TRƯỚC khối ký tên GVCN đã có (không gộp chung 1 bảng vì 2 vai trò khác nhau, ban cán sự và GVCN không cùng ký trên 1 hàng theo mẫu văn bản thông thường). Chỉ render khi có ít nhất 1 chức vụ đã có người giữ (`meta.banCanSuSignatures.length === 0` thì bỏ qua hẳn phần này). **`exportPdf.ts`**: `renderBanCanSuSignatures(meta)` tương tự nhưng dùng `<table>` HTML thường (không dùng `display:flex` dù đơn giản hơn, vì `html2canvas` hỗ trợ flexbox không ổn định/đầy đủ bằng bảng, và toàn bộ phần còn lại của trang PDF này vốn đã dùng bảng nhất quán) — mỗi `<td>` là 1 cột chức vụ, chia đều chiều rộng theo số cột, không viền, nội dung giống bản Word. `tsc`/`npm run build`/`npm test` (10/10) đều pass. Chưa tự mở lại file Word/PDF thật để xem bố cục 2 khối ký tên (ban cán sự + GVCN) có đúng như hình dung hay không — cần người dùng tự xuất thử 1 báo cáo có đủ ban cán sự (Lớp trưởng, Lớp phó...) để xác nhận. |
| C200 | ⛔ Bị chặn (cần xác nhận) | 29/07/2026 | Người dùng báo 2 việc: (1) thiết kế lại các bảng trong báo cáo cho tách nhau độc lập rõ ràng và thêm cột STT; (2) file PDF xuất ra không có nội dung, đang rỗng. | **(2) Lỗi PDF rỗng — sửa trước vì nghiêm trọng hơn**: tìm ra nguyên nhân qua đọc lại đúng đoạn code `jsPDF` đã đọc ở C198 (`Worker.toContainer`) — `doc.html()` chỉ `cloneNode()` ĐÚNG phần tử được truyền vào (không phải tổ tiên của nó) rồi gắn bản sao vào 1 wrapper riêng của chính nó (wrapper này đã tự đưa ra ngoài màn hình bằng `position:fixed; left:-100000px` sẵn có). Vì `container` truyền cho `doc.html()` ở C195-C198 tự khai báo `position:fixed; left:-9999px` NGAY TRÊN CHÍNH NÓ, bản sao sau khi bị gắn vào wrapper của jsPDF vẫn giữ nguyên khai báo đó và tự "thoát" khỏi wrapper, định vị lại theo viewport thật — nằm ngoài vùng `html2canvas` chụp, cho ra ảnh/PDF trắng trơn (lỗi này tồn tại từ C195, chỉ không lộ ra vì bị lỗi `oklch` che mất trước khi kịp render tới bước này). Sửa bằng cách tách làm 2 lớp: 1 `hiddenWrapper` (mới, có `position:fixed;left:-9999px` để ẩn khỏi màn hình chính) bọc ngoài `container` (không còn tự định vị gì cả) — chỉ truyền `container` (lớp trong) cho `doc.html()`, còn `hiddenWrapper` không được truyền đi nên không bị nhân bản/ảnh hưởng, nhờ đó bản sao của jsPDF hoạt động bình thường bên trong wrapper của chính nó mà không "thoát" ra ngoài. **(1) Thiết kế lại bảng**: áp dụng đồng bộ cả 3 nơi (xem trước trên web `ReportsPage.tsx`, `exportWord.ts`, `exportPdf.ts`) — thêm cột STT (căn giữa) vào 3 bảng trước đó thiếu (bảng "Theo nhóm vi phạm", bảng "Chi tiết theo mã vi phạm", bảng Ghi nhận tích cực — bảng Chuyên cần đã có STT từ trước); 2 bảng vi phạm ("Theo nhóm" và "Chi tiết theo mã") vốn nằm sát nhau nhìn như 1 bảng liền mạch, nay thêm 1 dòng tiêu đề phụ in nghiêng đậm nhỏ phía trên MỖI bảng ("Theo nhóm vi phạm" / "Chi tiết theo mã vi phạm") để tách bạch rõ đây là 2 bảng độc lập, không phải phần tiếp nối của nhau — áp dụng nhất quán ở cả bản xem trước, bản Word (thêm hàm `subHeading()` mới cạnh `sectionHeading()` đã có), và bản PDF (thêm hằng CSS `subHeading` mới). `tsc`/`npm run build`/`npm test` (10/10) đều pass. Đây là lần sửa PDF thứ 3 liên tiếp cho cùng 1 tính năng xuất file (C196 sửa oklch bằng iframe — sai; C198 sửa oklch đúng nguyên nhân nhưng chưa phát hiện lỗi rỗng nội dung; C200 sửa nội dung rỗng) — rất cần người dùng tự xuất thử và MỞ FILE PDF thật để xác nhận lần này đã có đủ nội dung, vì agent không có trình đọc PDF để tự kiểm tra kết quả cuối cùng. |
| C201 | ⛔ Bị chặn (cần xác nhận) | 30/07/2026 | Người dùng chỉ ra 1 badge hiển thị "Tiết 2026-01-02" (dùng đúng class mặc định của `Badge` trong `StudentProfilePage.tsx`) — cột "tiết" của 1 số bản ghi cũ bị lỗi nhập liệu/import khiến giá trị lưu là chuỗi ngày tháng thay vì tiết học thật (1-8 hoặc "Giờ ra chơi"), yêu cầu sửa lại chỉ hiển thị đúng "tiết", không cần lấy giá trị dạng năm-tháng-ngày đó. | Xác định đây là lỗi hiển thị bắt nguồn từ dữ liệu cũ (cột `tiet` của `ghi_nhan`/`de_xuat_ghi_nhan` đôi khi vô tình chứa chuỗi ngày `YYYY-MM-DD` từ trước, không phải lỗi UI mới) — sửa tận gốc bằng 1 hàm dùng chung thay vì vá riêng từng chỗ. Thêm `formatTietLabel(tiet)` vào `src/features/records/recordInsights.ts` (file tiện ích dùng chung cho mọi nơi hiển thị `GhiNhan`/`DeXuatGhiNhan`) — nếu giá trị khớp mẫu ngày tháng `^\d{4}-\d{1,2}-\d{1,2}$` thì trả về `null` (không hiển thị gì, coi như không có tiết, giống hệt hành vi khi `tiet` là `null`/rỗng), ngược lại trả về đúng `"Tiết {giá trị}"` như trước. Áp dụng hàm này thay cho việc tự ghép chuỗi `Tiết ${...}` trực tiếp tại cả 5 vị trí đang hiển thị tiết trong toàn bộ app: `CatalogPage.tsx` (dòng lịch sử ghi nhận theo danh mục), `StudentProfilePage.tsx` (badge lịch sử ghi nhận của học sinh — đúng vị trí người dùng chỉ ra, và dòng lịch sử đề xuất), `TeacherStudentDetailPage.tsx` (bảng ghi nhận của giáo viên và dòng lịch sử đề xuất) — nhất quán toàn app, không chỉ sửa đúng 1 chỗ người dùng nhìn thấy rồi bỏ sót các nơi khác dùng chung kiểu dữ liệu này. `tsc`/`npm run build`/`npm test` (10/10) đều pass. Đây là fix hiển thị, KHÔNG sửa lại dữ liệu gốc trong CSDL — các bản ghi cũ vẫn giữ nguyên giá trị sai trong cột `tiet`, chỉ không còn hiển thị ra giao diện; nếu muốn sửa dứt điểm dữ liệu gốc cần 1 migration `UPDATE` riêng (tương tự cách đã làm với lỗi thiếu số 0 đầu SĐT ở C185), chưa làm ở đây vì người dùng chỉ yêu cầu sửa phần hiển thị. |
| C202 | ⛔ Bị chặn (cần xác nhận) | 30/07/2026 | Người dùng yêu cầu 2 việc: (1) thêm chức vụ "Lớp phó kỷ luật" vào Ban cán sự lớp; (2) thêm nút tích chọn để tuỳ chỉnh ban cán sự nào được phép nhập đề xuất ghi nhận vi phạm, thay vì gán cứng theo 2 tên chức vụ cố định như từ C183/C199. | Đổi kiến trúc quyền "Nhập đề xuất ghi nhận cho lớp" từ gán cứng theo TÊN CHỨC VỤ (mảng `LOP_TRUONG_ELIGIBLE_ROLES` trong code) sang **cờ quyền độc lập theo từng học sinh** — linh hoạt hơn, giáo viên tự quyết định ai được quyền bất kể giữ chức vụ gì (kể cả Tổ trưởng/Tổ phó nếu muốn), đúng như yêu cầu "tích chọn tuỳ chọn". **Migration mới** `20260730000100_them_quyen_de_xuat_tuy_chon_ban_can_su.sql`: thêm cột `ban_can_su.duoc_de_xuat_ghi_nhan boolean not null default false`; backfill giữ nguyên quyền đang có cho 2 chức vụ cũ (`update ... set duoc_de_xuat_ghi_nhan = true where chuc_vu in ('Lớp trưởng', 'Lớp phó học tập')`) để không mất quyền đột ngột khi nâng cấp; sửa hàm helper dùng chung `_qlhs_lop_truong_ma_hs` (áp dụng cho cả 6 RPC liên quan đề xuất, xem C183) đổi điều kiện từ `chuc_vu in (...)` sang `duoc_de_xuat_ghi_nhan = true`; sửa `lay_ho_so_cong_khai` thêm trường `duoc_de_xuat_ghi_nhan` vào JSON `ban_can_su` trả về, để trang hồ sơ công khai biết đúng học sinh đang xem có quyền hay không mà không cần đoán qua tên chức vụ. **`types.ts`**: thêm `duoc_de_xuat_ghi_nhan: boolean` vào interface `BanCanSu`. **`TeacherStudentDetailPage.tsx`**: thêm "Lớp phó kỷ luật" vào `ROLE_OPTIONS` (giữa "Lớp phó lao động" và "Bí thư chi đoàn"); xoá hẳn `LOP_TRUONG_ROLE`/`LOP_TRUONG_ELIGIBLE_ROLES`; form gán chức vụ thêm checkbox "Cho phép gửi đề xuất ghi nhận" (state `roleForm.duocDeXuat`) — ô nhập "Mã PIN" chỉ hiện khi tích checkbox này, không còn phụ thuộc tên chức vụ chọn là gì; `saveRole()` lưu `duoc_de_xuat_ghi_nhan` cùng `ma_pin` (PIN chỉ giữ khi checkbox bật). **`StudentProfilePage.tsx`**: thêm state `canSubmitProposal: boolean` (tính qua hàm mới `canSubmitProposal(maHs, banCanSu)` — kiểm tra có bản ghi `ban_can_su` nào của học sinh này bật `duoc_de_xuat_ghi_nhan` hay không) thay cho việc so khớp `state.role` với mảng tên chức vụ cố định; khối hiển thị form gửi đề xuất (`LopTruongPanel`) đổi điều kiện sang `state.canSubmitProposal && token`. Cập nhật lại các nhãn tĩnh từng liệt kê cứng "lớp trưởng / lớp phó học tập" ở `TeacherStudentDetailPage.tsx` và `RecordEntryPage.tsx` (khối lịch sử/duyệt đề xuất) thành cách nói chung "ban cán sự lớp"/"học sinh được cấp quyền" cho khớp với cơ chế mới không còn cố định theo 2 chức vụ đó. Nhân tiện thêm "Lớp phó kỷ luật" vào `BAN_CAN_SU_SIGNATURE_ROLES` (danh sách chức vụ được ký xác nhận báo cáo tuần/tháng ở tính năng Báo cáo, C199) cho nhất quán vì đây cũng là chức vụ cấp lớp. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng**: migration mới cần dán vào Supabase Dashboard SQL Editor thì tính năng mới có hiệu lực thật; trước đó dù đã tick checkbox trên giao diện, quyền thật sự vẫn theo logic cũ (RPC nền chưa cập nhật). |
| C203 | ⛔ Bị chặn (cần xác nhận) | 31/07/2026 | Người dùng cung cấp đặc tả `docs/09-tinh-nang-goi-sms-phu-huynh.md` (do "kiến trúc sư Claude" viết ngoài IDE, dựa trên giả định chưa xác nhận với codebase thật) và yêu cầu đọc và triển khai: bấm số điện thoại (trang chi tiết + danh sách học sinh) hiện lựa chọn Gọi/Nhắn tin, nội dung SMS lấy từ dữ liệu đã import theo tuần/tháng. | Theo đúng yêu cầu "không tự đoán tên file/route nếu chưa xác nhận" của đặc tả, rà codebase thật trước khi code và phát hiện 1 sai lệch quan trọng: đặc tả giả định bảng `cau_hinh_tuan` dùng khoá `ma_tuan` (text, dạng "T04-2526"), nhưng schema thật dùng `tuan_so` (integer, khoá chính — xem migration `20260722000100`) — điều chỉnh toàn bộ thiết kế bảng mới theo đúng `tuan_so`, không làm theo đặc tả gốc. Cũng xác nhận trang "Danh sách học sinh" (`StudentsPage.tsx`) CHƯA hề hiển thị số điện thoại ở đâu (đúng như đặc tả nghi ngờ ở mục 3.2) — phải tự thêm hiển thị trước khi gắn action sheet. **Migration mới** `20260731000100_them_noi_dung_tin_nhan_phu_huynh.sql`: thêm giá trị enum `'tin_nhan_phu_huynh'` vào `loai_du_lieu_import`; bảng mới `noi_dung_tin_nhan` (ma_hs, loai_ky tuan/thang, tuan_so/thang+nam tuỳ loại, cột tính sẵn `ky_key` gộp chung 1 định dạng khoá cho cả 2 kiểu kỳ — tránh phải xử lý 2 unique index partial riêng biệt mà Postgres `ON CONFLICT`/`supabase-js .upsert()` không hỗ trợ chỉ định predicate; 1 unique index duy nhất `(ma_hs, ky_key)` cho phép import lại đúng 1 kỳ sẽ UPDATE thay vì tạo trùng, đúng quyết định thiết kế đã chốt); RLS chỉ cho `authenticated` (giáo viên đã đăng nhập) đọc/ghi, không cấp quyền gì cho `anon` — bảo mật nội dung liên lạc phụ huynh giống hệt nguyên tắc đã áp dụng cho `sdt_1`/`sdt_2`, không lộ qua route công khai `/hs/:token`. **`types.ts`**: thêm `'tin_nhan_phu_huynh'` vào `LoaiDuLieuImport`, type `LoaiKyTinNhan`/`NoiDungTinNhan` mới. **`SupabaseDataSource.ts`**: thêm `getMessageContents(maHs?)`; `prepareTinNhanImportRow()` validate `ma_hs` khớp roster, `loai_ky` hợp lệ, `tuan_so` tồn tại trong `CauHinhTuan` (nếu kỳ tuần) hoặc `thang`/`nam` hợp lệ (nếu kỳ tháng), `noi_dung` không rỗng — lỗi từng dòng hiện đúng theo cơ chế báo lỗi per-row đã có sẵn của `importJson`; import loại này dùng `.upsert(rows, { onConflict: 'ma_hs,ky_key' })` thay vì `.insert()` thường như các loại khác. **`ImportPage.tsx`**: thêm `{ value: 'tin_nhan_phu_huynh', label: 'Tin nhắn phụ huynh' }` vào `IMPORT_OPTIONS` — tái dùng nguyên luồng JSON textarea + bảng xem trước generic đã có cho các loại đơn giản (`hoc_sinh`/`phu_huynh`/`ban_can_su`), không xây thêm màn hình riêng. **`src/components/PhoneActionMenu.tsx`** (mới, dùng chung cho cả 2 trang): bấm vào số điện thoại mở menu nhỏ "📞 Gọi" (giữ nguyên `tel:`) và "💬 Nhắn tin" (`sms:<số>?body=<nội dung>` hoặc `sms:<số>&body=...` — tách theo User-Agent vì iOS Safari cần dấu `&` trước `body=` còn Android/đa số trình duyệt khác cần `?`, đúng khác biệt nền tảng nêu trong checklist đặc tả); tự đóng khi bấm ra ngoài. **`src/features/students/messageContents.ts`** (mới): `findCurrentMessage()` xác định nội dung "hiện tại" = kỳ có khoảng cách gần nhất tới ngày thực tế hôm nay (so ngày bắt đầu tuần từ `CauHinhTuan` hoặc ngày 15 mỗi tháng làm mốc ước lượng cho kỳ tháng) — đúng yêu cầu "không phải bản ghi mới nhập nhất". **`TeacherStudentDetailPage.tsx`**: `PhoneRow` đổi từ link `tel:` trực tiếp sang `PhoneActionMenu`, tất cả số điện thoại của học sinh (SĐT 1/SĐT 2/số phụ huynh) đều dùng chung 1 nội dung SMS "hiện tại" của đúng học sinh đó (SMS gắn với học sinh, không gắn với 1 số điện thoại cụ thể); thêm hẳn section mới "Lịch sử tin nhắn" liệt kê mọi kỳ đã import cho học sinh này (đánh dấu rõ kỳ nào là "Hiện tại"), mỗi dòng có nút "Dùng nội dung này" mở SMS đúng nội dung của kỳ đó. **`StudentsPage.tsx`**: thêm hiển thị SĐT 1/SĐT 2 (dạng pill, dùng `PhoneActionMenu`) vào phần chi tiết mở rộng của từng dòng học sinh (trước đây hoàn toàn không có), không thêm cột mới vào bảng chính để tránh làm bảng vỡ trên di động. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng cần xử lý/kiểm tra**: (1) migration mới cần dán vào Supabase Dashboard SQL Editor trước khi dùng thật; (2) `ALTER TYPE ... ADD VALUE` cho enum Postgres — nếu Supabase Dashboard chặn chạy trong 1 transaction gộp chung nhiều lệnh, có thể cần tách chạy riêng câu này trước, báo lại nếu gặp lỗi; (3) toàn bộ checklist kiểm thử trên thiết bị thật (Android + iOS) trong đặc tả mục 7 CHƯA được xác nhận — đặc biệt hành vi `sms:` khác nhau giữa 2 nền tảng và menu Gọi/Nhắn tin có bị che khuất khi mở bên trong bảng cuộn ngang (`overflow-x-auto`) ở `StudentsPage.tsx` hay không, cần người dùng tự thử trên điện thoại thật. |
| C204 | ⛔ Bị chặn (cần xác nhận) | 31/07/2026 | Người dùng yêu cầu thêm tính năng xoá và sửa trực tiếp nội dung tin nhắn đã import (SMS phụ huynh, C203), thực hiện ngay trong hồ sơ học sinh (`TeacherStudentDetailPage.tsx`) thay vì phải xoá cả lượt import. | **`DataSource.ts`/`SupabaseDataSource.ts`**: thêm `updateMessageContent(id, noiDung)` (update trực tiếp cột `noi_dung` theo `id`) và `deleteMessageContent(id)` (xoá đúng 1 dòng theo `id`) — thao tác theo TỪNG dòng `noi_dung_tin_nhan`, khác với `deleteImport()` hiện có (chỉ hỗ trợ xoá nguyên cả lượt import của loại `ghi_nhan`, không áp dụng cho loại mới). **`TeacherStudentDetailPage.tsx`**: section "Lịch sử tin nhắn" (thêm ở C203) giờ có thêm nút "Sửa" (chuyển dòng đó sang chế độ sửa trực tiếp tại chỗ — hiện `<textarea>` chứa nội dung hiện tại + nút Lưu/Huỷ, theo đúng mẫu inline-edit đã dùng ở `ParentContactHistoryPage.tsx`) và nút "Xoá" (có `window.confirm` xác nhận trước, nêu rõ đúng kỳ tuần/tháng sắp xoá để tránh xoá nhầm). Sửa/xoá xong cập nhật ngay `state.messages` tại chỗ (không cần tải lại trang) — bản ghi được đánh dấu "Hiện tại" (`currentMessage`) và nút "Dùng nội dung này" tự động phản ánh đúng nội dung mới sau khi sửa vì đều tính từ cùng 1 state. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C205 | ⛔ Bị chặn (cần xác nhận) | 31/07/2026 | Người dùng yêu cầu cho phép xoá nguyên lượt import loại "Tin nhắn phụ huynh" ngay ở trang Lịch sử import, giống loại GhiNhan đã hỗ trợ từ trước. | **`SupabaseDataSource.ts`**: `deleteImport()` trước đây chỉ chấp nhận `loai_du_lieu === 'ghi_nhan'` (báo lỗi với mọi loại khác) — mở rộng thêm `'tin_nhan_phu_huynh'`, xoá theo bảng/cột tương ứng: `ghi_nhan` xoá theo `ma_log_import`, còn `noi_dung_tin_nhan` xoá theo `nguon_import` (cột này đã được `prepareTinNhanImportRow()` ở C203 tự gán bằng đúng mã log nếu JSON không tự cung cấp `nguon_import` riêng, nên mặc định luôn truy vết đúng lần import). Nếu 1 kỳ tuần/tháng bị import đè bởi 1 lượt import SAU (do cơ chế upsert theo `ma_hs+ky_key` ở C203), `nguon_import` của dòng đó sẽ cập nhật theo lượt import mới nhất — xoá lượt import CŨ sẽ không đụng tới dòng đó nữa, đúng hành vi hợp lý (không xoá nhầm dữ liệu đã bị lượt import mới hơn ghi đè). **`ImportPage.tsx`**: `canDeleteImportLog()` cho phép cả 2 loại; các câu xác nhận/thông báo/tooltip trước đây viết cứng chữ "GhiNhan" nay dùng hàm mới `importTypeLabel()` (tra cứu nhãn từ `IMPORT_OPTIONS` đã có) để hiện đúng tên loại dữ liệu thật sự đang xoá ("GhiNhan" hoặc "Tin nhắn phụ huynh"), tránh gây hiểu lầm khi xoá nhầm loại. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C206 | ⛔ Bị chặn (cần xác nhận) | 01/08/2026 | Người dùng cung cấp đặc tả bản 2 `docs/10-tinh-nang-goi-sms-phu-huynh-v2.md` (đổi tên từ placeholder `1X-...`), thiết kế lại đơn giản hơn hẳn bản C203: bỏ hẳn khái niệm kỳ tuần/tháng trong JSON import, mỗi lần import là 1 bản ghi MỚI (không upsert), "nội dung hiện tại" = bản ghi mới nhất theo thời gian thực tế nhập; chuẩn bị sẵn cột `nguon`/`da_duyet` cho giai đoạn 2 (hệ thống tự sinh nội dung, cần GVCN duyệt). | Đây là bản THAY THẾ hoàn toàn thiết kế bảng `noi_dung_tin_nhan` đã làm ở C203 (không phải bổ sung thêm) — bảng cũ vừa tạo, gần như chắc chắn chưa có dữ liệu thật trên Supabase (chưa xác nhận đã dán migration C203 lên Dashboard), nên chọn cách migration mới **`DROP TABLE` rồi tạo lại** thay vì `ALTER` nhiều bước phức tạp (bỏ cột generated `ky_key`, đổi kiểu `created_by` từ text sang uuid...) — đã ghi rõ cảnh báo trong migration: nếu migration cũ đã chạy thật và đã có dữ liệu, bước này sẽ XOÁ SẠCH, cần kiểm tra trước khi chạy trên Supabase thật. **Giữ lại 1 điểm NGOÀI đặc tả gốc**: cột `nguon_import` (text) — đặc tả bản 2 không nhắc tới cột này, nhưng đây là cột đã dùng để hỗ trợ "xoá nguyên lượt import" (tính năng người dùng yêu cầu ngay trước đó ở C205) — nếu bỏ theo đúng đặc tả sẽ làm mất tính năng vừa mới thêm, nên chủ động giữ lại thay vì làm đúng 100% đặc tả. **Migration mới** `20260801000100_don_gian_hoa_noi_dung_tin_nhan.sql`: `drop table` + tạo lại `noi_dung_tin_nhan` với cột `noi_dung`, `ghi_chu` (nhãn tự do, không bắt buộc), `nguon` (`nhap_tay`/`tu_dong`, mặc định `nhap_tay`), `da_duyet` (mặc định `true`), `nguon_import`, `created_by` (đổi sang `uuid references auth.users(id)` đúng đặc tả thay vì text tự do như C203), bỏ hẳn `loai_ky`/`tuan_so`/`thang`/`nam`/cột tính sẵn `ky_key`; RLS giữ nguyên chỉ cho `authenticated`. **`types.ts`**: `NoiDungTinNhan` bỏ các field theo kỳ, thêm `ghi_chu`/`nguon` (`NguonTinNhan`)/`da_duyet`; xoá hẳn type `LoaiKyTinNhan` không còn dùng. **`SupabaseDataSource.ts`**: `prepareTinNhanImportRow()` bỏ hết validate tuần/tháng, chỉ còn kiểm `ma_hs` khớp roster + `noi_dung` không rỗng; `created_by` lấy từ `getSupabaseClient().auth.getUser()` (UUID giáo viên đang đăng nhập thật, không còn nhận text tuỳ ý từ JSON); import loại này quay lại dùng `.insert()` thuần như mọi loại khác (bỏ hẳn nhánh `.upsert(onConflict: 'ma_hs,ky_key')` đã thêm ở C203 vì không còn khái niệm "cùng 1 kỳ" để coi là trùng — mỗi lần import luôn là thêm mới). **`messageContents.ts`**: `findCurrentMessage()` viết lại còn 1 dòng logic — lọc `da_duyet=true` rồi lấy `created_at` mới nhất (bỏ tham số `weekConfig` không còn cần); thêm `formatMessageTimestamp()` thay cho `formatKyLabel()` đã xoá. **`TeacherStudentDetailPage.tsx`**: bỏ hẳn việc fetch/lưu `weekConfig` (chỉ được thêm riêng cho tính năng này ở C203, không dùng gì khác trong trang) vì `findCurrentMessage` không còn cần; mỗi dòng "Lịch sử tin nhắn" hiển thị `ghi_chu` nếu giáo viên có ghi, không thì hiện ngày giờ import thay cho nhãn "Tuần N"/"Tháng N/YYYY" cũ. **`StudentsPage.tsx`**: bỏ tham số `weekConfig` khỏi 2 lệnh gọi `findCurrentMessage` (trang này vẫn giữ `weekConfig` cho mục đích khác — tính tuần hiện tại — không xoá biến, chỉ bỏ khỏi lệnh gọi không còn cần). **`tao csdl/mau-tin-nhan-phu-huynh.json`**: viết lại mẫu theo đúng định dạng mới (chỉ `ma_hs`/`noi_dung`, `ghi_chu` tuỳ chọn). `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng**: nếu migration `20260731000100` (bản C203) đã được dán chạy trên Supabase Dashboard thật và đã có dữ liệu tin nhắn thật trong đó, migration mới này sẽ XOÁ SẠCH bảng — cần kiểm tra và sao lưu (nếu cần) trước khi dán migration mới lên Supabase; nếu migration cũ CHƯA từng chạy trên Supabase thật thì chỉ cần dán đúng 1 migration mới này (bỏ qua migration cũ). |
| C207 | ⛔ Bị chặn (cần xác nhận) | 05/08/2026 | Người dùng báo lỗi layout: đoạn `<p class="wrap-break-word text-sm text-slate-700">` hiển thị nội dung tin nhắn phụ huynh trên di động bị xuống dòng từng chữ cái một thay vì xuống dòng theo từ. | Root cause KHÔNG phải do `wrap-break-word` (đây là utility hợp lệ của Tailwind v4, tương đương `overflow-wrap: break-word`) mà do container cha bị bóp quá hẹp: khối "Lịch sử tin nhắn" và 2 chỗ tương tự trong `TeacherStudentDetailPage.tsx` dùng `flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between`, còn div chứa đoạn văn bản chỉ có `min-w-0` (không có `flex-1`). Vì `--breakpoint-sm: 375px` đã bị ghi đè từ trước (xem `src/index.css`) nên `sm:` gần như luôn kích hoạt trên điện thoại thật — trình duyệt chuyển ngay sang bố cục hàng ngang (`flex-row`) dù màn hình di động chật, và vì cụm nút hành động bên cạnh (`shrink-0`) không chịu co lại còn khối văn bản có `min-w-0` (không giới hạn dưới) nhưng không có `flex-1` (không được chia phần rộng công bằng), trình duyệt co khối văn bản xuống gần như 0px để nhường chỗ cho nút — buộc `wrap-break-word` phải bẻ từng ký tự để vẫn vừa cột cực hẹp đó. Áp dụng đúng công thức đã dùng nhất quán trong toàn bộ app cho lỗi bố cục di động cùng loại (`sm:` → `md:` cho các chỗ chuyển hàng/cột, thêm `flex-1` cho khối văn bản cạnh phần tử `shrink-0`) tại 3 vị trí cùng mẫu trong file: khối "Lịch sử tin nhắn" (nội dung SMS, dòng người dùng chỉ ra), khối thẻ đề xuất ghi nhận (`DeXuatCard`), và `PhoneRow` (số điện thoại/tên). Không sửa nơi khác dùng `wrap-break-word` không nằm trong mẫu `sm:flex-row` bị lỗi này (ví dụ khối liên lạc phụ huynh dùng `flex-col` cố định, không bị ảnh hưởng). `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C208 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng yêu cầu: ở phần học sinh đề xuất danh mục mới (giáo viên duyệt đề xuất, tạo danh mục điểm mới từ đề xuất), mã danh mục hiện đang phải điền tay rồi mới tự kiểm tra trùng — cần tự động sinh mã theo nhóm (ví dụ chọn nhóm "Nề nếp" thì tự sinh mã, đổi qua nhóm khác thì tự kiểm tra lại và sinh mã khác không trùng), duy nhất. | Phát hiện app đã có sẵn đúng cơ chế này ở `CatalogPage.tsx` (form "Thêm danh mục" của giáo viên, hàm nội bộ `nextCodeForGroup(group, catalog)` — dò số lớn nhất đã dùng theo đúng tiền tố nhóm rồi +1, tự tăng tiếp nếu trùng) nhưng **chưa được áp dụng** cho form tạo danh mục mới từ đề xuất của học sinh trong `RecordEntryPage.tsx` (`ApprovalCard`), nơi field "Mã mới" trước đây khởi tạo rỗng và chỉ được điền tay, chỉ báo lỗi trùng khi bấm lưu. Thay vì viết lại logic riêng (dễ lệch nhau về sau), tách `nextCodeForGroup` ra file dùng chung mới **`src/features/catalog/catalogCode.ts`** (export function, giữ nguyên logic gốc không đổi), `CatalogPage.tsx` xoá hàm nội bộ trùng lặp và import lại từ file này (không đổi hành vi trang này). **`RecordEntryPage.tsx`**: `createForm` (state tạo danh mục mới trong `ApprovalCard`) đổi khởi tạo từ object tĩnh sang hàm lazy-init gọi `nextCodeForGroup(nhom, catalog)` ngay khi mở form (nhóm mặc định lấy từ `proposal.de_xuat_nhom` học sinh đã chọn khi gửi đề xuất); thêm hàm `changeCreateGroup(nhom)` set cả `nhom` lẫn `ma_danh_muc` (tính lại qua `nextCodeForGroup`) cùng lúc, gắn vào `onChange` của select "Nhóm" — đúng yêu cầu "chọn qua mục khác thì check lại mã của mục đó". `catalog` dùng để dò trùng là danh sách đã tải từ CSDL lúc mở trang duyệt đề xuất (props `catalog` truyền xuống từ `RecordEntryPage`), nên đảm bảo kiểm tra đúng dữ liệu CSDL hiện có, không phải danh sách tĩnh. Field "Mã mới" đổi nhãn thành "Mã mới (tự sinh theo nhóm, có thể sửa)" — vẫn giữ cho giáo viên sửa tay nếu cần (đúng UX đã có sẵn ở `CatalogPage.tsx`, không khoá cứng field), kiểm tra trùng lần cuối trước khi lưu (`createCatalogItem`) vẫn giữ nguyên như cũ làm lưới an toàn nếu giáo viên tự sửa tay thành mã trùng. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C209 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng chỉ ra kẽ hở của cơ chế tự sinh mã vừa thêm ở C208: nếu 2 học sinh cùng đề xuất ghi nhận thuộc cùng nhóm (ví dụ cùng nhóm "Kỷ luật") và giáo viên mở 2 thẻ duyệt cùng lúc, cả 2 thẻ đều gợi ý CÙNG 1 mã (ví dụ cả 2 đều gợi ý KL18, vì tại thời điểm mở thẻ, CSDL chưa có danh mục nào tên KL18 nên cả 2 tính ra kết quả giống nhau); khi thẻ 1 được duyệt trước, mã KL18 đã được lưu thật xuống CSDL, nhưng thẻ 2 vẫn hiển thị KL18 (mã cũ tính từ lúc mở, không tự cập nhật) — cần kiểm tra lại và tự sinh mã khác đúng lúc phát hiện trùng, không để giáo viên bấm lưu rồi mới báo lỗi. | Nguyên nhân: `createForm.ma_danh_muc` trong `ApprovalCard` (`RecordEntryPage.tsx`) chỉ được tính 1 LẦN lúc mount (lazy `useState` initializer) hoặc lúc đổi nhóm (`changeCreateGroup`) — không có cơ chế nào theo dõi khi prop `catalog` của CHÍNH THẺ ĐÓ đổi (do 1 thẻ khác vừa duyệt xong, `handleCatalogCreated` ở component cha thêm item mới vào `state.catalog` rồi truyền lại qua prop `catalog` xuống TẤT CẢ các thẻ đang mở, kể cả thẻ chưa duyệt). Thêm state `codeTouched` (đánh dấu giáo viên đã tự tay sửa ô "Mã mới" hay chưa) và 1 `useEffect` theo dõi `[catalog, codeTouched]`: mỗi khi prop `catalog` đổi (tức vừa có 1 danh mục mới được tạo ở bất kỳ thẻ nào), nếu ô mã ở thẻ NÀY chưa bị giáo viên tự sửa tay và mã hiện đang đề xuất đã bị trùng trong `catalog` mới nhất, tự động gọi lại `nextCodeForGroup()` để sinh mã khác — hoàn toàn tự động, giáo viên không cần thao tác gì thêm ở thẻ thứ 2. Nếu giáo viên ĐÃ tự sửa tay mã (`codeTouched = true`), tôn trọng lựa chọn của họ, không tự ý ghi đè, nhưng thêm dòng cảnh báo runtime `codeConflict` (so `createForm.ma_danh_muc` với `catalog` mỗi lần render) hiển thị ngay dưới ô nhập "Mã ... đã tồn tại — đổi mã khác." để giáo viên thấy NGAY khi đang gõ, không phải đợi bấm Lưu mới biết (thông báo lỗi lúc bấm Lưu trong `createCatalogItem` cũng được cập nhật câu chữ rõ hơn: "có thể vừa được tạo từ 1 đề xuất khác"). `changeCreateGroup()` khi đổi nhóm cũng reset `codeTouched` về `false` để mã mới sinh theo nhóm tiếp tục được tự động theo dõi trùng lặp. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C210 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng yêu cầu: khi sửa 1 danh mục điểm ở `CatalogPage.tsx` (đổi điểm cộng/trừ), cần cập nhật lại luôn cho toàn bộ ghi nhận (GhiNhan) học sinh đã có sẵn thuộc danh mục đó, không chỉ áp dụng cho ghi nhận mới về sau. | Rà `GhiNhan` phát hiện mỗi dòng ghi nhận lưu SNAPSHOT điểm riêng ở cột `diem_cong_tru` ngay lúc tạo (gán bằng `catalogItem.diem` tại thời điểm đó — xem `SupabaseDataSource.ts` dòng tạo GhiNhan và `RecordEntryPage.tsx`), và điểm hiển thị/tính tổng kết (`scoring.ts` hàm `scoreDelta`) luôn ưu tiên dùng `diem_cong_tru` của chính dòng đó thay vì đọc điểm hiện tại của danh mục — nên trước đây sửa điểm 1 danh mục chỉ ảnh hưởng ghi nhận MỚI tạo sau đó, các ghi nhận cũ vẫn giữ điểm snapshot lúc tạo, không tự cập nhật theo. Thêm method mới **`syncCatalogPointToRecords(maDanhMuc, diem)`** vào `DataSource.ts`/`SupabaseDataSource.ts` — update hàng loạt `ghi_nhan.diem_cong_tru = diem` cho mọi dòng có đúng `ma_danh_muc`, trả về số dòng đã cập nhật. **`CatalogPage.tsx`**: `saveCatalogItem()` ở nhánh sửa (không phải thêm mới) — sau khi lưu thành công thay đổi của danh mục, so `original.diem` (điểm cũ, lấy từ `catalog` state trước khi sửa) với `updated.diem` (điểm mới đã lưu); nếu khác nhau mới gọi `syncCatalogPointToRecords` (tránh gọi lãng phí khi giáo viên chỉ sửa tên/mô tả, không đổi điểm), đồng thời cập nhật ngay `records` state tại chỗ (không cần tải lại trang) để bảng "Ghi nhận theo danh mục" hiển thị điểm mới tức thì; hiện thông báo rõ số lượt ghi nhận đã đồng bộ. Bọc riêng bước đồng bộ trong `try/catch` tách biệt khỏi bước lưu danh mục — nếu đồng bộ lỗi (ví dụ mất mạng giữa chừng), báo lỗi rõ "Đã lưu danh mục nhưng chưa đồng bộ được điểm cho ghi nhận cũ" thay vì hiểu lầm là lưu danh mục thất bại (danh mục thực tế đã lưu xong). Không đổi hành vi đề xuất (`DeXuatGhiNhan`) vì bảng này không lưu snapshot điểm, không cần đồng bộ. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C211 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng yêu cầu: "làm theo 11 dac ta he thong dong hanh · MD" — đọc và triển khai đầy đủ `docs/11-dac-ta-he-thong-dong-hanh.md`: hệ thống "Đồng hành" rule-based (cảnh báo/nhắc nhở, huy hiệu, câu định hướng) mà toàn bộ ngưỡng/câu chữ là dữ liệu sửa được trên web qua trang quản lý riêng, không hardcode trong TypeScript. | Đây là tính năng lớn nhất từ trước tới nay, gồm 2 migration mới, 1 module tính toán mới, 1 trang quản lý 4 tab, và tích hợp hiển thị ở cả trang giáo viên lẫn trang công khai học sinh. **Trước khi code đã phát hiện 2 tài liệu tham chiếu trong đặc tả không tồn tại trong repo** — file Excel seed gốc `bang-du-lieu-he-thong-dong-hanh.xlsx` và tài liệu luận giải `10-luan-huong-phat-trien-he-thong.md` (số `docs/10` đã bị dùng cho tính năng SMS trước đó) — đã ghi đầy đủ 6 khuyến nghị `[KN-01]` đến `[KN-06]` vào cuối chính file đặc tả theo đúng quy trình mục 11 của đặc tả, KHÔNG tự ý âm thầm đổi khác. **Migration 1** `20260806000100_tao_bang_he_thong_dong_hanh.sql`: tạo 5 bảng `dong_hanh_chi_so` (từ điển 10 chỉ số, seed sẵn), `dong_hanh_luat` (5 luật CB1–CB5 seed làm ví dụ khởi tạo — KHÔNG phải dữ liệu thật, xem KN-01), `dong_hanh_huy_hieu` (5 huy hiệu, điều kiện dạng mảng AND lưu `jsonb`), `dong_hanh_cau_dinh_huong` (5 câu), `dong_hanh_duyet` (trạng thái duyệt theo học sinh/tuần/luật — có thêm cột `loai` ('canh_bao'|'dinh_huong') NGOÀI đặc tả gốc để trang công khai phân biệt được 2 loại câu mà không cần lộ danh sách luật gốc, xem KN-03); RLS `authenticated` giống mọi bảng khác. **Migration 2** `20260806000200_mo_rong_ho_so_cong_khai_dong_hanh.sql`: mở rộng RPC `lay_ho_so_cong_khai` (đã sửa 4 lần từ C183) thêm 3 cột trả về: `attendance` (diem_danh CHINH_KHOA của đúng học sinh, để trang công khai tự tính chỉ số vắng/trễ giống bên giáo viên), `huy_hieu` (định nghĩa huy hiệu `dang_bat=true`, vì huy hiệu tự động không cần duyệt nên phải lộ định nghĩa để client tự tính), `duyet` (CHỈ các dòng `dong_hanh_duyet` đã `trang_thai='da_duyet'` của đúng học sinh — tuyệt đối không lộ câu `cho_duyet`/`da_an` qua RPC công khai này). **`src/features/companion/computeMetrics.ts`** (mới): `tinhChiSoTuan()` — hàm DUY NHẤT tính toán, tái dùng `isViolationRecord`/`isPositiveRecord` (`DashboardPage.tsx`) và filter theo `tuan_so`/`ma_hs`, trả về đủ 11 chỉ số kể cả `xu_huong_loi` (chênh lệch lỗi tuần này–tuần trước, chỉ số bổ sung được đặc tả gợi ý tên nhưng không định nghĩa rõ ở mục 3). Đếm vắng/trễ theo từng DÒNG `diem_danh` (mỗi buổi 1 dòng, không gộp thành ngày) vì logic "gộp buổi sáng/chiều" mà đặc tả nhắc tới không tồn tại trong code thật (xem KN-05). **`src/features/companion/applyRules.ts`** (mới): `apDungLuat()` so `chi_so` với `nguong` theo `phep_so_sanh`, xử lý `nhom_che`+`uu_tien` (chỉ giữ luật ưu tiên cao nhất trong cùng nhóm che) và thay placeholder `{n}`/`{ma}`/`{ngay}`; `apDungHuyHieu()` chạy AND tất cả điều kiện trong `dieu_kien` jsonb; `chonCauDinhHuong()` (hàm tự thiết kế, đặc tả không mô tả thuật toán — xem KN-04) chọn câu định hướng theo `nhom_che`/`muc_do` của luật ưu tiên cao nhất đã khớp, mặc định `gan_voi='mac_dinh_tot'` nếu không luật nào khớp. **`src/features/companion/RuleManagerPage.tsx`** (mới, route `/dong-hanh`, thêm mục "Đồng hành" 🤝 vào menu `Layout.tsx`): 4 tab CRUD (Cảnh báo & nhắc nhở, Huy hiệu — có form thêm/bớt nhiều dòng điều kiện AND, Câu định hướng, Chỉ số — chỉ xem theo đúng tinh thần "ít sửa" của đặc tả, xem KN-06) đều lưu thẳng qua `dataSource`, có nút bật/tắt (`dang_bat`) từng luật/huy hiệu/câu; cuối trang có khối "Xem thử ngay" — chọn 1 học sinh + 1 tuần, chạy `tinhChiSoTuan`+`apDungLuat`+`apDungHuyHieu`+`chonCauDinhHuong` trên dữ liệu thật, hiện ngay kết quả — đúng yêu cầu "vừa test vừa chỉnh" mục 5. **`TeacherStudentDetailPage.tsx`**: thêm section "Đồng hành" (component `CompanionSection`) ngay sau phần đầu trang — chọn tuần, hiện huy hiệu tự động, danh sách cảnh báo khớp kèm nút Duyệt/Ẩn (ghi vào `dong_hanh_duyet` qua `dataSource.duyetDongHanh`), câu định hướng gợi ý kèm Duyệt/Ẩn; state `success` của trang này được nạp thêm `attendance`/`weekConfig`/`luat`/`huyHieu`/`cauDinhHuong`/`duyet` trong cùng `Promise.all` đã có. **`StudentProfilePage.tsx`** (bản công khai): thêm `CompanionSummary` hiện ngay sau "Tóm tắt hồ sơ", trước phần Lớp trưởng/Ghi nhận mới nhất — CHỈ tính huy hiệu tự động (`apDungHuyHieu`) và hiện các câu cảnh báo/định hướng đã có sẵn trong `profile.duyet` (đã lọc `da_duyet` ngay từ RPC phía Supabase, trang này không tự tính/hiện câu chưa duyệt) — đúng ranh giới bắt buộc ở mục 6 đặc tả. Tự ẩn cả khối nếu tuần đó không có gì để hiện (không huy hiệu, không cảnh báo, không định hướng), tránh hiện khung rỗng. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Lưu ý quan trọng — CHƯA kiểm thử được trên trình duyệt thật với Supabase thật** (đúng mục 9 đặc tả yêu cầu, nhưng môi trường triển khai này không có kết nối Supabase sống): cả 2 migration cần dán tuần tự vào Supabase Dashboard SQL Editor (migration 1 trước, migration 2 sau vì nó `create or replace` đúng RPC mà migration 1 không đụng tới nhưng phụ thuộc 2 bảng mới); sau đó giáo viên cần tự thực hiện đủ 5 bước kiểm thử ở mục 9 đặc tả (sửa 1 ngưỡng → xác nhận không cần deploy lại; thêm 1 luật mới qua web; tạo câu cảnh báo để `cho_duyet` → xác nhận bản công khai không thấy, duyệt xong mới thấy; tắt 1 huy hiệu → xác nhận biến mất; kiểm tra 1 em dính nhiều luật cùng `nhom_che` chỉ hiện luật ưu tiên cao nhất) trước khi coi tính năng là hoàn thành thật sự. Toàn bộ 6 khuyến nghị KN-01 đến KN-06 cần kiến trúc sư rà lại, đặc biệt KN-01 (dữ liệu luật/huy hiệu/câu định hướng hiện là ví dụ tự viết, không phải dữ liệu thật từ Excel) và KN-04 (thuật toán chọn câu định hướng tự thiết kế, chưa được xác nhận đúng ý đồ ban đầu). |
| C212 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng dán migration `20260806000200_mo_rong_ho_so_cong_khai_dong_hanh.sql` (C211) lên Supabase Dashboard SQL Editor và báo lỗi: `ERROR: 42P13: cannot change return type of existing function... HINT: Use DROP FUNCTION lay_ho_so_cong_khai(text,text,text) first.` | Nguyên nhân: hàm `lay_ho_so_cong_khai` dùng `RETURNS TABLE (...)`, tức các cột trả về là OUT parameters — Postgres không cho phép `create or replace function` đổi số lượng/kiểu cột OUT (ở đây thêm 3 cột mới `attendance`/`huy_hieu`/`duyet` so với bản C202/C203 trước đó), dù các lần sửa `lay_ho_so_cong_khai` trước (C183 → C202 → C203) chưa từng gặp lỗi này vì chỉ đổi NỘI DUNG bên trong `jsonb_build_object`, không đổi danh sách cột `returns table`. Vì migration này CHƯA từng chạy thành công lần nào trên Supabase thật (báo lỗi ngay từ lần dán đầu tiên), sửa trực tiếp file migration `20260806000200...` (không tạo migration mới) — thêm `drop function if exists public.lay_ho_so_cong_khai(text, text, text);` ngay trước `create or replace function`, đúng theo hint Postgres đưa ra. Không vi phạm quy ước "không sửa migration sau khi đã chạy thật" vì bản cũ chưa bao giờ áp dụng thành công. Không có thay đổi TypeScript nào, `tsc`/`npm run build`/`npm test` (10/10) vẫn pass. **Người dùng cần dán lại đúng nội dung migration này (đã sửa) lên Supabase Dashboard SQL Editor để chạy lại.** |
| C213 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng yêu cầu bố cục lại toàn bộ layout trang hồ sơ học sinh ở CẢ 2 chế độ (giáo viên `TeacherStudentDetailPage.tsx` và học sinh công khai `StudentProfilePage.tsx`): trang công khai cần Hệ thống Đồng hành (C211) nổi bật, sinh động, có biểu tượng gây hứng thú; các nội dung khác gom vào menu/tab bấm mới hiện, không hiện hết ra màn hình cùng lúc. | **`StudentProfilePage.tsx` (bản công khai)**: bỏ hẳn cơ chế accordion thu/phóng từng section (`collapsedSections`/`ProfileSectionHeader`/`toggleSection` — đã xoá sạch, không còn dùng) vì nó xung đột với đúng yêu cầu "bấm mới hiện" (trước đây mọi section luôn hiện sẵn, accordion chỉ ẩn/hiện nội dung bên trong, không thực sự gọn màn hình). Đổi cấu trúc trang thành: (1) Header định danh học sinh — luôn hiện; (2) `CompanionSummary` (Đồng hành) — luôn hiện ngay sau header, thiết kế lại hẳn để "nổi bật, sinh động": nền gradient hổ phách/cam ấm (`bg-linear-to-br from-amber-100 via-orange-50 to-yellow-100`), icon 🤝 lớn mờ trang trí góc trên, thêm "lời mở đầu" đổi giọng theo tình hình tuần (🎉 nếu có huy hiệu, 💪 nếu có cảnh báo, 🌱 nếu tuần bình yên — thực hiện đúng mục 6.1 đặc tả C211 "Lời mở đầu theo tinh thần tuần" mà bản C211 trước đó đã bỏ qua), huy hiệu hiển thị dạng thẻ lưới lớn có icon 3xl + hiệu ứng `hover:scale-105`, cảnh báo/định hướng có icon ⚠️/💡 riêng biệt; **không còn ẩn hẳn khi rỗng** (trước đây `return null` nếu không có gì) — nay luôn hiện kèm câu động viên mặc định, đúng tinh thần "trang phải nổi bật, không được biến mất"; (3) Các nội dung còn lại gom vào 4 tab (`PROFILE_TABS`): "⭐ Nổi bật" (gộp Lớp trưởng CTA nếu có quyền + Ghi nhận mới nhất — trước đây 2 khối này luôn hiện full màn hình), "🕘 Lịch sử", "📊 Điểm tuần", "👤 Cá nhân" — đúng 1 khối nội dung hiện tại 1 thời điểm, có cả thanh tab ngang (desktop, `hidden md:block`) và thanh điều hướng dưới cùng cho di động (tái dùng, đổi từ `PROFILE_SECTIONS` cũ sang `PROFILE_TABS` thống nhất 1 nguồn dữ liệu duy nhất cho cả menu ☰, tab ngang, và thanh dưới di động — trước đây có 2 danh sách tách rời `PROFILE_SECTIONS`/tabs cục bộ trong `ProfileTabs` dễ lệch nhau). **`TeacherStudentDetailPage.tsx`**: giữ nguyên Header + `CompanionSection` (Đồng hành) luôn hiện đầu trang (giáo viên cần thấy ngay để duyệt), thêm thanh tab `TEACHER_TABS` ngay dưới đó gom 6 section trước đây luôn hiện full màn hình (Ban cán sự, Ghi nhận, Đề xuất từ ban cán sự, Lịch sử tin nhắn, Liên lạc phụ huynh, Thông tin cá nhân) thành 4 tab: "📝 Ghi nhận & đề xuất" (gộp 2 section liên quan), "📞 Liên lạc PH" (gộp Tin nhắn + Liên lạc phụ huynh, cùng chủ đề liên lạc), "🎓 Ban cán sự", "👤 Thông tin". `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Chưa kiểm thử được trên trình duyệt thật** (môi trường này không có kết nối Supabase sống để đăng nhập xem dữ liệu thật) — cần người dùng tự mở cả 2 trang trên điện thoại/máy tính thật để xác nhận bố cục tab mới dễ dùng, đặc biệt phần Đồng hành mới có thực sự "nhìn vào thích thú" như mong muốn hay cần chỉnh thêm màu sắc/hiệu ứng. |
| C214 | ⛔ Bị chặn (cần xác nhận) | 06/08/2026 | Người dùng báo lỗi trên di động: bấm vào số điện thoại (menu "Gọi"/"Nhắn tin" từ `PhoneActionMenu`) bị lệch layout ra ngoài vùng có thể thao tác, không bấm được. | Root cause: `PhoneActionMenu.tsx` định vị menu popup bằng `position: absolute` bên trong chính nút bấm (`<div className="relative inline-block">...<div className="absolute right-0 top-full">`). Nơi dùng component này ở `StudentsPage.tsx` (`StudentPhonePill`, hiển thị SĐT trong phần chi tiết mở rộng của từng dòng học sinh) nằm bên trong `<div className="overflow-x-auto">` bọc cả bảng — theo đúng cơ chế CSS đã gặp trước đây với jsPDF (bài học "phần tử absolute bên trong ancestor có overflow sẽ bị ancestor đó cắt/kẹp"), khi `overflow-x` được đặt khác `visible`, trình duyệt cũng ngầm áp `overflow-y` tương đương, khiến menu popup bị bảng cắt mất hoặc đẩy lệch ra ngoài vùng nhìn thấy/bấm được trên di động, dù trên desktop (bảng đủ rộng, không cần cuộn ngang) lại không thấy lỗi. Sửa bằng cách **portal menu thẳng ra `document.body`** (dùng `createPortal` từ `react-dom`, cùng nguyên tắc jsPDF: phần tử được portal ra ngoài không còn bị bất kỳ ancestor overflow nào cắt) và định vị bằng `position: fixed` tính theo toạ độ thật của nút bấm qua `getBoundingClientRect()` (kẹp trong khoảng viewport để không tràn 2 bên màn hình di động hẹp), thay vì `absolute` phụ thuộc vào vị trí DOM cha. Menu tự đóng khi cuộn (`window.addEventListener('scroll', ..., true)` bắt cả sự kiện cuộn của bảng `overflow-x-auto` lẫn cuộn trang) hoặc đổi kích thước màn hình, tránh menu "trôi" sai vị trí sau khi tính toạ độ. Áp dụng cho cả 2 nơi dùng component (`StudentsPage.tsx` và `TeacherStudentDetailPage.tsx`), không cần sửa gì ở 2 file đó vì `PhoneActionMenu` tự đóng gói toàn bộ logic định vị. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C215 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng báo: phần "Hệ thống Đồng hành" ở trang xem hồ sơ học sinh chế độ giáo viên hiện chữ tiếng Việt không dấu. | Root cause: dữ liệu SEED của migration `20260806000100_tao_bang_he_thong_dong_hanh.sql` (C211) — tên chỉ số, tên luật, câu hiển thị cảnh báo, tên huy hiệu, mô tả huy hiệu, câu định hướng — bị gõ KHÔNG DẤU giống văn phong comment SQL đã dùng xuyên suốt các migration (quy ước comment luôn viết không dấu để tránh lỗi encoding khi chạy trên 1 số công cụ dòng lệnh), nhưng lần này áp dụng nhầm luôn vào GIÁ TRỊ DỮ LIỆU thật sẽ hiển thị nguyên văn ra UI (khác với comment, không ai nhìn thấy) — ví dụ `'Vang 1 buoi - nhac som'`, `'Tuan nay em vang khong phep {n} buoi...'`. Vì migration gốc CÓ THỂ đã chạy thành công trên Supabase thật (không như migration mở rộng RPC ở C211/C212 bị lỗi ngay từ đầu), không sửa lại file migration cũ mà thêm migration mới **`20260807000100_them_dau_tieng_viet_seed_dong_hanh.sql`** — chỉ gồm các câu `UPDATE ... SET ... WHERE ma_... = '...'` viết lại đúng tiếng Việt có dấu cho từng dòng cụ thể trong cả 4 bảng seed (`dong_hanh_chi_so`, `dong_hanh_luat`, `dong_hanh_huy_hieu`, `dong_hanh_cau_dinh_huong`) theo đúng `ma_chi_so`/`ma_luat`/`ma_huy_hieu`/`ma_cau` đã seed — chạy an toàn dù bảng đã có dữ liệu (chỉ sửa đúng dòng theo khoá) hay chưa (0 dòng bị ảnh hưởng, không lỗi). Đã rà lại toàn bộ chữ tĩnh (label/tiêu đề) trong `RuleManagerPage.tsx` và `CompanionSection` (`TeacherStudentDetailPage.tsx`) — không phát hiện chữ thiếu dấu nào khác ngoài dữ liệu seed này, xác nhận lỗi chỉ nằm ở seed, không phải ở code giao diện. Không có thay đổi TypeScript, `tsc`/`npm test` (10/10) vẫn pass. **Người dùng cần dán migration mới này lên Supabase Dashboard SQL Editor** (chạy sau 2 migration đã có của C211/C212, theo đúng thứ tự tên file). |
| C216 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của thanh tab giáo viên (`TeacherStudentDetailPage.tsx`, C213) và báo bố cục không tối ưu cho di động. | Nguyên nhân: thanh tab dùng `grid grid-cols-2 gap-1 sm:grid-cols-4`, mỗi nút bố trí icon + nhãn NẰM NGANG (`flex items-center gap-1.5`) trong 1 ô lưới 2 cột trên di động — nhãn dài như "Ghi nhận & đề xuất" bị bó hẹp trong nửa chiều rộng màn hình, chữ dồn/xuống dòng lệch, icon và chữ chen nhau, khó bấm đúng ý. Đổi bố cục nút trên di động sang **icon nằm trên, nhãn nằm dưới** (`flex-col`, giống đúng mẫu thanh điều hướng dưới cùng đã dùng ở `StudentProfilePage.tsx`/`PROFILE_TABS`), container đổi từ `grid grid-cols-2` sang `flex overflow-x-auto` (cuộn ngang nếu 4 nút không vừa màn hình cực hẹp, mỗi nút `min-w-20 shrink-0` giữ độ rộng tối thiểu để không bị bóp méo) thay vì ép cứng 2x2; cỡ chữ nhãn giảm xuống `text-xs` trên di động cho gọn. Dùng breakpoint `md:` (không phải `sm:`) để chuyển sang bố cục ngang (icon+nhãn cùng hàng) và lưới 4 cột — đúng công thức đã rút ra và áp dụng nhất quán trong toàn app từ trước (`--breakpoint-sm` đã bị ghi đè xuống 375px nên `sm:` gần như luôn kích hoạt trên điện thoại thật, dùng nhầm `sm:` ở đây sẽ khiến bố cục ngang áp dụng sớm ngay trên hầu hết điện thoại thật, làm mất tác dụng của bản sửa gọn-cho-di-động này). `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C217 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của khối định danh học sinh (`StudentProfileHeader`, đầu trang hồ sơ công khai) và yêu cầu thiết kế lại cho nổi bật, bắt mắt, phù hợp đối tượng học sinh 16-18 tuổi — có icon/hình ảnh sinh động, không đơn điệu như hiện tại (nền xanh nhạt phẳng, avatar chữ cái đơn giản). | Thiết kế lại `StudentProfileHeader` theo tinh thần trẻ trung, năng động hơn: nền đổi từ `bg-sky-100` phẳng sang **gradient rực rỡ** `bg-linear-to-br from-blue-600 via-indigo-600 to-purple-600` với chữ trắng, thêm 2 icon lớn mờ trang trí góc (🎓 và ✨, giống thủ pháp đã dùng cho khối Đồng hành ở C213); avatar tăng kích thước (h-16 w-16, trước h-12 w-12), có viền sáng nổi bật `ring-4 ring-white/40` + hiệu ứng kính mờ `backdrop-blur`, và **hiển thị ảnh đại diện thật** (`student.anh_dai_dien`) nếu có thay vì luôn chỉ hiện chữ cái đầu tên (cột này đã có sẵn trong CSDL/`HocSinh` nhưng trước đây chưa được dùng ở đây). Vai trò (role) đổi từ chữ nhỏ đơn thuần sang dạng "pill" bo tròn nổi bật trên nền kính mờ. Thêm 1 hàng thẻ thông tin nhanh dạng pill mà trước đây hoàn toàn không hiển thị: 👥 Tổ (nếu có), 💁‍♀️/🙋‍♂️ Nữ/Nam, và 🚩 Cờ đỏ (chỉ hiện nếu `la_co_do = true`, nổi bật riêng bằng nền đỏ) — đều là các trường đã có sẵn trong `HocSinh` (`to`, `nu`, `la_co_do`) nhưng trước đây không được tận dụng hiển thị ở khối định danh này. Ô đếm "ghi nhận" đổi từ số nhỏ khô khan sang khối trắng nổi bật có icon 📋, số to đậm màu tím-chàm tương phản mạnh với nền gradient phía sau. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C218 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của khối "Ghi nhận của em" (header + 1 thẻ ghi nhận) và yêu cầu thiết kế lại gọn gàng hơn, xem chi tiết được khi cần (bấm/hover), tối ưu trên nhiều thiết bị. | **Header `FeaturedRecords`**: đổi từ khối phẳng `bg-blue-100` sang thẻ gradient nổi bật `bg-linear-to-br from-blue-600 to-cyan-500` chữ trắng, icon 🏆 lớn mờ trang trí góc, câu hướng dẫn ngắn "Chạm vào từng dòng để xem chi tiết đầy đủ" — đúng yêu cầu người dùng biết cách tương tác. `ImpactSummary` (dùng chung với tab Lịch sử) đổi từ 1 khối bảng 2 cột sang 2 "pill" trắng nổi trên nền gradient, có icon 🌟/⚠️, gọn và bắt mắt hơn ở cả 2 nơi dùng. **Từng dòng ghi nhận**: tách khỏi `RecordSummary` cũ (component này giữ nguyên, vẫn dùng cho tab "Lịch sử" — nơi người dùng không yêu cầu đổi, luôn hiện đủ chi tiết vì đã ở màn hình riêng có phân trang) — viết component mới `FeaturedRecordCard` dùng thẻ HTML gốc `<details>`/`<summary>` (có sẵn điều hướng bàn phím, hoạt động tốt trên cả cảm ứng lẫn chuột không cần thêm state React riêng cho từng thẻ): mặc định chỉ hiện 1 dòng gọn (icon 🌟/⚠️/📌 theo tính chất + nội dung rút gọn 1 dòng `truncate` + mã danh mục/ngày/điểm), có viền màu bên trái (`border-l-4`) báo hiệu tích cực/vi phạm/trung tính thay vì tô cả nền màu như trước (đỡ rối mắt khi xếp nhiều dòng liền nhau); bấm vào (hoặc di chuột rồi bấm — có `hover:bg-slate-50` gợi ý tương tác được) mới xổ ra đầy đủ: mã danh mục, các badge (tác động, loại ghi nhận, tiết, môn, điểm, số lần lặp), khối gợi ý xử lý nếu là vi phạm lặp lại — y hệt nội dung chi tiết cũ, chỉ khác là ẩn mặc định. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C219 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của nút "Nhập đề xuất ghi nhận cho lớp" (`LopTruongPanel`, chỉ hiện cho Lớp trưởng/Lớp phó có quyền) và yêu cầu thiết kế lại hoàn toàn cho thật nổi bật/ấn tượng ("nhìn vào mê luôn"), đồng thời hỏi có nên đổi vị trí hiển thị không. | **Đổi vị trí**: trước đây nút này nằm bên trong tab "Nổi bật" (chỉ thấy khi đúng tab đó đang mở, xem C213) — nhưng đây là 1 hành động (CTA) chỉ dành riêng cho 1 nhóm nhỏ học sinh có quyền, không phải nội dung để lướt xem, nên chuyển ra **luôn hiển thị ngay dưới khối Đồng hành, phía trên thanh tab** — bất kể đang xem tab nào cũng thấy ngay, không bị chôn trong tab. **Thiết kế lại nút đóng (chưa mở form)**: đổi từ khối phẳng `bg-teal-100` sang thẻ gradient rực rỡ `from-teal-500 via-emerald-500 to-cyan-500` chữ trắng, icon 📢 lớn đặt trong vòng tròn kính mờ kèm **hiệu ứng "beacon" nhấp nháy lan toả** (`animate-ping`, vòng sáng mở rộng rồi mờ dần quanh icon liên tục — kiểu hiệu ứng thường dùng để thu hút chú ý tới thông báo/nút hành động quan trọng) để bắt mắt ngay từ cái nhìn đầu, icon lớn ✍️ mờ trang trí góc phóng to nhẹ khi hover, mũi tên → trượt sang phải khi hover (chỉ hiện từ `sm:` trở lên, tránh chật trên màn hình rất hẹp); toàn bộ nút sáng hơn + đổ bóng đậm hơn khi hover (`hover:brightness-105 hover:shadow-2xl`) và hơi lún xuống khi chạm/bấm (`active:scale-[0.99]`) để có phản hồi rõ ràng khi tương tác trên di động. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C220 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng báo: chia sẻ link cá nhân `/#/hs/<token>` cho học sinh, nhưng khi học sinh "Thêm vào màn hình chính" trên điện thoại, icon PWA chỉ lưu được `http://localhost:5173/qlhs` (không giữ được `#/hs/<token>`) — bấm icon vào lại ra đúng màn hình đăng nhập giáo viên, học sinh không đăng nhập được. Yêu cầu: 1 link/icon dùng chung cho cả giáo viên và học sinh, tự điều hướng đúng theo tài khoản khi đăng nhập. | Xác nhận đúng: PWA "Thêm vào màn hình chính" luôn mở theo `start_url` khai báo trong `vite.config.ts` (`'/qlhs/'`), trình duyệt bỏ qua route hash hiện tại lúc bấm thêm — đây là hành vi chuẩn của PWA, không có cách nào ép icon nhớ đúng `/hs/:token` riêng của từng học sinh. Hướng giải quyết đúng đắn không phải "giữ được link cũ" mà là **làm cho trang gốc `/` tự thân đăng nhập được cho cả 2 vai trò**, để học sinh mở icon app y như 1 app thật, tự đăng nhập bằng SĐT + mật khẩu của mình mà không cần biết token. Thêm RPC mới `lay_ho_so_cong_khai_theo_sdt(p_sdt, p_mat_khau)` (migration `20260807000200_dang_nhap_hoc_sinh_theo_sdt.sql`) — dò `hoc_sinh` theo `sdt_1` khớp (thay vì theo token như `lay_ho_so_cong_khai` cũ), tìm ra đúng `token_ho_so` rồi GỌI LẠI `lay_ho_so_cong_khai(token, sdt, mat_khau)` sẵn có để trả đúng y hệt cấu trúc dữ liệu (không viết lại toàn bộ `jsonb_build_object`, tránh trùng lặp logic và lệch nhau khi 1 trong 2 hàm được sửa sau này). **`DataSource.ts`/`SupabaseDataSource.ts`**: thêm `getPublicStudentProfileByPhone(sdt, matKhau)`, tách phần map dữ liệu RPC dùng chung (`mapPublicProfileRpcRow`) để cả 2 hàm `getPublicStudentProfile`/`getPublicStudentProfileByPhone` tái dùng, không chép lại. **`StudentProfilePage.tsx`**: export thêm `loginStorageKey()` (trước đây chỉ dùng nội bộ file) để `Layout.tsx` ghi đúng cùng định dạng khoá `sessionStorage`, tránh 2 nơi tự định nghĩa khoá dễ lệch nhau. **`Layout.tsx`**: đổi `TeacherLoginPage` thành `UnifiedLoginPage` — 1 form duy nhất ở trang gốc `/`, ô "định danh" (email hoặc SĐT) tự nhận diện qua regex `/^0\d{9}$/`: nếu là số điện thoại → gọi `getPublicStudentProfileByPhone`, thành công thì lưu session vào đúng `sessionStorage` key theo token tìm được rồi **điều hướng client-side** (`useNavigate`, không tải lại trang) sang `/hs/:token` — `StudentProfilePage.tsx` đã có sẵn logic tự nạp lại phiên từ `sessionStorage` lúc mount nên không cần sửa gì thêm ở trang đó; nếu không phải số điện thoại → giữ nguyên luồng đăng nhập Supabase email/mật khẩu cho giáo viên như cũ. Trường "định danh" đổi `type="email"` thành `type="text"` để chấp nhận cả số điện thoại. Không đổi gì ở route `/hs/:token` — link cá nhân cũ vẫn dùng được bình thường, đây chỉ là thêm 1 đường vào nữa từ trang gốc. `tsc`/`npm run build`/`npm test` (10/10) đều pass. **Cần dán migration `20260807000200_dang_nhap_hoc_sinh_theo_sdt.sql` lên Supabase Dashboard SQL Editor** (chạy sau các migration trước đó của C211/C212/C215) rồi hướng dẫn học sinh: mở link bất kỳ 1 lần để "Thêm vào màn hình chính" (icon sẽ trỏ về trang gốc), lần sau bấm icon sẽ ra màn hình đăng nhập chung, nhập đúng SĐT + 3 số cuối SĐT là vào được hồ sơ của mình. |
| C221 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng chỉnh lại yêu cầu ở C220: không muốn 1 ô nhập chung tự đoán "Email (giáo viên) hoặc số điện thoại (học sinh)" — muốn màn hình đăng nhập có 2 chế độ tách bạch, chuyển qua lại bằng nút bấm rõ ràng cho từng vai trò. | Thay `UnifiedLoginPage` (C220) từ 1 ô "định danh" dùng chung + tự nhận diện qua regex, sang **2 nút chuyển chế độ tường minh** "👩‍🏫 Giáo viên" / "🎓 Học sinh" dạng segmented control ở đầu form (nút đang chọn tô nền xanh); mỗi chế độ có form + state riêng biệt hoàn toàn (không dùng chung 1 field `identifier` nữa): chế độ Giáo viên giữ nguyên `type="email"` + nhãn "Email" như thiết kế gốc trước C220; chế độ Học sinh dùng đúng các thuộc tính input đã có sẵn ở form đăng nhập gốc của `StudentProfilePage.tsx` (`type="tel"`, `inputMode="numeric"`, `maxLength=10` cho SĐT; `inputMode="numeric"`, `maxLength=3` cho mật khẩu, nhãn ghi rõ "Mật khẩu (3 số cuối SĐT)"), validate đúng thông báo lỗi tương tự trang đó ("Số điện thoại phải gồm đúng 10 chữ số, bắt đầu bằng số 0.", "Mật khẩu phải gồm đúng 3 chữ số."). Logic xác thực/điều hướng phía sau (RPC `lay_ho_so_cong_khai_theo_sdt`, lưu `sessionStorage` theo `loginStorageKey()`, điều hướng `navigate('/hs/:token')`) giữ nguyên như C220, chỉ đổi phần giao diện nhập liệu. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C222 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng chỉ ra rủi ro bảo mật: nhãn "Mật khẩu (3 số cuối SĐT)" ở form đăng nhập học sinh (C221) lộ ngay cách suy ra mật khẩu chỉ từ số điện thoại — ai biết SĐT của 1 học sinh là đoán được mật khẩu ngay trên chính màn hình đăng nhập, cần bỏ; đồng thời yêu cầu rà lại validation phần đăng nhập. | Đổi nhãn ô mật khẩu học sinh từ "Mật khẩu (3 số cuối SĐT)" về đúng "Mật khẩu" trơn (không tiết lộ quy tắc suy ra mật khẩu) — khớp với nhãn đã dùng ở form đăng nhập gốc của `StudentProfilePage.tsx` (nơi này chưa từng ghi rõ quy tắc, chỉ C221 mới lỡ thêm chú thích lộ bí mật). Validation client-side cho mật khẩu vẫn giữ nguyên như C221 (đúng 3 chữ số, `inputMode="numeric"` + `maxLength=3`), không đổi logic — chỉ đổi câu chữ hiển thị. Rà lại luôn nút "Đăng nhập" chế độ Giáo viên: trước đây chỉ khoá nút khi đang gửi (`disabled={submitting}`), không khoá khi trường còn trống (chỉ dựa vào `required` gốc của trình duyệt) — thêm điều kiện `!email.trim() || !teacherPassword.trim()` vào `disabled`, khớp đúng cách nút "Đăng nhập" chế độ Học sinh đã làm, cho nhất quán cả 2 chế độ và tránh gửi request rỗng nếu trình duyệt nào đó bỏ qua `required`. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C223 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng yêu cầu 2 việc: (1) ghi 1 dòng thể hiện bản quyền thuộc về "Nguyễn Duy Tân, thầy giáo Tin học" vào migration `20260807000200_dang_nhap_hoc_sinh_theo_sdt.sql` (nhìn chuyên nghiệp); (2) thiết kế lại hoàn toàn giao diện màn hình đăng nhập (C220-C222) cho thật bắt mắt, "cool ngầu" hợp gu học sinh, có thể thêm icon/hình ảnh động, hiệu ứng lung linh. | **Bản quyền**: thêm khối comment tiêu đề `-- (c) Nguyen Duy Tan - Giao vien Tin hoc, GVCN 11C5` ở đầu file migration đã có (chỉ thêm comment, không đổi logic SQL nào — an toàn sửa trực tiếp dù migration này có thể đã dán chạy, vì comment SQL không ảnh hưởng hành vi hàm đã tạo). **Giao diện đăng nhập**: thêm bộ hiệu ứng CSS dùng chung vào `src/index.css` (`@keyframes qlhs-gradient-shift/qlhs-float-up/qlhs-glow-pulse` + class `.qlhs-login-bg/.qlhs-sparkle/.qlhs-glow`, có `@media (prefers-reduced-motion: reduce)` tắt hết animation cho người dùng đã bật chế độ giảm chuyển động ở hệ điều hành — tôn trọng khả năng tiếp cận). `Layout.tsx`: `TeacherAuthShell` (khung bọc chung cho cả màn hình đăng nhập lẫn màn hình "đang kiểm tra phiên") đổi nền sang gradient tím-xanh-hồng chuyển động liên tục (`qlhs-login-bg`), rắc thêm 10 hạt sparkle (✨🌟💫⭐🎓) bay từ dưới lên xuyên suốt màn hình với độ trễ/tốc độ lệch nhau (mảng `LOGIN_SPARKLES` tĩnh, không cần state/random runtime). Thẻ đăng nhập (`UnifiedLoginPage`) đổi từ khối trắng phẳng sang **thẻ kính mờ (glassmorphism)** `bg-white/10 backdrop-blur-xl` viền sáng có hiệu ứng phát sáng nhấp nháy liên tục (`qlhs-glow`), icon 🎓 lớn mờ trang trí góc, icon mũ tốt nghiệp nảy nhẹ (`animate-bounce`) trong vòng tròn kính mờ ở đầu thẻ, tiêu đề đổi thành "Chào mừng trở lại!" chữ gradient rực rỡ (cyan→hồng→vàng, `bg-clip-text`). 2 nút chuyển chế độ Giáo viên/Học sinh đổi màu cho hợp nền tối (nút đang chọn nổi trắng, chữ tím đậm). Nút "Đăng nhập" mỗi chế độ đổi màu gradient riêng — Giáo viên: cyan→xanh dương kèm mũi tên "Đăng nhập →"; Học sinh: hồng tím→vàng cam kèm câu "Vào lớp thôi! 🚀" trẻ trung hơn hẳn chữ "Đăng nhập" khô khan cũ — cả 2 đều sáng lên + đổ bóng màu khi hover. Ô nhập vẫn giữ nền trắng đục (không làm kính mờ luôn ô nhập) để chữ nhập vào luôn rõ ràng dễ đọc, chỉ nhãn chữ đổi sang trắng cho tương phản đúng với nền tối phía sau. Thêm dòng bản quyền "© 2026 Nguyễn Duy Tân — Giáo viên Tin học" mờ nhẹ dưới cùng thẻ đăng nhập (khớp yêu cầu (1) ở phần giao diện, không chỉ trong migration). `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C224 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của thanh điều hướng dưới cùng trên di động (trang hồ sơ công khai) và yêu cầu thiết kế lại hoàn toàn, dùng icon "xịn hơn đẹp hơn ngầu hơn" thay vì emoji cũ. | Icon emoji (⭐🕘📊👤) hiển thị khác nhau tuỳ font hệ điều hành/trình duyệt (không đồng bộ, không sắc nét) — đổi sang **icon SVG nét vẽ tự tay** (component `NavTabIcon` mới, chỉ dùng riêng cho thanh nav dưới cùng, không đổi `PROFILE_TABS` dùng chung cho menu ☰ và thanh tab desktop để không phá vỡ nơi khác đang dùng emoji): ngôi sao đặc (Nổi bật), đồng hồ viền mảnh có kim (Lịch sử), biểu đồ cột tăng dần (Điểm tuần), người viền mảnh (Cá nhân) — phong cách nét mảnh đồng nhất (`stroke-width 2`, bo góc), sắc nét ở mọi kích thước màn hình, không phụ thuộc font emoji máy. Bố cục thanh nav đổi từ dải xám phẳng dính sát 2 mép màn hình + cuộn ngang (do dùng `min-w-16 shrink-0` ép chiều rộng cố định) sang **thanh nổi bo góc lớn** cách mép 1 khoảng (`px-3`), nền kính mờ trắng đục `bg-white/85 backdrop-blur-lg`, đổ bóng mềm lan toả phía trên; 4 nút chia đều `flex-1` (không cần cuộn ngang nữa vì chỉ có đúng 4 mục, chiều rộng co giãn theo màn hình). Tab đang chọn có **viên tròn nền xanh nhạt nổi bật quanh icon** (`bg-blue-100`, phóng to nhẹ `scale-105`, đổ bóng trong `shadow-inner`) thay vì chỉ đổi màu chữ đơn thuần như trước; tab bấm xuống co nhẹ (`group-active:scale-95`) tạo phản hồi chạm rõ ràng trên di động. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C225 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng báo: bấm vào mã danh mục (badge dạng nút bo tròn, vd "KL18") để xem chi tiết, nội dung hiển thị trên di động bị khuất/che lấp không nhìn thấy được. | Đúng lớp lỗi đã gặp và sửa ở `PhoneActionMenu.tsx` (C214): `CatalogCodeBadge.tsx` định vị popover chi tiết bằng `position: absolute` ngay bên trong chính nút bấm (`<span className="relative inline-flex">...<span className="absolute left-0 top-full">`). Component này được dùng ở rất nhiều nơi nằm trong bảng/hàng có `overflow-x-auto` (`CatalogPage.tsx`, `TeacherStudentDetailPage.tsx`, `RecordEntryPage.tsx`, danh sách ghi nhận ở `StudentProfilePage.tsx`...) nên trên di động, ancestor có `overflow-x-auto` cắt/kẹp mất popover hoặc đẩy nó ra ngoài vùng nhìn thấy, đúng như người dùng mô tả "bị khuất". Sửa theo đúng cách đã dùng cho `PhoneActionMenu`: **portal popover ra `document.body`** (`createPortal`) và định vị bằng `position: fixed` tính theo toạ độ thật của nút bấm qua `getBoundingClientRect()` (kẹp trong khoảng viewport để không tràn 2 bên màn hình hẹp), thay vì phụ thuộc vị trí DOM cha; tự đóng khi cuộn (bắt cả cuộn của bảng `overflow-x-auto` lẫn cuộn trang) hoặc đổi kích thước màn hình. Không cần sửa gì ở các trang đang dùng `CatalogCodeBadge` vì toàn bộ logic định vị đã đóng gói trong chính component. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
| C226 | ⛔ Bị chặn (cần xác nhận) | 07/08/2026 | Người dùng dán HTML thực tế của thanh điều hướng dưới cùng (trang hồ sơ công khai, C224) và yêu cầu: thanh này không nên "ghim cứng" cố định — kéo xuống thì ẩn đi, kéo lên thì hiện lại, giống ứng dụng Facebook. | Phát hiện `Layout.tsx` (menu điều hướng phía giáo viên) đã có sẵn đúng cơ chế này (`bottomBarVisible` state + `lastScrollY` ref, theo dõi `window.scroll` — kéo xuống quá 8px thì ẩn, kéo lên quá 8px hoặc còn gần đỉnh trang (`scrollY < 24`) thì hiện lại, có `transition-transform duration-200` + `translate-y-0/translate-y-full` để trượt mượt) nhưng trang hồ sơ công khai (`StudentProfilePage.tsx`) chưa từng có, nên áp dụng lại y hệt logic đó cho nhất quán hành vi toàn app thay vì viết cách khác. Thêm state `bottomNavVisible` + `lastScrollY` ref + `useEffect` lắng nghe sự kiện `scroll` (copy nguyên logic ngưỡng 8px/24px đã kiểm chứng từ `Layout.tsx`, không thay đổi con số). Thanh nav (`<nav>` đã thiết kế nổi bo góc ở C224) đổi `className` thêm `transition-transform duration-200` và điều kiện `translate-y-0`/`translate-y-full` theo `bottomNavVisible`, giữ nguyên toàn bộ phần giao diện icon/bố cục đã làm ở C224, chỉ thêm hành vi ẩn/hiện theo hướng cuộn. `tsc`/`npm run build`/`npm test` (10/10) đều pass. |
---

**Chú thích trạng thái**: `☐ Chưa làm` / `🔄 Đang làm` / `✅ Xong` / `⛔ Bị chặn (cần xác nhận)`

# Theo dõi tiến độ — Giai đoạn 1

Cập nhật file này sau **mỗi commit hoàn thành** (xem quy trình ở [05-quy-tac-ai-agent.md](05-quy-tac-ai-agent.md)). Danh sách commit đã cập nhật ngày 11/07/2026 theo tài liệu 04 (bản 28 commit, gồm cả C021a).

## Nhóm A — Chuẩn bị dữ liệu (deadline: Thứ Hai 13/07/2026)

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C001 | ✅ Xong | 11/07/2026 |  |
| C002 | ✅ Xong | 11/07/2026 |  |
| C003 | ✅ Xong | 11/07/2026 | Script SetupSheet.gs + seed DanhMucDiem |
| C004 | ✅ Xong | 11/07/2026 | 36 HS + 2 tuần trong du-lieu-mau/ |
| C005 | ✅ Xong | 11/07/2026 | Phiếu 3 phần + bảng tra cứu mã |
| C006 | ✅ Xong | 11/07/2026 | huong-dan-quy-trinh-import.md |

## Nhóm B — Khung sườn ứng dụng web

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C007 | ✅ Xong | 11/07/2026 | Vite + React + TS strict |
| C008 | ✅ Xong | 11/07/2026 | Tailwind v4 + ResponsiveDemo |
| C009 | ✅ Xong | 11/07/2026 | GitHub Actions deploy-pages |
| C010 | ✅ Xong | 11/07/2026 | HashRouter + Layout |
| C011 | ✅ Xong | 11/07/2026 | DataSource + types |
| C012 | ✅ Xong | 11/07/2026 | Code.gs doGet/doPost |
| C013 | ✅ Xong | 11/07/2026 | Import batch + Drive + log |

## Nhóm C — Tính năng cốt lõi

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C014 | ✅ Xong | 11/07/2026 | GoogleSheetsDataSource đọc dữ liệu + trang học sinh kiểm chứng getStudents |
| C015 | ✅ Xong | 11/07/2026 | Trang học sinh có tìm kiếm + thêm/sửa/xoá qua GoogleSheetsDataSource |
| C016 | ✅ Xong | 11/07/2026 | Màn hình Import JSON chung: chọn loại, dán/tải file, preview, xác nhận import |
| C017 | ✅ Xong | 11/07/2026 | Hồ sơ theo token + vai trò BanCanSu, không hiển thị SĐT phụ huynh |
| C018 | ✅ Xong | 11/07/2026 | Hồ sơ hiển thị lịch sử GhiNhan theo học sinh, sắp xếp mới nhất và gom theo tuần |
| C019 | ✅ Xong | 11/07/2026 | Logic tính điểm thi đua: 4 thành phần, học tập, tổng hợp chia 6, bỏ qua tập thể/tổ trực — ~~⚠️ có lỗi 66,67~~ đã sửa ở C031 |
| C020 | ✅ Xong | 11/07/2026 | Hồ sơ hiển thị điểm thi đua, điểm học tập, tổng hợp và xếp loại |
| C021 | ✅ Xong | 11/07/2026 | Dashboard giáo viên: điểm thi đua, cảnh báo, sự kiện tập thể/tổ trực |
| C021a | ✅ Xong | 11/07/2026 | Dashboard xử lý sự kiện tập thể: gán 1 HS, áp dụng lớp/tổ, bỏ qua |
| C022 | ✅ Xong | 11/07/2026 | Gợi ý xử lý sư phạm rule-based trên dashboard |
| C023 | ✅ Xong | 11/07/2026 | Tối ưu hồ sơ di động với khối ghi nhận hôm nay nổi bật |
| C024 | ✅ Xong | 11/07/2026 | Nút tải mẫu phiếu ghi nhận HTML in được từ web app |

## Nhóm D — Hoàn thiện & bảo mật

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C025 | ✅ Xong | 11/07/2026 | Ẩn SĐT khỏi giao diện và response hồ sơ public theo token |
| C026 | ✅ Xong | 11/07/2026 | Rà responsive: header/nav tối ưu mobile và laptop |
| C027 | ✅ Xong | 11/07/2026 | README hướng dẫn sử dụng + tag v0.1.0-phase1 |

---

## Cải tiến sau triển khai (xem chi tiết ở [06-cai-tien-sau-trien-khai.md](06-cai-tien-sau-trien-khai.md))

| ID | Trạng thái | Ngày xong | Ghi chú |
|---|---|---|---|
| C028 | ✅ Xong | 11/07/2026 | Cơ chế write_secret ban đầu cho doPost; đã được thay thế bởi đăng nhập giáo viên + session token ở C060 |
| C029 | ✅ Xong | 11/07/2026 | README/hướng dẫn deploy ghi rõ Execute as Me, Anyone, Manage deployments; phần bảo mật đã cập nhật lại theo C060 |
| C030 | ✅ Xong | 11/07/2026 | Danh sách học sinh mở rộng accordion, xem/copy link hồ sơ |
| C031 | ✅ Xong | 11/07/2026 | Chưa có điểm học tập thì chia 4 thành phần, không còn mặc định 66,67 |
| C032 | ✅ Xong | 11/07/2026 | Thêm trường to cho HocSinh, hiển thị tổ trên danh sách/hồ sơ và xử lý tổ trực |
| C033 | ✅ Xong | 11/07/2026 | Dashboard có nhật ký theo ngày trong tuần, kể cả ngày chưa có ghi nhận |
| C034 | ✅ Xong | 11/07/2026 | Import GhiNhan tự khớp ho_ten sang ma_hs, tự điền tuần/diện/điểm và log lỗi từng dòng |
| C035 | ✅ Xong | 11/07/2026 | Thêm mẫu JSON và prompt chuyển ảnh phiếu sang JSON vào du-lieu-mau |
| C036 | ✅ Xong | 11/07/2026 | Xoá dữ liệu theo lần import: nút xoá trên lịch sử import, Apps Script xoá GhiNhan theo ma_log_import và đánh dấu log da_xoa |
| C037 | ✅ Xong | 11/07/2026 | Bộ chọn tuần dùng chung cho Dashboard và hồ sơ học sinh, mặc định chọn theo CauHinhTuan |
| C038 | ✅ Xong | 11/07/2026 | Hồ sơ mặc định hiển thị toàn bộ lịch sử, có bộ lọc tuần đang chọn và ngày cụ thể |
| C039 | ✅ Xong | 11/07/2026 | Apps Script tự tính tuan_so khi import, cảnh báo nếu ngoài CauHinhTuan và thêm vaLaiTuanSoChoGhiNhan() để vá dữ liệu cũ |
| C040 | ✅ Xong | 11/07/2026 | Thêm bộ chọn ngày cụ thể cho Nhật ký theo ngày và lịch sử ghi nhận hồ sơ |
| C041 | ✅ Xong | 11/07/2026 | Xác minh danh sách học sinh; thêm nút Chi tiết/Thu gọn rõ ràng cho từng dòng |
| C042 | ✅ Xong | 11/07/2026 | Xác minh màn Import bằng Chrome headless: có Lịch sử import và nút Xoá dữ liệu của lần này |
| C043 | ✅ Xong | 11/07/2026 | Dashboard có vùng thống kê tổng quan theo tài liệu 07, chia nhóm hành động ngay và quan sát chung |
| C044 | ✅ Xong | 11/07/2026 | Thêm nút thu gọn/mở rộng cả khối danh sách trên Dashboard và trang Học sinh |
| C045 | ✅ Xong | 11/07/2026 | Drill-down cho các thẻ TK02-TK05 ngay trên Dashboard, mở đúng danh sách chi tiết cấu thành |
| C046 | ✅ Xong | 11/07/2026 | Bấm sự kiện tổ trực mở Thông tin Tổ: tổ trưởng, danh sách học sinh và lịch sử nhắc tổ gần đây |
| C047 | ✅ Xong | 11/07/2026 | Thêm helper màu chung và áp dụng cho Dashboard, hồ sơ, lịch sử ghi nhận, nhật ký ngày, danh sách học sinh |
| C048 | ✅ Xong | 11/07/2026 | Sửa ngưỡng xếp loại theo trạng thái có/chưa có điểm học tập, thêm ghi chú so sánh điểm và dữ liệu đối chiếu cá nhân |
| C049 | ✅ Xong | 12/07/2026 | Thêm khu vực Xem theo Nhóm vi phạm trên Dashboard, có chọn nhóm, hiện đủ/ẩn học sinh sạch và TK06 nhảy tới nhóm Kỷ luật |
| C050 | ✅ Xong | 12/07/2026 | Mở rộng điều hướng TK01/TK06/TK07/TK08 và thêm popup tra cứu mã cho badge ở Dashboard/Hồ sơ |
| C051 | ✅ Xong | 12/07/2026 | Thêm loai_tuan và script taoCauHinhTuanNamHoc() sinh cấu hình tuần cả năm, đánh dấu tuần nghỉ |
| C052 | ✅ Xong | 12/07/2026 | Bộ chọn tuần có nút trước/sau, chọn nhanh nhóm theo tháng/học kỳ và bỏ qua tuần nghỉ |
| C053 | ✅ Xong | 12/07/2026 | Drill-down TK hiển thị bằng modal overlay, đóng bằng X/nền tối/Esc |
| C054 | ✅ Xong | 12/07/2026 | Bấm lại nhóm đang chọn sẽ bỏ lọc và quay về danh sách tổng mặc định |
| C055 | ✅ Xong | 12/07/2026 | Thêm STT cho bảng điểm Dashboard, Xem theo Nhóm và các danh sách học sinh trong modal TK |
| C056 | ✅ Xong | 12/07/2026 | Thêm route giáo viên /quan-ly/hoc-sinh/:maHs có SĐT, tel:, edit trực tiếp; link danh sách trỏ đúng route mới |
| C056a | ✅ Xong | 12/07/2026 | Sửa lỗi SĐT từ Sheet trả về dạng số làm trang chi tiết giáo viên crash khi bấm Xem hồ sơ đầy đủ |
| C057 | ✅ Xong | 12/07/2026 | Mẫu phiếu in trong app và docs/mau-phieu-ghi-nhan.md chuyển sang 1 bảng duy nhất, không mã |
| C058 | ✅ Xong | 12/07/2026 | Prompt AI suy luận mã/phạm vi từ phiếu tự do, map thành tích sang KT và bổ sung KT01-KT05 vào seed DanhMucDiem |
| C059 | ✅ Xong | 12/07/2026 | Cập nhật Vite base path thành /qlhs/ cho GitHub Pages repo teacherduytan/qlhs |
| C059a | ✅ Xong | 12/07/2026 | Hoàn thiện CI/CD GitHub Pages: deploy bằng Actions, tránh trùng artifact và đọc VITE_APPS_SCRIPT_URL từ GitHub Secrets |
| C060 | ✅ Xong | 13/07/2026 | Thêm đăng nhập giáo viên thật qua Apps Script, session token lưu sessionStorage, bỏ VITE write_secret khỏi bundle, ẩn lối điều hướng từ hồ sơ học sinh sang vùng giáo viên |
| C061 | ✅ Xong | 13/07/2026 | Sửa prompt tách điểm số thành dòng hoc_tap riêng; import cảnh báo/bỏ qua diem_so_mon sai loại và Điểm học tập chỉ đọc loai=hoc_tap |
| C062 | ✅ Xong | 13/07/2026 | Đưa ghi nhận của học sinh thành nội dung nổi bật đầu trang hồ sơ public; thông tin cá nhân chuyển vào tab phụ |
| C063 | ✅ Xong | 13/07/2026 | Tách khen thưởng/KT khỏi Vi phạm phổ biến và thêm ghi nhận tích cực trên Dashboard |
| C064 | ✅ Xong | 13/07/2026 | Phân màu nền/viền các section Dashboard và hồ sơ học sinh theo khối nội dung |
| C065 | ✅ Xong | 13/07/2026 | Lọc/sắp xếp bảng, tổng tích cực/tiêu cực, đếm lặp vi phạm và gợi ý xử lý |
| C066 | ✅ Xong | 13/07/2026 | Thêm điều hướng nhanh tới section và nút thu gọn/mở rộng từng section |
| C067 | ✅ Xong | 13/07/2026 | TK05 đếm/mở đầy đủ danh sách vi phạm và đổi nhãn tiêu cực thành vi phạm |
| C068 | ✅ Xong | 13/07/2026 | Đưa vi phạm học tập tự do như quên dụng cụ vào Xem theo Nhóm vi phạm |
| C069 | ✅ Xong | 13/07/2026 | Xem theo Nhóm vi phạm hiển thị nội dung vi phạm thay vì chỉ mã |
| C070 | ✅ Xong | 13/07/2026 | Phân loại Tích cực/Vi phạm ngay trong cột Nội dung vi phạm/ghi nhận |
| C071 | ✅ Xong | 13/07/2026 | Thêm trang quản lý danh mục vi phạm/tích cực và CRUD tab DanhMucDiem |
| C072 | ✅ Xong | 13/07/2026 | Gắn ghi nhận vi phạm/tích cực lên học sinh theo cá nhân, tổ hoặc cả lớp |
| C073 | ✅ Xong | 13/07/2026 | Đồng bộ logic ghi nhận tay với import JSON và chỉ gắn danh mục cá nhân lên học sinh |
| C074 | ✅ Xong | 13/07/2026 | Liên kết nội dung Xem theo nhóm vi phạm với DanhMucDiem |
| C075 | ✅ Xong | 14/07/2026 | Xoá ghi nhận/vi phạm trong hồ sơ giáo viên của học sinh |
| C076 | ✅ Xong | 14/07/2026 | Báo rõ lỗi xoá ghi nhận khi Apps Script chưa deploy action delete_record |
| C077 | ✅ Xong | 14/07/2026 | Thêm health check Apps Script và lỗi xoá ghi nhận chi tiết hơn |
| C078 | ✅ Xong | 14/07/2026 | Chuyển đăng nhập/kiểm tra phiên giáo viên sang GET để tránh Failed to fetch |
| C079 | ✅ Xong | 14/07/2026 | Cập nhật Web App URL Apps Script hiện hành và hướng dẫn đồng bộ endpoint |
| C080 | ✅ Xong | 15/07/2026 | Xoá học sinh/ghi nhận khỏi danh mục theo chiều ngược và hỗ trợ xoá nhiều dòng |
| C081 | ✅ Xong | 15/07/2026 | Đổi danh sách học sinh theo danh mục sang modal thay vì section dưới bảng |
| C082 | ✅ Xong | 15/07/2026 | Rà và ép import GhiNhan khớp DanhMucDiem động, cảnh báo mã chưa có |
| C083 | ✅ Xong | 15/07/2026 | Rà soát dữ liệu mẫu/dữ liệu giả, cập nhật prompt AI theo import danh mục động |
| C084 | ✅ Xong | 15/07/2026 | Cập nhật prompt AI: đối chiếu DanhMucDiem hiện hành và đề xuất danh mục mới từ mô tả thô |
| C085 | ✅ Xong | 15/07/2026 | Tạo nhanh danh mục từ đề xuất AI ngay trong màn hình Import |
| C086 | ✅ Xong | 15/07/2026 | Chuẩn hoá luồng tạo mới bằng modal và wizard |
| C087 | ✅ Xong | 15/07/2026 | Tự sinh mã danh mục không trùng theo nhóm |
| C088 | ✅ Xong | 15/07/2026 | Cập nhật prompt: cung cấp danh sách học sinh để AI khớp tên viết tay/viết tắt |
| C089 | ✅ Xong | 15/07/2026 | Cập nhật prompt: AI đọc ngữ cảnh phiếu viết tay không đúng cấu trúc |
| C090 | ✅ Xong | 15/07/2026 | Cập nhật prompt: bắt AI tự kiểm tra mã danh mục và mã học sinh trước khi xuất JSON |
| C091 | ✅ Xong | 15/07/2026 | Đồng bộ bản prompt cũ trong docs với bản chuẩn ở du-lieu-mau để tránh lấy nhầm |
| C092 | ✅ Xong | 15/07/2026 | Làm rõ cách import xử lý dòng JSON có ma_hs null |
| C093 | ✅ Xong | 15/07/2026 | Màn Import tự xử lý dòng ma_hs null bằng gắn/tạo học sinh với mã tự sinh; prompt không bắt AI tự quyết ma_hs |
| C094 | ✅ Xong | 15/07/2026 | Màn Import tạo danh mục thiếu từ mã JSON và cho import trước các dòng đủ điều kiện |
| C095 | ✅ Xong | 15/07/2026 | Sửa hồi quy C094 để logic xử lý ma_hs null của C093 vẫn nhận diện dòng cá nhân theo ma_hs/ho_ten |
| C096 | ✅ Xong | 15/07/2026 | Sửa nhãn hồ sơ/chi tiết học sinh để mã KT hoặc điểm dương hiển thị là Tích cực/thành tích |
| C097 | ✅ Xong | 15/07/2026 | Đồng bộ giao diện chi tiết học sinh của giáo viên với hồ sơ học sinh |
| C098 | ✅ Xong | 15/07/2026 | Làm rõ màu sắc tích cực và vi phạm trên trang quản lý học sinh |
| C099 | ✅ Xong | 15/07/2026 | Đổ màu toàn bộ dòng học sinh theo tích cực/vi phạm trong danh sách quản lý |
| C100 | ✅ Xong | 15/07/2026 | Đổ màu tích cực/vi phạm trong trang chi tiết học sinh |
| C101 | ✅ Xong | 15/07/2026 | Cập nhật prompt để không dùng mã NN08/NN09 cho lỗi không mang dụng cụ học tập |
| C102 | ✅ Xong | 15/07/2026 | Import cho so sánh đề xuất danh mục mới với danh mục cũ và chọn dùng mã có sẵn |
| C103 | ✅ Xong | 15/07/2026 | Bổ sung mô tả và đề xuất xử lý/phạt cho danh mục vi phạm/tích cực |
| C104 | ✅ Xong | 15/07/2026 | Cập nhật prompt AI để đề xuất danh mục mới có mô tả và đề xuất xử lý |
| C105 | ✅ Xong | 15/07/2026 | Tạo danh mục mã đề xuất xử lý/phạt và liên kết từ danh mục điểm/import |
| C106 | ✅ Xong | 15/07/2026 | Báo lỗi rõ khi Apps Script chưa deploy action DanhMucXuLy |
| C107 | ✅ Xong | 15/07/2026 | Chặn tạo mã xử lý/phạt trùng nội dung khi người dùng bấm lặp |
| C108 | ✅ Xong | 16/07/2026 | Sửa luồng import từng phần để tiếp tục xử lý các dòng còn lại |
| C109 | ✅ Xong | 16/07/2026 | Tự gợi ý/gắn danh mục cho dòng import thiếu ma_danh_muc |
| C110 | ✅ Xong | 16/07/2026 | Chặn spam bấm import gây tạo dư dữ liệu |
| C111 | ✅ Xong | 16/07/2026 | Cập nhật prompt để AI không đưa ghi chú đối chiếu DanhMucDiem vào nội dung |
| C112 | ✅ Xong | 16/07/2026 | Cập nhật prompt bằng ngữ cảnh DanhMucDiem và logic import hiện tại |
| C113 | ✅ Xong | 16/07/2026 | Thêm unit test cho logic so khớp DanhMucDiem khi import |
| C114 | ✅ Xong | 17/07/2026 | Rà soát nút Tải phiếu mẫu sau các thay đổi import/danh mục |
| C115 | ✅ Xong | 17/07/2026 | Chuyển phiếu ghi nhận sang dạng ghi theo nội dung, nhiều học sinh chung một dòng |
| C116 | ✅ Xong | 20/07/2026 | Xây dựng tính năng Báo cáo sĩ số từ spec |
| C117 | ✅ Xong | 20/07/2026 | Thiết kế lại bộ lọc thời gian Dashboard và tự mở rộng CauHinhTuan theo ngày thực tế |
| C118 | ✅ Xong | 22/07/2026 | Tạo migration Supabase theo schema thật đã đối chiếu từ Apps Script và docs |
| C119 | ✅ Xong | 22/07/2026 | Chuyển đăng nhập giáo viên sang Supabase Auth và gửi access token cho Apps Script |
| C120 | ✅ Xong | 23/07/2026 | Chuyển lớp đọc/ghi dữ liệu chính sang SupabaseDataSource; Apps Script chỉ còn bridge cho hồ sơ công khai và báo cáo sĩ số |
| C121 | ⛔ Bị chặn (cần xác nhận) | 23/07/2026 | Export/import dữ liệu thật sang Supabase: enum sạch, 36 học sinh và 29 ghi nhận đã nhập; còn lệch DanhMucXuLy do 3 dòng trùng unique và chưa kiểm UI đăng nhập thật |
| C122 | ✅ Xong | 23/07/2026 | Bỏ unique sai trên DanhMucXuLy.noi_dung_xu_ly và import đủ 7/7 mã xử lý; NN09 vẫn tham chiếu XL05 |
| C123 | ✅ Xong | 23/07/2026 | Chuyển hồ sơ học sinh public sang RPC Supabase SECURITY DEFINER theo token, không mở select anon trực tiếp |
| C124 | ✅ Xong | 23/07/2026 | Siết RPC hồ sơ public: bỏ to_jsonb, liệt kê rõ cột public cho records/catalog/ban_can_su/week_config |
| C125 | ✅ Xong | 23/07/2026 | Ghi nhận cách sinh token hồ sơ public hiện tại và khuyến nghị nâng độ dài token ở bước sau |
| C126 | ✅ Xong | 23/07/2026 | Cập nhật GitHub Pages workflow để truyền VITE_SUPABASE_URL và VITE_SUPABASE_ANON_KEY vào bước build |
| C127 | ✅ Xong | 23/07/2026 | Tạo schema điểm danh Supabase, seed nhóm điểm danh và chuyển báo cáo sĩ số chính khóa sang RPC tinh_bao_cao_si_so |
| C128 | ✅ Xong | 23/07/2026 | Import CSV điểm danh thật 4 tuần: 22 ngoại lệ vào diem_danh và 22 dòng liên lạc phụ huynh; RPC sĩ số 09/07/2026 buổi sáng trả đúng danh sách vắng |
| C129 | ⛔ Bị chặn (cần xác nhận) | 23/07/2026 | Chưa tự bấm UI trang Sĩ số bằng trình duyệt tự động vì .env không có tài khoản/session giáo viên Supabase; đã kiểm DB/RPC là nguồn dữ liệu frontend sẽ gọi |
| C130 | ✅ Xong | 24/07/2026 | Tạo trang Điểm danh giáo viên chính khóa: số liệu ngày/tuần/tháng, lưới tuần, sửa vắng/trễ qua RPC upsert_diem_danh và ghi liên lạc phụ huynh |
| C131 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Chưa tự bấm UI Điểm danh bằng trình duyệt đăng nhập thật vì .env không có tài khoản/session giáo viên Supabase; đã kiểm build, test và DB rollback |
| C132 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Chuyển tạo link Google Form điểm danh từ Apps Script sang RPC Supabase `tao_link_form_diem_danh`; app không còn phụ thuộc Apps Script cho bất kỳ thao tác nào. Cần giáo viên tự INSERT dòng cấu hình thật (form_base_url, form_password, entries) vào bảng `cau_hinh_form_diem_danh` vì chỉ họ có các giá trị bí mật đang lưu trong Apps Script Script Properties |
| C133 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Rà lại giao diện Điểm danh/Sĩ số trên điện thoại: modal sửa điểm danh tự cuộn, thu hẹp cột lưới tuần, sửa khối Nội trú/Bán trú/Hai buổi bị chèn chữ; sửa lỗi Safari iOS chặn tab Google Form vì window.open() gọi sau await. Không có trình duyệt/điện thoại thật để tự bấm kiểm — cần giáo viên xác nhận trên điện thoại thật (đặc biệt Safari iOS) trước khi đánh dấu Xong |
| C134 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Sửa mất lịch sử liên lạc phụ huynh khi sửa lại điểm danh: `lien_lac_phu_huynh.diem_danh_id` đổi từ `on delete cascade` sang `on delete set null`, thêm cột chụp nhanh `ma_hs`/`ho_ten`/`ngay`/`buoi` ngay lúc ghi để lịch sử không mất khi dòng `diem_danh` gốc bị xoá lúc chuyển về Có mặt. Migration đã apply thành công (giáo viên xác nhận), nhưng chưa tự bấm UI thật: đánh dấu vắng + ghi liên lạc, chuyển lại Có mặt, kiểm tra dòng `lien_lac_phu_huynh` vẫn còn với `ma_hs`/`ngay`/`buoi` đúng |
| C135 | ✅ Xong | 24/07/2026 | Sửa trạng thái C133/C134 từ "Xong" về đúng "Bị chặn (cần xác nhận)" theo mục 6 của 05-quy-tac-ai-agent.md — chưa ai tự bấm UI thật trên điện thoại/trình duyệt để xác nhận |
| C136 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Nội dung liên lạc phụ huynh trước đây chỉ ghi một chiều, không có nơi xem lại. Thêm trang riêng "Lịch sử liên lạc" (`/lien-lac-phu-huynh`), lọc theo tên/mã học sinh và khoảng ngày, đọc từ `lien_lac_phu_huynh` (không cần migration mới, RLS đã cho `authenticated` SELECT sẵn từ C127). Chưa tự bấm UI thật để xác nhận danh sách hiển thị đúng dữ liệu thật |
| C137 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Trang cá nhân học sinh (chế độ giáo viên, `/quan-ly/hoc-sinh/:maHs`) chưa lưu/hiện thông tin liên lạc phụ huynh dù đây là nơi tổng hợp mọi thông tin của học sinh. Thêm khối "Liên lạc phụ huynh" hiện lịch sử đã liên lạc của đúng học sinh đó, kèm link sang trang Lịch sử liên lạc lọc sẵn theo mã học sinh. Cố ý KHÔNG thêm vào trang hồ sơ public `/hs/:token` vì đây là ghi chú nội bộ giáo viên, tương tự lý do SĐT phụ huynh không hiện ở trang public (tài liệu 01 mục 6). Chưa tự bấm UI thật để xác nhận |
| C138 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Rà toàn app (Dashboard, Ghi nhận, Điểm danh, Lịch sử liên lạc, Danh sách học sinh) tìm chỗ tên/mã học sinh hoặc mã danh mục hiển thị dạng chữ thường thay vì link điều hướng. Gắn `Link` sang `/quan-ly/hoc-sinh/:maHs` hoặc `/danh-muc?ma=...` cho các chỗ tìm thấy: bảng điểm thi đua, sự kiện tổ trực/tập thể, nhật ký theo ngày, Tổ trưởng trong TeamInfoPanel (Dashboard); badge danh mục trong bảng ghi nhận (RecordEntryPage); cột Mã HS (StudentsPage); lưới tuần, danh sách ngoại lệ Sáng/Chiều, danh sách "Cần liên lạc phụ huynh" (AttendanceManagementPage); tên học sinh trong Lịch sử liên lạc. Cố ý KHÔNG đổi: 2 tiêu đề modal sửa điểm danh (đang sửa đúng học sinh đó, link sẽ mất dữ liệu form dở dang); trang hồ sơ public `/hs/:token` (route giáo viên không được lộ ra public); các nút chọn danh mục dạng picker (bấm để chọn cho form, không phải hiển thị dữ liệu đã có) |
| C139 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Trang Lịch sử liên lạc (C136) chỉ mới xem/thêm, chưa sửa/xoá được nội dung đã ghi. Thêm `updateParentContact`/`deleteParentContact` vào `DataSource`/`SupabaseDataSource` (không cần migration mới, RLS `authenticated` đã cho UPDATE/DELETE từ C127); `ParentContactHistoryPage.tsx` thêm nút Sửa (mở form sửa hình thức + nội dung tại chỗ) và Xoá (có xác nhận) cho từng dòng. Chưa tự bấm UI thật để xác nhận sửa/xoá hoạt động đúng trên dữ liệu thật |
| C140 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Giáo viên đánh dấu vắng nhưng dễ quên đã gọi phụ huynh hay chưa, phải cuộn xuống khối "Cần liên lạc phụ huynh" mới biết. Thêm badge "Chưa gọi PH" (màu cam) ngay tại chỗ hiển thị bản ghi vắng/trễ: danh sách ngoại lệ Sáng/Chiều ở tab Ngày, và ô trong lưới tuần. Dùng lại đúng tập id từ `getPendingParentContacts()` (đã loại các bản ghi đã có `lien_lac_phu_huynh`, chỉ áp dụng `vang_co_phep`/`vang_khong_phep` theo đúng quy tắc cũ, không áp dụng cho `trễ`) — không cần query Supabase thêm. Chưa tự bấm UI thật để xác nhận badge hiện đúng vị trí và biến mất sau khi ghi liên lạc |
| C141 | ⛔ Bị chặn (cần xác nhận) | 24/07/2026 | Lưới tuần chỉ có bảng ma trận học sinh × ngày, không có cách xoá nhanh 1 lượt điểm danh nhầm, và trang thiếu 3 số liệu tổng quan Sĩ số/Hiện diện/Vắng. Theo lựa chọn của người dùng: (1) thêm bên dưới lưới tuần danh sách chi tiết **từng ngày** trong tuần, mỗi ngày 2 khối Sáng/Chiều (dùng lại `CompactDayAttendance`, lọc theo `weekEntries`); (2) thêm nút "Xoá" cạnh "Sửa" trong `SessionBlock` — xác nhận rồi chuyển thẳng về Có mặt, không cần mở modal; (3) thêm 3 thẻ **Sĩ số/Hiện diện/Vắng** tính theo **số học sinh duy nhất** (Vắng = học sinh có ≥1 lượt vắng có phép/không phép trong khoảng đang xem; Trễ không tính là vắng; Hiện diện = Sĩ số − Vắng), áp dụng chung cho cả Ngày/Tuần/Tháng vì dùng lại đúng `summaryEntries` đã có sẵn theo từng scope. Chưa tự bấm UI thật để xác nhận |
| C142 | ✅ Xong | 24/07/2026 | Thiết kế lại toàn bộ trang Điểm danh theo yêu cầu người dùng, đã hỏi lại 2 câu chốt cấu trúc trước khi code: gộp tab Ngày + Tuần (C133/C141) thành **1 công cụ điểm danh duy nhất** (chọn ngày bất kỳ bằng input ngày + dropdown Sáng/Chiều), bỏ bảng ma trận và danh sách theo từng ngày; tab Tháng giữ riêng để xem tổng quan. Danh sách học sinh giờ là **thẻ** cho từng học sinh (toàn bộ lớp, không chỉ ngoại lệ), mỗi thẻ hiện nhanh trạng thái hiện tại, số lần vắng trong tuần/tháng của đúng học sinh đó, và badge "Chưa gọi PH" nếu chưa liên lạc; đổi trạng thái ngay trên thẻ (không cần modal). **Bỏ "Trễ" khỏi lựa chọn điểm danh** (chỉ còn Có mặt/Vắng có phép/Vắng không phép) vì Trễ thuộc danh mục vi phạm ở trang Ghi nhận — bản ghi Trễ cũ vẫn hiển thị đúng ở tab Tháng vì `TrangThaiDiemDanh`/DB không đổi, chỉ ẩn khỏi các lựa chọn ghi mới. Tab Tháng liệt kê toàn bộ ngoại lệ trong tháng, mỗi dòng có nút "Sửa ở Điểm danh" nhảy thẳng sang đúng ngày/buổi đó. Xoá `EditAttendanceModal`, `CompactDayAttendance`, `SessionBlock`, `SessionBadge`, bảng ma trận tuần vì không còn cần. Chưa tự bấm UI thật để xác nhận |
| C143 | ✅ Xong | 24/07/2026 | Ngay sau C142, người dùng thấy danh sách thẻ toàn bộ 36 học sinh vẫn cồng kềnh — yêu cầu gộp lại 1 vùng: dropdown Sáng/Chiều (đã có), dropdown chọn học sinh, dropdown chọn trạng thái vắng, rồi hiện kết quả là danh sách học sinh đã điểm danh kèm trạng thái. Bỏ hẳn `StudentAttendanceCard` cho toàn bộ lớp, quay lại đúng kiểu form "Thêm nhanh" (chọn học sinh + chọn trạng thái + ô liên lạc phụ huynh khi cần) nhưng gộp về 1 vùng duy nhất thay vì 2 khối Sáng/Chiều song song như bản cũ trước C142. Bên dưới form là "Danh sách đã điểm danh" chỉ liệt kê học sinh đang có ngoại lệ cho đúng buổi đang chọn, giữ nguyên các badge Tuần vắng/Tháng vắng/Chưa gọi PH và nút Xoá nhanh. Chưa tự bấm UI thật để xác nhận |
| C144 | ✅ Xong | 24/07/2026 | "Danh sách đã điểm danh" chỉ có nút Xoá (chuyển thẳng về Có mặt), không có cách đổi sang trạng thái vắng khác hoặc sửa nội dung liên lạc phụ huynh mà không xoá rồi ghi lại từ đầu. Thêm nút "Sửa" cạnh "Xoá", mở modal `EditEntryModal` (dropdown trạng thái + ô liên lạc phụ huynh khi chọn vắng) cho đúng học sinh/buổi/ngày đang chọn, Lưu gọi lại `saveAttendance` rồi tự đóng modal. Người dùng đã tự bấm UI thật và xác nhận ổn |
| C145 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Người dùng test điểm danh Thứ Bảy 25/07/2026, tính Sĩ số không ra đúng. Nguyên nhân: `CauHinhTuan` chỉ cấu hình 5 ngày Thứ Hai-Thứ Sáu (`so_ngay: 5`), Thứ Bảy/Chủ Nhật không nằm trong `tu_ngay..den_ngay` của tuần nào; `activeWeek` cũ fallback `\|\| weeks[0]` khi không khớp, tức lấy nhầm tuần đầu tiên từng cấu hình (Tuần 1) thay vì tuần hiện tại, làm sai `tuan_so` lúc lưu và sai toàn bộ số liệu Sĩ số/Hiện diện/Vắng/Tuần vắng dựa trên đó. | Thêm `resolveWeekForDate`: nếu không có tuần chứa đúng ngày, lấy tuần có `tu_ngay` lớn nhất mà vẫn `<= ngày đang chọn` (tuần Thứ Hai-Thứ Sáu gần nhất đã bắt đầu), chỉ dùng tuần đầu tiên nếu ngày đang chọn còn trước cả tuần cấu hình sớm nhất. **Lưu ý**: bản ghi Thứ Bảy 25/07 test trước khi sửa có thể đã lưu nhầm `tuan_so` của Tuần 1 — cần bạn tự kiểm tra lại trong "Danh sách đã điểm danh" hoặc Supabase, xoá/sửa lại nếu còn sai. Người dùng đã xác nhận đúng hiện tượng "ngày 25/07 ghi Tuần 1" trên UI thật, khớp chẩn đoán |
| C146 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Khối "Cần liên lạc phụ huynh" chỉ có nút "Ghi nhận" — nếu học sinh bị đánh dấu vắng nhầm (không cần liên lạc phụ huynh) thì phải nhảy sang Điểm danh mới xoá được, không xoá thẳng tại đây. Thêm nút "Xoá" cạnh "Ghi nhận": `window.confirm` rồi gọi thẳng `upsertAttendanceEntry` với `trang_thai: 'co_mat'` dùng đúng `ma_hs`/`ngay`/`tuan_so`/`buoi` sẵn có trên `DiemDanhCanLienLac` (không cần tính lại `activeWeek`), refresh danh sách sau khi xoá. Chưa tự bấm UI thật để xác nhận |
| C147 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Trang Ghi nhận: mục "1. Chọn nội dung" hiện lưới thẻ toàn bộ danh mục cá nhân (nhiều hàng, chiếm rất nhiều chỗ), mục "2. Chọn cách gắn lên học sinh" cũng là 3 nút thẻ song song — người dùng yêu cầu chuyển sang dropdown cho gọn. | Mục 1: bỏ lưới `button` thẻ, thay bằng 1 `<select>` liệt kê `visibleCatalog` (vẫn lọc được qua ô tìm + dropdown Tất cả/Tích cực/Vi phạm/Theo dõi có sẵn); bên dưới hiện 1 khối xem trước chi tiết (badge mã, điểm, tên mục, nhóm/phạm vi/nghiêm trọng) chỉ cho đúng mục đang chọn thay vì cả danh sách. Mục 2: bỏ 3 `ModeButton`, thay bằng 1 `<select>` 3 lựa chọn (Chọn từng học sinh/Theo tổ/Cả lớp), mỗi lựa chọn có số đếm ngay trong nhãn; thêm `teamStudentCount` tính riêng theo tổ đang chọn để không phụ thuộc `targetStudents` (vốn chỉ đúng khi tổ đang là chế độ active). Xoá hẳn component `ModeButton` không còn dùng. Không đổi phần chọn học sinh theo checkbox (đa lựa chọn, dropdown đơn không thay được) và dropdown chọn tổ (đã là dropdown từ trước). Chưa tự bấm UI thật để xác nhận |
| C148 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Thêm phân quyền cho học sinh giữ chức vụ "Lớp trưởng" tự gửi đề xuất ghi nhận (vi phạm hoặc tích cực) cho bạn cùng lớp qua link hồ sơ công khai `/hs/:token`, giáo viên duyệt trước khi tính điểm. Đã hỏi rõ 3 quyết định nghiệp vụ trước khi code: (1) bắt buộc giáo viên duyệt, không tính điểm ngay; (2) lớp trưởng được đề xuất cả vi phạm lẫn tích cực (không giới hạn chỉ vi phạm); (3) xác thực bằng mã PIN riêng do giáo viên cấp, ngoài token của link. | **Migration** `20260725000100_lop_truong_de_xuat_ghi_nhan.sql`: thêm cột `ban_can_su.ma_pin` (không grant SELECT cho anon/authenticated, chỉ đọc được trong function security definer); bảng `de_xuat_ghi_nhan` (trang_thai cho_duyet/da_duyet/tu_choi, RLS `authenticated` full quyền); 3 RPC — `xac_thuc_pin_lop_truong`, `lay_du_lieu_lop_truong` (trả roster + danh mục cá nhân nếu PIN đúng, ngược lại 0 dòng, không lộ dữ liệu), `gui_de_xuat_ghi_nhan` (ghi đề xuất, raise exception nếu PIN sai). **DataSource**: thêm `addBanCanSu`/`updateBanCanSu`/`deleteBanCanSu` (trước đây chỉ đọc được, không có UI gán chức vụ), `verifyLopTruongPin`, `getLopTruongData`, `submitDeXuatGhiNhan`, `getDeXuatGhiNhan`, `approveDeXuatGhiNhan` (tạo `ghi_nhan` thật qua `addRecords`, gắn `ma_ghi_nhan` vào đề xuất), `rejectDeXuatGhiNhan`. **`TeacherStudentDetailPage.tsx`**: khối "Ban cán sự" mới — dropdown chọn chức vụ, ô Tổ/Ngày bắt đầu, ô Mã PIN chỉ hiện khi chọn "Lớp trưởng". **`StudentProfilePage.tsx`**: nếu `role === 'Lớp trưởng'`, hiện khối "Nhập đề xuất ghi nhận cho lớp" — nhập PIN trước, đúng thì mở form chọn học sinh + danh mục + nội dung, gửi qua RPC. **`RecordEntryPage.tsx`**: thêm khối "Đề xuất ghi nhận từ lớp trưởng" liệt kê các đề xuất `cho_duyet`, nút Duyệt/Từ chối. Chưa tự bấm UI thật để xác nhận; migration chưa được apply lên Supabase |
| C149 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Sau C148, người dùng yêu cầu thêm: nếu danh mục hệ thống không có mục nào khớp, lớp trưởng được đề xuất danh mục mới (không bắt buộc chọn từ danh sách có sẵn); giáo viên khi duyệt phải sửa được nội dung cho chuẩn, hoặc gắn vào danh mục có sẵn gần đúng, hoặc tạo hẳn danh mục mới ngay tại chỗ trước khi tính điểm. | **Migration** `20260725000200_de_xuat_danh_muc_moi_tu_lop_truong.sql`: `de_xuat_ghi_nhan.ma_danh_muc` đổi thành nullable, thêm cột `de_xuat_nhom` (CC/VS/NN/KL/KT, chỉ có giá trị khi `ma_danh_muc` là null); RPC `gui_de_xuat_ghi_nhan` thêm tham số `p_de_xuat_nhom`, chấp nhận `ma_danh_muc` null nếu có `de_xuat_nhom` + nội dung mô tả. **`StudentProfilePage.tsx`**: dropdown danh mục thêm lựa chọn "➕ Không có, đề xuất danh mục mới" — chọn xong hiện thêm dropdown Nhóm, nội dung trở thành bắt buộc (mô tả cho giáo viên hiểu đề xuất là gì). **`SupabaseDataSource.approveDeXuatGhiNhan`** đổi sang nhận `overrides: { maDanhMuc?, noiDung? }` — ghi `ghi_nhan` theo giá trị cuối cùng do giáo viên chọn/sửa (không còn bắt buộc trùng khớp dữ liệu gốc học sinh gửi), đồng thời cập nhật lại `ma_danh_muc`/`noi_dung` vào chính dòng `de_xuat_ghi_nhan` để lưu đúng những gì đã duyệt. **`RecordEntryPage.tsx`**: mỗi đề xuất giờ là `ProposalReviewCard` — dropdown chọn danh mục có sẵn (sửa được kể cả khi đề xuất gốc đã có mã), nút "Tạo danh mục mới từ đề xuất này" mở mini-form (mã, nhóm, tên, điểm, nghiêm trọng) gọi `addPointCatalogItem` rồi tự chọn luôn mã mới tạo, ô nội dung sửa được trước khi Duyệt. Danh mục vừa tạo được thêm ngay vào `state.catalog` qua callback `onCatalogCreated` để các đề xuất khác trong cùng phiên cũng thấy mã mới đó. Chưa tự bấm UI thật để xác nhận |
| C150 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | Thêm 3 việc theo yêu cầu: (1) lịch sử đề xuất của chính lớp trưởng, sửa/xoá được khi còn `cho_duyet`; (2) dropdown chọn ngày xảy ra vì lớp trưởng có thể nhập bù vào cuối tuần (Thứ Bảy/Chủ Nhật không thuộc tuần cấu hình nào); (3) nút thông báo cho giáo viên, đếm số đề xuất chờ duyệt trên biểu tượng. | **Migration** `20260725000300_lich_su_de_xuat_va_ngay.sql`: thêm cột `de_xuat_ghi_nhan.ngay` (mặc định `current_date`); `gui_de_xuat_ghi_nhan` thêm tham số `p_ngay`; RPC mới `lay_lich_su_de_xuat_lop_truong` (trả toàn bộ đề xuất do đúng lớp trưởng đó gửi, mọi trạng thái), `sua_de_xuat_ghi_nhan` và `xoa_de_xuat_ghi_nhan` (chỉ cho phép khi `ma_hs_de_xuat` khớp đúng lớp trưởng xác thực qua token+PIN và `trang_thai = cho_duyet`). `SupabaseDataSource.approveDeXuatGhiNhan` dùng `proposal.ngay` (ngày lớp trưởng chọn) thay vì luôn lấy ngày hôm nay khi tạo `ghi_nhan` thật. **`StudentProfilePage.tsx`**: form gửi đề xuất thêm input ngày (`max` = hôm nay); thêm khối "Lịch sử đề xuất của tôi" liệt kê mọi đề xuất đã gửi kèm trạng thái (Chờ duyệt/Đã duyệt/Bị từ chối), đề xuất `cho_duyet` có nút Sửa (mở lại đúng các trường đã gửi để sửa) và Xoá (có `window.confirm`). **`Layout.tsx`**: thêm nút "🔔 Thông báo" trỏ sang `/ghi-nhan`, đếm số đề xuất `cho_duyet` qua `getDeXuatGhiNhan()`, refetch mỗi khi đổi trang để cập nhật badge sau khi giáo viên duyệt/từ chối. Chưa tự bấm UI thật để xác nhận |
| C151 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | 3 việc theo yêu cầu: (1) trang Ghi nhận không tự chọn sẵn học sinh đầu tiên nữa; (2) Tổng quan giáo viên xoá được ghi nhận vi phạm/tích cực trực tiếp, không phải mở hồ sơ học sinh; (3) nút Thông báo bấm vào đọc được danh sách ngay (dropdown), bấm vào từng đề xuất thì điều hướng đến hồ sơ học sinh liên quan, có nút Duyệt/Từ chối nhanh ngay trong dropdown. | **`RecordEntryPage.tsx`**: bỏ dòng `setSelectedStudentIds(students[0] ? [students[0].ma_hs] : [])`, giữ nguyên `useState<string[]>([])` mặc định rỗng. **`DashboardPage.tsx`**: thêm `deleteRecord`/`deletingRecordId` theo đúng pattern đã dùng ở `TeacherStudentDetailPage.tsx` (`window.confirm` → `dataSource.deleteRecord` → lọc khỏi `state.records`); gắn nút "Xoá" vào 2 chỗ hiện ghi nhận cá nhân có `ma_ghi_nhan`: khối "Nhật ký theo ngày" và bảng "Xem theo Nhóm vi phạm" (`GroupViolationView` nhận thêm prop `onDeleteRecord`/`deletingRecordId`). Không đổi khối "Sự kiện của lớp/tổ" vì đó là luồng xử lý sự kiện tập thể riêng, xoá thẳng không phù hợp. **`Layout.tsx`**: đổi nút Thông báo từ `Link` sang `button` mở dropdown tại chỗ, tải kèm `getStudents()`/`getPointCatalog()` để hiện tên học sinh + tên danh mục ngay trong danh sách; mỗi dòng bấm vào điều hướng sang `/quan-ly/hoc-sinh/:ma_hs` (hoặc `/ghi-nhan` nếu là đề xuất danh mục mới, vì cần vào đó mới xử lý được), có nút Duyệt/Từ chối ngay trong dropdown cho đề xuất đã có `ma_danh_muc` sẵn (gọi thẳng `approveDeXuatGhiNhan`/`rejectDeXuatGhiNhan`, không cần override vì đã có danh mục). Chưa tự bấm UI thật để xác nhận |
| C152 | ⛔ Bị chặn (cần xác nhận) | 25/07/2026 | 3 việc theo yêu cầu: (1) form đề xuất của lớp trưởng thêm Tiết/Môn (không bắt buộc), người ghi vẫn tự động theo chức vụ, không cần chọn; (2) `nguoi_ghi` khi duyệt đề xuất đọc đúng `chuc_vu` thật của lớp trưởng trong `ban_can_su` thay vì hardcode chữ "Lớp trưởng"; (3) chế độ giáo viên xem hồ sơ học sinh thấy được lịch sử đề xuất liên quan đến học sinh đó, sửa/xoá trực tiếp không cần PIN. | **Migration** `20260725000400_them_tiet_mon_de_xuat_ghi_nhan.sql`: thêm cột `de_xuat_ghi_nhan.tiet`, `mon_hoc`; `gui_de_xuat_ghi_nhan`/`sua_de_xuat_ghi_nhan` thêm tham số `p_tiet`/`p_mon_hoc`. **`SupabaseDataSource.approveDeXuatGhiNhan`**: tra `ban_can_su` theo `ma_hs_de_xuat` để lấy `chuc_vu` thật ghép vào `nguoi_ghi` (fallback "Lớp trưởng" nếu không tìm thấy), truyền `proposal.tiet`/`proposal.mon_hoc` vào `ghi_nhan` tạo mới thay vì luôn `null`. `getDeXuatGhiNhan` nhận thêm `options.maHs` để lọc theo học sinh. Thêm `updateDeXuatGhiNhanByTeacher`/`deleteDeXuatGhiNhanByTeacher` (gọi thẳng bảng `de_xuat_ghi_nhan` vì RLS đã cho `authenticated` full quyền, không cần RPC/PIN). **`StudentProfilePage.tsx`**: form gửi đề xuất và form sửa trong lịch sử đều thêm 2 ô Tiết/Môn (input thường, không bắt buộc), thêm dòng chú thích "Người ghi sẽ tự động ghi theo chức vụ". **`TeacherStudentDetailPage.tsx`**: thêm khối "Lịch sử đề xuất ghi nhận cho học sinh này" (load qua `getDeXuatGhiNhan({ maHs })`), mỗi dòng có nút Sửa (danh mục/nhóm mới/nội dung/ngày/tiết/môn)/Xoá dùng thẳng API giáo viên, không giới hạn theo trạng thái (có ghi chú khi đề xuất đã xử lý). Chưa tự bấm UI thật để xác nhận |
| C153 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo layout vỡ lệch trên di động ở tab Ghi nhận/Điểm danh/trang HS; hỏi lại thì chỉ rõ cụ thể: tab Sĩ số và Liên lạc PH bị tràn email ra ngoài khung. | Nguyên nhân: `nguoi_lien_lac` lưu email đăng nhập Supabase của giáo viên (chuỗi dài không có khoảng trắng) và được render trong `<p>` không cho phép ngắt dòng, tràn ngang trên màn hình hẹp. **`ParentContactHistoryPage.tsx`**: đổi `shrink-0` thành `min-w-0` trên div bọc, thêm `min-w-0 wrap-break-word` vào `<p>` hiện `nguoi_lien_lac`. **`TeacherStudentDetailPage.tsx`**: thêm `min-w-0 wrap-break-word` vào cả 2 `<p>` trong khối "Liên lạc phụ huynh" (ngày/buổi/hình thức và thời gian/người liên lạc) để chúng co giãn đúng trong `sm:flex-row`. Chưa xác nhận được cụ thể phần Ghi nhận/trang HS còn lỗi gì khác — đang chờ người dùng mô tả/gửi ảnh thêm nếu vẫn còn vỡ lệch sau bản này. |
| C154 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu sắp xếp lại nội dung trong khối `rounded-md border border-slate-200 p-3` (thẻ lượt liên lạc phụ huynh) theo dòng để tối ưu cho di động, thay vì chia 2 cột `sm:flex-row` (dễ chật vì `--breakpoint-sm` = 375px kích hoạt gần như mọi điện thoại). | **`ParentContactHistoryPage.tsx`**: bỏ layout `flex flex-col gap-1 sm:flex-row sm:items-start sm:justify-between` (tên/ngày một bên, thời gian/người liên lạc một bên), thay bằng `space-y-1` xếp dọc 3 dòng riêng (tên, ngày·buổi·hình thức, thời gian·người liên lạc); nút Sửa/Xoá chuyển xuống hàng riêng bên dưới (`mt-2 flex gap-2`, mỗi nút `flex-1 sm:flex-none` để chiếm đều chiều ngang trên điện thoại, không còn ép chung hàng với dòng text dài). **`TeacherStudentDetailPage.tsx`**: áp dụng đúng cách xếp dọc tương tự cho khối "Liên lạc phụ huynh" (không có nút nên chỉ bỏ `sm:flex-row`, dùng `space-y-1`). |
| C155 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu dịch khối nút `mt-2 flex gap-2` (Sửa/Xoá) trong thẻ liên lạc phụ huynh xuống dưới phần nội dung, thay vì nằm ngay trên nó. | **`ParentContactHistoryPage.tsx`**: chuyển khối nút Sửa/Xoá vào bên trong nhánh `else` của `editingId === item.id`, đặt sau `<p>{item.noi_dung}</p>` (bọc chung bằng fragment `<>...</>`) — khi đang sửa vẫn chỉ hiện form sửa như cũ, không hiện nút Sửa/Xoá; khi không sửa thì thứ tự hiển thị giờ là: thông tin (tên/ngày/thời gian) → nội dung → nút Sửa/Xoá. |
| C156 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu menu tab ở header ghim (sticky) khi cuộn xuống, và trên di động ẩn danh sách tab vào nút "..." — bấm vào mới hiện ra, để tối ưu không gian màn hình nhỏ; sau đó góp ý thêm rằng cả khối nút Thông báo/Tải mẫu phiếu/Đăng xuất/email xếp chồng dọc luôn hiện ra trên di động nhìn thô, cần gọn lại theo chuẩn mobile. | **`Layout.tsx`**: `<header>` thêm `sticky top-0 z-40` để dính lại khi cuộn. Tách phần render danh sách tab ra component `NavLinkList` (nhận `orientation: 'row' | 'col'` để tái dùng cho cả 2 layout) tránh lặp code. Đổi hẳn cấu trúc header từ 2 hàng `flex-col md:flex-row` sang 1 thanh app-bar `items-center justify-between` duy nhất: bên trái là brand, bên phải là 1 hàng ngang gồm nút Thông báo (luôn hiện, chữ "Thông báo" ẩn dưới `sm:` chỉ còn icon 🔔 cho gọn), rồi đến `nav`/`Tải mẫu phiếu`/`Đăng xuất`/email chỉ hiện từ `md:` (`hidden md:flex`), và cuối cùng nút toggle `⋯ {currentNavLabel}` chỉ hiện dưới `md:hidden`. Bấm nút `⋯` mở 1 dropdown tuyệt đối duy nhất (theo đúng pattern dropdown Thông báo có sẵn) gộp cả tab điều hướng (`NavLinkList orientation="col"`) lẫn 2 nút Tải mẫu phiếu/Đăng xuất và dòng email — tất cả gọn vào 1 nút thay vì xếp chồng 4-5 khối riêng biệt luôn hiện như trước; `navOpen` tự đóng khi đổi `pathname` hoặc sau khi bấm 1 mục trong menu. Từ `md:` trở lên hành vi giữ nguyên như cũ (mọi thứ hiện ngang, không có nút `⋯`). |
| C157 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Sau C156, người dùng báo dropdown Thông báo (`w-80`) bị lệch khỏi màn hình trên di động. | Nguyên nhân: dropdown dùng `absolute right-0` neo theo `div` bọc riêng nút 🔔 — trước C156 div đó chiếm trọn bề ngang container (`flex-col` xếp dọc) nên `right-0` trùng đúng mép phải màn hình; sau khi gộp 🔔 vào chung 1 hàng ngang với nút `⋯`, div bọc 🔔 chỉ còn rộng bằng chính nút đó và không còn là phần tử phải-cùng-hàng, khiến điểm neo lệch vào giữa màn hình rồi dropdown 320px tràn ra ngoài. **`Layout.tsx`**: đổi dropdown Thông báo sang `fixed inset-x-3 top-16 max-h-[70vh]` trên di động (bám theo viewport, không phụ thuộc vị trí neo) và chỉ quay lại `md:absolute md:right-0 md:top-12 md:w-80 md:max-h-96` từ `md:` (768px) trở lên — dùng `md:` chứ không phải `sm:` vì dự án đã đặt `--breakpoint-sm` = 375px (kích hoạt trên hầu hết điện thoại thật), chỉ `md:` mới đảm bảo đây là màn hình đủ rộng để neo tuyệt đối an toàn. Dropdown menu `⋯` (`w-56`) không đổi vì nó vẫn là phần tử cuối cùng bên phải hàng nên neo đúng mép màn hình. |
| C158 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng yêu cầu 2 dropdown (Thông báo và menu `⋯`) tự đóng lại khi bấm/thao tác ra chỗ khác, thay vì phải bấm lại đúng nút để đóng. | **`Layout.tsx`**: thêm `notifRef`/`navRef` (`useRef<HTMLDivElement>`) gắn vào đúng 2 `div` bọc từng dropdown; thêm 1 `useEffect` lắng nghe `pointerdown` trên `document` (chỉ đăng ký khi có ít nhất 1 trong 2 đang mở, gỡ khi cả 2 đóng để tránh listener thừa) — nếu điểm bấm nằm ngoài `notifRef`/`navRef` tương ứng thì tự `setNotifOpen(false)`/`setNavOpen(false)`. Dùng `pointerdown` thay vì `click` để đóng sớm hơn (trước khi phần tử bên dưới nhận sự kiện click, tránh trường hợp bấm xuyên qua dropdown vào nút khác). Không ảnh hưởng các nút Sửa/Xoá/Duyệt/Từ chối bên trong dropdown vì chúng nằm trong cùng `ref`, `contains(target)` vẫn đúng dù dropdown Thông báo dùng `position: fixed` (fixed chỉ đổi vị trí hiển thị, không đổi quan hệ cha-con trong DOM). |
| C159 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo dropdown Thông báo vẫn tràn trên giao diện desktop (C157 chỉ mới sửa cho di động). | Nguyên nhân giống hệt C157 nhưng xảy ra ở nhánh `md:` trở lên: `div` bọc riêng nút 🔔 vẫn không phải phần tử cuối/phải-nhất của hàng trên desktop (sau nó còn `nav` ngang + khối Tải mẫu phiếu/Đăng xuất/email), nên `right-0` neo theo div đó vẫn lệch sang trái so với mép phải thật, khiến dropdown 320px tràn/che lên nav và các nút phía sau thay vì nằm gọn sát mép phải. **`Layout.tsx`**: sửa tận gốc thay vì vá theo từng breakpoint — chuyển `relative` từ `div` bọc riêng nút 🔔 sang `div` bọc TOÀN BỘ cụm bên phải header (`flex items-center gap-2` → `relative flex items-center gap-2`), vì CSS `absolute` luôn neo theo tổ tiên `position` gần nhất bất kể có nằm lồng trong div trung gian không-positioned hay không — nhờ đó dropdown tự động neo đúng mép phải thật của hàng header trên MỌI kích thước màn hình, không cần tách `fixed`/`md:absolute` như C157 nữa. Bỏ hẳn kiểu `fixed inset-x-3` của C157, quay về `absolute right-0 top-12` đơn giản, chỉ thêm `max-w-[calc(100vw-1.5rem)]` làm lưới an toàn cho màn hình cực hẹp (dưới 320px). |
| C160 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thiết kế lại menu header giống Facebook: ẩn gọn, chỉ hiện khi bấm — áp dụng cho MỌI kích thước màn hình, không riêng di động (trước đó desktop vẫn luôn hiện đầy đủ thanh tab + nút Tải mẫu phiếu/Đăng xuất/email). | **`Layout.tsx`**: bỏ hẳn thanh tab ngang + khối nút luôn hiện trên desktop (từng có `hidden md:flex`); header giờ chỉ còn Brand + 2 nút icon tròn (`h-10 w-10 rounded-full`) không đổi theo breakpoint: 🔔 Thông báo (bỏ chữ "Thông báo", chỉ còn icon + `title` tooltip) và ☰ Menu (thay hẳn nút `⋯ {currentNavLabel}` cũ). Bấm ☰ mở 1 dropdown duy nhất (không còn tách bản mobile/desktop) gộp cả tab điều hướng, Tải mẫu phiếu, Đăng xuất, email — dùng chung ở mọi kích thước màn hình. Đơn giản hoá `NavLinkList` bỏ tham số `orientation` (chỉ còn 1 kiểu dọc) vì không còn nơi nào cần kiểu hàng ngang nữa. |
| C161 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn màu nền toàn bộ giao diện đậm lên (nền trang + card/section + header), vì bảng màu pastel hiện tại (`bg-*-50`) quá nhạt. | Bump cơ học toàn bộ `bg-{màu}-50` (slate/blue/indigo/cyan/emerald/amber/rose/orange/teal/violet/sky/red) → `bg-{màu}-100` bằng `sed -E` trên 12 file feature đang dùng màu pastel (khớp ranh giới từ `\b` để không đụng `bg-blue-500`...). Riêng nền canvas cấp trang (không phải card/section) đẩy thêm 1 nấc nữa lên `bg-slate-200`: `Layout.tsx` (wrapper chính `<div className="flex min-h-screen flex-col ...">` và `TeacherAuthShell`), `StudentProfilePage.tsx` (`<main>` trang hồ sơ công khai), `src/index.css` (`body`) — vì các trang xác thực (Dashboard, Ghi nhận, Điểm danh...) đều render qua `<Outlet>` bên trong wrapper này của `Layout.tsx` nên tự động ăn theo, không cần sửa từng trang. Header/footer đổi từ `bg-white` sang `bg-slate-100` (border `slate-200`→`slate-300`) để tạo phân lớp rõ: nội dung trắng < header/footer xám nhạt < nền trang xám đậm hơn. Không đụng tới 237 chỗ `bg-white` khác (bảng dữ liệu, modal, ô input...) vì đó là nội dung cần giữ nền sáng để đọc được, không phải phần "nền/canvas". |
| C162 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | 2 việc: (1) rà toàn bộ list trong app, giới hạn 5 dòng/trang, dài hơn thì phân trang; (2) đổi text footer từ "Trường THCS & THPT Lạc Hồng · Năm học 2025–2026" thành lời giới thiệu dạy lập trình của Thầy Nguyễn Duy Tân. | Thêm `src/components/Pagination.tsx`: hook `usePagination<T>(items, pageSize=5)` (tự reset về trang 1 khi `items.length` đổi, tự kẹp `page` không vượt `totalPages`) + component `Pagination` (nút Trước/Sau, ẩn hẳn nếu chỉ 1 trang). Áp dụng cho các list dữ liệu thật (không đụng dropdown option cố định nhỏ như `STATUS_OPTIONS`/`NHOM_OPTIONS`): **`Layout.tsx`** (danh sách đề xuất trong dropdown Thông báo), **`RecordEntryPage.tsx`** (đề xuất chờ duyệt, 2 `PreviewTable` xem trước/vừa tạo), **`AttendanceManagementPage.tsx`** (danh sách đã điểm danh, ngoại lệ trong tháng, cần liên lạc phụ huynh), **`ParentContactHistoryPage.tsx`** (lịch sử liên lạc đã lọc), **`TeacherStudentDetailPage.tsx`** (bảng ghi nhận, đề xuất lớp trưởng, lịch sử liên lạc PH), **`StudentsPage.tsx`** (bảng học sinh chính), **`CatalogPage.tsx`** (bảng danh mục, bảng ghi nhận theo danh mục trong modal), **`ImportPage.tsx`** (chỉ bảng log import — không đụng các bước wizard khớp dữ liệu vì phân trang sẽ che mất dòng cần xử lý), **`StudentProfilePage.tsx`** (lịch sử ghi nhận phân trang theo *nhóm tuần* thay vì từng dòng — giữ nguyên UX gộp theo tuần; lịch sử đề xuất của lớp trưởng). Chưa làm `DashboardPage.tsx` (nhiều list lồng nhau: bảng điểm tuần, nhật ký ngày, các modal drill-down, xem theo nhóm vi phạm) vì file quá lớn/phức tạp, cần một lượt riêng để không làm hỏng logic tính điểm — sẽ hỏi người dùng có muốn tiếp tục không. **`Layout.tsx`** footer: đổi text sang lời giới thiệu Thầy Nguyễn Duy Tân (sửa "chuyện" → "chuyên" cho đúng ngữ pháp, giữ nguyên phần còn lại). |

| C163 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo tab Tổng quan (Dashboard) chưa được phân trang ở C162; đồng thời yêu cầu bổ sung nút nhảy nhanh về trang 1 và trang cuối cho component `Pagination`. | **`Pagination.tsx`**: thêm 2 nút icon `«`/`»` (nhảy thẳng trang 1/trang cuối) hai bên nút "← Trước"/"Sau →"; hook `usePagination` trả thêm `pageSize`/`startIndex` để nơi gọi tự tính đúng số thứ tự (STT) theo trang thay vì luôn hiện lại 1-5 mỗi trang. **`DashboardPage.tsx`**: phân trang toàn bộ list còn thiếu — bảng điểm thi đua `sortedScores` (STT tính lại theo `scoresPage.startIndex`), "Sự kiện của lớp/tổ" `collectiveEvents`, "Nhật ký theo ngày" `dailyLogs`, bảng "Xem theo Nhóm vi phạm" `visibleRows` (trong `GroupViolationView`), và cả 4 nhánh của modal chi tiết (`OverviewDrillDownPanel`: attention/records/events/students — mỗi nhánh 1 hook `usePagination` riêng vì `drillDown.items` là union type theo `kind`, không thể narrow rồi mới gọi hook do luật hook phải gọi vô điều kiện; nhánh không khớp `kind` hiện tại nhận mảng rỗng nên `Pagination` tự ẩn, không xung đột). Cố tình không phân trang `TeamInfoPanel` (danh sách thành viên tổ — cỡ tổ cố định nhỏ; lịch sử sự kiện tổ trực đã tự giới hạn `slice(0, 5)` như một panel "gần đây" gọn, không phải list duyệt đầy đủ). |
| C164 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng mở file `tao csdl/huong-dan-pwa.md` (hướng dẫn tự soạn trước đó) và bảo đọc rồi làm: chuyển QLHS thành PWA (cài lên màn hình chính, cache để xem offline, tải nhanh hơn lần sau), không đổi backend/routing/nghiệp vụ. | Hướng dẫn gốc viết trước khi migrate khỏi Apps Script nên phần `runtimeCaching` mẫu trỏ vào `script.google.com` — đã lỗi thời từ C132 (backend giờ là Supabase thuần, xác nhận qua `src/lib/supabaseClient.ts`); đổi pattern sang `https://*.supabase.co/*`, thêm `method: 'GET'` để chỉ cache các lượt đọc PostgREST, không cache nhầm POST/PATCH/DELETE (ghi dữ liệu). Cài `vite-plugin-pwa`; cấu hình trong `vite.config.ts` với `start_url`/`scope` khớp `base: '/qlhs/'` sẵn có. Chưa có logo nên tạo tạm 4 icon PNG nền màu xanh `#1e40af` (khớp `theme_color`) bằng script Node viết tay (tự dựng PNG qua `zlib.deflateSync` + CRC32, không cần thư viện ảnh) — đặt trong `public/icons/` (thư mục `public/` cũng chưa từng có, tạo mới): `icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `apple-touch-icon.png`. Thêm 5 thẻ meta iOS vào `index.html` theo đúng hướng dẫn. Đã xác minh được: `npm run build` sinh đủ `manifest.webmanifest`, `sw.js`, `workbox-*.js`, `registerSW.js`; `npm run preview` phục vụ đúng `index.html`/`manifest.webmanifest`/`sw.js`/icon đều trả 200; `npm test` vẫn pass. **Chưa xác minh được** (cần người dùng tự làm, agent không có trình duyệt thật để kiểm tra): checklist DevTools Application (Manifest/Service Workers không lỗi), icon "Cài đặt" trên thanh địa chỉ, cài trên Android/iPhone thật, tắt mạng reload xem còn vào được không, và điểm Lighthouse PWA — đề nghị người dùng làm theo đúng checklist trong `huong-dan-pwa.md` sau khi deploy rồi phản hồi lại. Icon hiện chỉ là ô màu xanh đặc, chưa có logo/chữ "QLHS" — nên thay bằng icon thật khi có logo chính thức. |
| C165 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng đã chuẩn bị sẵn icon thật (nhân vật "Thầy Tân") trong `tao csdl/icon/` — 4 file đúng tên/kích thước cộng 1 file gốc `retro_master_1024.png`. | Copy 4 file `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` từ `tao csdl/icon/` đè lên bản placeholder (ô màu xanh đặc) trong `public/icons/` của C164 — đúng kích thước yêu cầu, không cần resize. `npm run build` xác nhận cả 4 icon được copy đúng vào `dist/icons/`, `npm test` vẫn pass. Chưa tự kiểm tra icon maskable có nằm gọn trong vùng an toàn 80% hay không (không có công cụ hình ảnh trong môi trường agent) — đề nghị người dùng tự soi lại bằng maskable.app/editor nếu thấy icon bị cắt góc khi cài trên Android thật. |
| C166 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm các tab dạng icon cố định dưới màn hình di động (kiểu bottom tab bar app thật), tự ẩn khi cuộn xuống và hiện lại khi cuộn lên. | **`Layout.tsx`**: thêm `icon` (emoji) vào từng phần tử `navItems`. Thêm state `bottomBarVisible` + `lastScrollY` (ref) và 1 `useEffect` lắng nghe sự kiện `scroll` trên `window` (passive) — cuộn xuống quá 8px thì ẩn, cuộn lên quá 8px thì hiện, riêng khi gần đầu trang (`scrollY < 24`) luôn hiện. Thêm `<nav>` cố định `fixed inset-x-0 bottom-0 md:hidden` liệt kê toàn bộ 8 tab dạng icon+nhãn ngắn, cuộn ngang được (`overflow-x-auto`) vì 8 tab không vừa 1 màn hình hẹp, tab đang active tô màu xanh; ẩn/hiện bằng `translate-y-full`/`translate-y-0` (transition mượt) thay vì gỡ khỏi DOM để tránh giật layout. `<main>` thêm `pb-20 md:pb-6` để nội dung cuối trang không bị thanh tab che khuất trên di động. Không đụng tới nút `☰ Menu` ở header (C160) — vẫn giữ nguyên vai trò menu đầy đủ + Tải mẫu phiếu/Đăng xuất/email, thanh tab dưới chỉ là lối tắt điều hướng nhanh, có trùng lặp nội dung với menu ☰ nhưng là chủ đích (mẫu UI phổ biến: bottom bar cho điều hướng nhanh, menu đầy đủ cho hành động phụ). |
| C167 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo thanh tab dưới (C166) nằm sát cạnh dưới màn hình, dễ chạm trúng vùng vuốt chuyển ứng dụng (home indicator gesture) của iPhone không có nút Home. | Nguyên nhân: thanh tab dùng `bottom-0` chạm thẳng mép an toàn của thiết bị, và `index.html` chưa khai báo `viewport-fit=cover` nên `env(safe-area-inset-bottom)` luôn trả về 0 (khai báo là điều kiện bắt buộc để giá trị này có tác dụng trên iOS Safari). **`index.html`**: thêm `viewport-fit=cover` vào thẻ `viewport`. **`Layout.tsx`**: khối icon bên trong thanh tab đổi `py-1` thành `pt-1 pb-[calc(0.5rem+env(safe-area-inset-bottom))]` — chừa thêm đúng khoảng vùng an toàn của thiết bị (0 trên Android/desktop, ~34px trên iPhone có home indicator) cộng thêm 0.5rem đệm cho thoáng, đẩy hẳn vùng bấm icon lên cao hơn khỏi dải vuốt cử chỉ; `<main>` cũng đổi `pb-20` thành `pb-[calc(5rem+env(safe-area-inset-bottom))]` để nội dung cuối trang không bị thanh tab (nay cao hơn) che khuất. Đã build kiểm tra class `safe-area-inset-bottom` có xuất hiện đúng trong CSS bundle sinh ra; `tsc`/`npm run build`/`npm test` đều pass. Chưa tự đo được khoảng cách thực tế trên iPhone thật (không có thiết bị/trình duyệt thật trong môi trường agent) — cần người dùng xác nhận lại trên iPhone sau khi deploy. |
| C168 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo header đang bị tai thỏ (notch/Dynamic Island) iPhone che khuất khi mở app dạng PWA cài đặt, muốn dịch header xuống. | Nguyên nhân giống hệt thanh tab dưới ở C167 nhưng ở cạnh trên: header `sticky top-0` chưa trừ vùng an toàn phía trên (chỉ lộ ra khi chạy standalone/full-screen sau khi cài PWA — Safari mở tab thường tự chừa chỗ, nhưng chế độ `display: standalone` thì không). **`Layout.tsx`**: thêm `pt-[env(safe-area-inset-top)]` vào chính thẻ `<header>` (ngoài `div` nội dung có `py-3` sẵn) — nền header vẫn phủ lên tới đỉnh màn hình (không hở màu nền trang phía sau tai thỏ), còn nội dung (logo/nút) tự động đẩy xuống dưới vùng tai thỏ. Cùng cơ chế `env(safe-area-inset-*)` đã bật từ C167 (`viewport-fit=cover` trong `index.html`), không cần thêm cấu hình mới. Build/test đều pass. Chưa tự đo được trên iPhone thật có notch/Dynamic Island (không có thiết bị/trình duyệt thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy, đặc biệt sau khi cài PWA lên màn hình chính (chế độ standalone). |
| C169 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm tính năng kéo màn hình xuống để làm mới dữ liệu (pull-to-refresh) kiểu Facebook, đặc biệt hữu ích khi mở app ở chế độ PWA cài đặt (không có pull-to-refresh gốc của trình duyệt). | Thêm `src/components/PullToRefresh.tsx`: theo dõi `touchstart`/`touchmove`/`touchend` trên `document` — chỉ bắt đầu kéo khi đang ở đúng đỉnh trang (`window.scrollY === 0`), kéo xuống dùng hệ số cản `RESISTANCE = 0.5` (cảm giác co giãn tự nhiên, không dịch 1:1 theo tay), giới hạn tối đa `MAX_PULL = 110px`; qua ngưỡng `PULL_THRESHOLD = 70px` mới nhả tay thì kích hoạt làm mới. Vì app có rất nhiều trang với luồng tải dữ liệu riêng (Dashboard, Ghi nhận, Điểm danh, Học sinh...), cách làm mới đơn giản và chắc chắn đúng ở MỌI trang mà không cần nối dây riêng từng nơi là gọi thẳng `window.location.reload()` — tải lại toàn trang, đi qua đúng service worker `NetworkFirst` cho API Supabase (đã cấu hình ở C164) nên vẫn ưu tiên lấy dữ liệu mới nhất khi có mạng. Chỉ gọi `event.preventDefault()` (nếu `cancelable`) khi `touchmove` đã xác nhận đang kéo xuống ở đỉnh trang, để tránh chặn nhầm việc cuộn trang bình thường. Có chỉ báo trực quan phía trên nội dung: mũi tên xoay dần theo % kéo, đổi thành icon quay tròn + chữ "Đang làm mới..." khi vượt ngưỡng. Gắn vào `Layout.tsx`, bọc quanh `<Outlet />` bên trong `<main>` nên áp dụng cho mọi trang đã đăng nhập cùng lúc, không cần sửa từng trang. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác kéo thật trên màn cảm ứng để kiểm tra cảm giác/độ nhạy (không có thiết bị cảm ứng thật trong môi trường agent) — cần người dùng tự thử trên điện thoại thật, đặc biệt xem có xung đột với cử chỉ vuốt xuống mở Trung tâm Thông báo trên đầu màn hình iPhone hay không. |
| C170 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn trang link hồ sơ học sinh (public, không qua `Layout.tsx`) có menu ẩn dạng "☰" giống style header trang chủ giáo viên (C160), cộng thêm thanh icon chọn mục cạnh dưới màn hình di động (giống C166), lưu tâm vùng tai thỏ và cử chỉ vuốt cạnh dưới iPhone (giống C167/C168). | **`StudentProfilePage.tsx`**: thêm `icon` (emoji) vào từng phần tử `PROFILE_SECTIONS` (🧾 Tóm tắt, ⭐ Ghi nhận, 🕘 Lịch sử, 📊 Điểm tuần, 👤 Cá nhân). Thay khối tiêu đề tĩnh + card `ProfileSectionNav` (luôn hiện 5 nút bấm dạng pill) bằng 1 thanh header `sticky top-0` riêng cho trang này (không dùng chung `Layout.tsx` vì đây là route public không qua layout xác thực) — brand bên trái, nút icon tròn "☰" bên phải mở dropdown liệt kê đúng 5 mục cũ (đóng khi bấm ra ngoài qua `menuRef` + `pointerdown` listener, cùng pattern với C158); xoá hẳn component `ProfileSectionNav` cũ vì không còn nơi nào gọi. Thêm `<nav>` cố định `fixed inset-x-0 bottom-0 md:hidden` bên dưới cùng, y hệt kiểu bottom tab bar ở `Layout.tsx` (C166): icon lớn + nhãn nhỏ cho cả 5 mục, tô xanh mục đang khớp `activeTab`, cuộn ngang được. Áp dụng luôn 2 bản vá an toàn thiết bị đã có: header thêm `pt-[env(safe-area-inset-top)]` (tránh tai thỏ/Dynamic Island, như C168), thanh tab dưới thêm `pb-[calc(0.5rem+env(safe-area-inset-bottom))]` (tránh dải cử chỉ chuyển ứng dụng iPhone, như C167), `<main>` thêm `pb-[calc(5rem+env(safe-area-inset-bottom))]` để nội dung không bị thanh tab che. Không đụng tính năng pull-to-refresh (C169) vì trang này không nằm trong `Layout.tsx`. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật trên iPhone/Android để kiểm tra bố cục và vùng an toàn (không có thiết bị thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy. |
| C171 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn trang link hồ sơ học sinh cũng có pull-to-refresh như C169 — trang này không đi qua `Layout.tsx` nên chưa tự có tính năng đó. | **`StudentProfilePage.tsx`**: import component `PullToRefresh` có sẵn từ C169 (`src/components/PullToRefresh.tsx`, không viết lại logic), bọc quanh `<section>` nội dung chính (giữ header `sticky` và thanh tab dưới `fixed` ở ngoài, không bọc — 2 khối đó không cần cuộn/kéo). Cùng cơ chế `window.location.reload()` khi kéo qua ngưỡng 70px như ở `Layout.tsx`. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác kéo thật trên thiết bị cảm ứng (không có trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy. |

| C172 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn form đề xuất của lớp trưởng (`rounded-lg border border-teal-300 bg-teal-100 p-4 shadow-sm` trong trang link hồ sơ) hỗ trợ chọn nhiều học sinh và nhiều danh mục cùng lúc (tránh phải lặp thao tác từng cặp học sinh-danh mục), và đổi cách nhập từ "dàn trải hết các trường cùng lúc" sang luồng từng bước có Tiếp theo/Quay lại để tránh bỏ sót trường. | **`StudentProfilePage.tsx`** (`LopTruongPanel`): đổi `selectedMaHs`/`selectedCatalog` (string đơn) thành `selectedStudents`/`selectedCatalogCodes` (mảng, chọn qua checkbox list `max-h-64 overflow-y-auto`) + `includeNewCategory` (checkbox riêng cho "đề xuất danh mục mới", tách khỏi việc chọn nhiều danh mục có sẵn). Thêm state `step: 'students' | 'catalog' | 'details' | 'review'` điều khiển 1 wizard 4 bước (chỉ hiện đúng 1 bước tại 1 thời điểm, có thanh chỉ số bước + nút "← Quay lại"/"Tiếp theo →", validate bắt buộc mỗi bước trước khi qua bước sau: bước 1 cần ≥1 học sinh, bước 2 cần ≥1 danh mục hoặc bật đề xuất mới, bước 3 nếu bật đề xuất mới thì bắt buộc nội dung). Bước 4 (Xác nhận) hiện tổng số đề xuất sẽ tạo = số học sinh × số danh mục (kể cả danh mục mới, tính +1), liệt kê tên học sinh/danh mục đã chọn, rồi bấm "Gửi N đề xuất" mới thật sự gọi API — hàm `submitAll` dựng mảng "combos" (mọi cặp học sinh×danh mục), gọi `dataSource.submitDeXuatGhiNhan` tuần tự cho từng combo (không dùng `Promise.all` để tránh làm nghẽn RPC Supabase khi số lượng lớn), có `submitProgress { done, total }` hiện tiến độ, đếm số lỗi để báo "Gửi được X/Y đề xuất" nếu có lỗi thay vì chỉ báo chung chung. Sau khi gửi xong reset toàn bộ state về bước 1. Không đụng `ProposalHistoryItem` (form sửa lại 1 đề xuất đã gửi) vì đó vẫn đúng là sửa 1 học sinh + 1 danh mục, không cần đổi. `tsc` (có bật `noUnusedLocals`)/`npm run build`/`npm test` đều pass, xác nhận không còn sót state single-select cũ. Chưa tự thao tác thật để kiểm tra cảm giác luồng 4 bước trên di động (không có thiết bị thật trong môi trường agent) — cần người dùng tự thử và góp ý nếu luồng bước chưa hợp lý. |
| C173 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn bước 4 (Xác nhận gửi) của wizard đề xuất lớp trưởng (C172) hiện rõ nội dung đã chọn theo từng học sinh cụ thể, không bắt học sinh phải tự suy luận từ con số "N học sinh × M danh mục" và 2 dòng liệt kê tên tách rời (dễ nhầm ai bị áp mục nào khi có nhiều học sinh/danh mục). | **`StudentProfilePage.tsx`** (`LopTruongPanel`, bước `review`): bỏ cách hiện phép tính trừu tượng + 2 dòng liệt kê tên tách rời, thay bằng danh sách từng học sinh — mỗi học sinh 1 khối riêng ghi rõ tên, bên dưới là `<ul>` liệt kê đúng các danh mục (mã + tên đầy đủ + điểm, không chỉ mã) sẽ áp cho riêng người đó (kể cả dòng "➕ Đề xuất danh mục mới · nhóm ..." nếu có bật, tra tên nhóm qua `NHOM_OPTIONS` thay vì hiện code thô). Vì hiện tại mọi học sinh được chọn đều nhận đúng cùng 1 tập danh mục (múc chọn ở bước 2 là chung, chưa hỗ trợ chọn khác nhau theo từng người), danh sách con lặp lại giống nhau cho mỗi học sinh — nhưng hiện tường minh dạng "học sinh A: mục X, mục Y" thay vì bắt tự suy ra từ phép nhân, đúng yêu cầu "không cần đoán hay suy luận". Thêm khối riêng hiện rõ "Áp dụng cho tất cả: ngày/tiết/môn" và dòng Nội dung (in nghiêng nếu có nhập, hoặc ghi chú rõ "chưa nhập, mỗi đề xuất sẽ tự lấy theo tên danh mục tương ứng" nếu để trống — đúng hành vi thật của `approveDeXuatGhiNhan` phía giáo viên duyệt, không phải suy đoán). `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật để xem giao diện có quá dài khi chọn nhiều học sinh/danh mục hay không (không có thiết bị thật trong môi trường agent) — cần người dùng tự thử với số lượng lớn (VD 10 học sinh) để góp ý nếu cần thu gọn thêm. |
| C174 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn Tiết/Môn trong form đề xuất lớp trưởng chuyển từ ô nhập tự do sang dropdown cố định: Tiết = 1-8 hoặc "Giờ ra chơi"; Môn = Toán, Lý, Địa, Văn, TVAV, ANH, KTPL, GDTC, Sinh hoạt dưới cờ (SHDC), Sinh hoạt chủ nhiệm (SHCN) — mỗi dropdown chỉ chọn được 1 giá trị. | **`StudentProfilePage.tsx`**: thêm hằng `TIET_OPTIONS` (mảng '1'..'8' + 'Giờ ra chơi') và `MON_OPTIONS` (10 mã môn đúng thứ tự người dùng liệt kê) + `MON_LABELS` (map riêng cho SHDC/SHCN hiện tên đầy đủ trong ngoặc, các môn còn lại hiện nguyên mã). Đổi cả 2 chỗ đang dùng `<input>` tự do cho tiết/môn thành `<select>`: form gửi đề xuất mới (bước 3 "Thông tin chung" của wizard) và form sửa đề xuất trong lịch sử (`ProposalHistoryItem`). Giá trị lưu vẫn là string tự do như cũ (cột `tiet`/`mon_hoc` không đổi kiểu dữ liệu), chỉ giới hạn UI nhập liệu còn đúng các lựa chọn hợp lệ, tránh gõ sai chính tả/định dạng tiết-môn không thống nhất giữa các lần nhập. `tsc`/`npm run build`/`npm test` đều pass. Đề xuất cũ đã lưu trước đây (nếu có tiết/môn dạng tự do khác các lựa chọn mới) khi mở form sửa sẽ không khớp lựa chọn nào trong dropdown — cần người dùng lưu ý khi sửa đề xuất cũ, có thể cần chọn lại đúng giá trị. |
| C175 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng báo trang xem học sinh (`TeacherStudentDetailPage.tsx`) có nhiều khối div lồng nhau bị chèn chữ/xuống dòng sai trên di động, nêu ví dụ khối `rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm` (thẻ tiêu đề hồ sơ), yêu cầu rà lại toàn trang và chọn cách làm tốt nhất cho di động. | Nguyên nhân gốc trùng với các lỗi header đã sửa trước đây (C157/C159): dự án đặt `--breakpoint-sm: 375px` (từ C008) nên mọi `sm:flex-row` chuyển sang xếp ngang ngay trên điện thoại thật (390-430px) thay vì đợi đến máy tính bảng, cộng thêm khối chứa text bên trái không có `min-w-0` nên trình duyệt không cho phép nó co lại/ngắt dòng đúng cách trong flex row (mặc định `min-width: auto` của flex item chặn wrap). Sửa tận gốc bằng 2 nguyên tắc áp dụng nhất quán cho mọi vị trí xếp "khối tiêu đề dài + 1 nút/badge ngắn cạnh bên" trong file: (1) đổi `sm:flex-row sm:items-*` thành `md:flex-row md:items-*` (mốc `md` 768px không bị override, mới thật sự "đủ rộng để xếp ngang"); (2) thêm `min-w-0` vào khối text bên trái và `shrink-0` vào nút/badge bên phải để 2 bên chia không gian đúng thay vì bên trái bị đẩy tràn. Áp dụng cho: tiêu đề trang (brand + nút "Xem link học sinh"/"Về danh sách"), thẻ tiêu đề hồ sơ học sinh (tên/avatar cạnh khối STT+Ghi nhận — khối STT+Ghi nhận đổi từ `min-w-40` cố định sang `w-full md:w-40` để chiếm trọn hàng riêng khi xếp dọc trên di động, gọn hơn hẳn khối nhỏ bị bóp), 3 tiêu đề section ("Ghi nhận của học sinh", "Liên lạc phụ huynh", "Chỉnh sửa thông tin" đều có nút hành động cạnh bên), khối đề xuất lớp trưởng (`TeacherProposalItem`, thêm `wrap-break-word` vào các dòng text bên cạnh badge trạng thái), và `PhoneRow` (tên phụ huynh dài cạnh số điện thoại). Không đụng các bảng dữ liệu (ghi nhận) vì đã dùng đúng `overflow-x-auto` (cuộn ngang) từ trước — đó là cách xử lý bảng chuẩn, khác với lỗi "flex bị bóp chữ" đang sửa. `tsc`/`npm run build`/`npm test` đều pass. Chưa tự thao tác thật trên điện thoại để kiểm tra bố cục mới (không có thiết bị thật trong môi trường agent) — cần người dùng xác nhận lại sau khi deploy, đặc biệt các thẻ có tên phụ huynh/nội dung dài. |
| C176 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng gửi ảnh chụp cho thấy tên học sinh vẫn bị cắt "Vũ Trần Khá..." trong thẻ tiêu đề hồ sơ (`rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm`) dù đã báo đã sửa ở C175 — vì C175 mới chỉ commit local, chưa `push` nên bản đang chạy thật vẫn là code cũ. | Đã push C170-C175 lên `origin/master`. Đồng thời làm chắc thêm 1 lớp phòng ngừa tại đúng chỗ người dùng chụp ảnh: `<h3>` tên học sinh trong thẻ tiêu đề đổi từ `truncate` (cắt + "...", luôn mất chữ khi hẹp bất kể nguyên nhân gì) sang `wrap-break-word` (cho xuống dòng khi cần, không bao giờ mất chữ) + cỡ chữ giảm `text-xl` trên di động, giữ `sm:text-2xl` từ tablet trở lên — tên học sinh dù dài cỡ nào cũng luôn đọc được đầy đủ, chỉ đánh đổi bằng việc thẻ có thể cao thêm 1 dòng khi tên rất dài, chấp nhận được vì thà xuống dòng còn hơn mất thông tin tên học sinh. `tsc`/`npm run build`/`npm test` đều pass. |
| C177 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Ảnh chụp cho thấy tên vẫn bị cắt ở khối `rounded-lg border border-sky-200 bg-sky-100 p-4 shadow-sm` — hoá ra đây là component `StudentProfileHeader` trong `StudentProfilePage.tsx` (trang link công khai học sinh xem), KHÔNG phải `TeacherStudentDetailPage.tsx` mà C175/C176 đã sửa (2 file trùng tên class ngẫu nhiên). Người dùng yêu cầu bố cục lại hẳn layout thay vì tiếp tục vá vì bề ngang di động quá hạn chế. | **`StudentProfilePage.tsx`**: sửa đúng `StudentProfileHeader` — layout cũ `flex items-center gap-3` không có nhánh xếp dọc nào cả (không phải lỗi breakpoint mà là hoàn toàn thiếu responsive), nên tên luôn bị bóp bởi avatar + khối "N ghi nhận" cố định bên phải. Bố cục lại: `flex-col md:flex-row md:items-center md:justify-between` — trên di động, avatar+tên nằm 1 hàng riêng (được toàn bộ bề ngang thẻ), khối số liệu "N ghi nhận" xuống hàng dưới hiện dạng ngang gọn (`flex items-center gap-4`) thay vì ép chung 1 hàng với tên; từ `md:` trở lên khôi phục đúng bố cục ngang cũ. Tên đổi từ `truncate` sang `wrap-break-word` (không bao giờ mất chữ). Nhân dịp rà toàn bộ file (đúng như người dùng yêu cầu "rất nhiều hiện tượng này trong trang đó"), sửa thêm 6 vị trí cùng lỗi: `ProfileSectionHeader` (tiêu đề + nút Mở rộng/Thu gọn dùng chung cho mọi section), `FeaturedRecords`/`RecordHistory` (tiêu đề dài + `ImpactSummary`), `ScoreSummary` (tiêu đề + hộp xếp loại), `ProfileCard` (tên ở tab Cá nhân), `ProposalHistoryItem` (tên/nội dung đề xuất cạnh badge trạng thái) — đều theo cùng công thức: đổi `sm:` → `md:` cho các cặp tiêu đề dài + khối cố định bên cạnh (băng qua đúng ngưỡng `--breakpoint-sm: 375px` bị override từ C008), thêm `min-w-0`/`wrap-break-word` vào khối text, `shrink-0` vào khối cố định. `tsc`/`npm run build`/`npm test` đều pass. |
| C178 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn bố cục lại lưới điểm 5 cột (`grid gap-px bg-amber-200 sm:grid-cols-5` trong `ScoreSummary`, tab "Điểm tuần" của trang thông tin học sinh) cho hợp di động — 5 cột (Chuyên cần/Vệ sinh/Nề nếp/Kỷ luật/Học tập) nhồi vào 1 hàng ngay từ `sm:` (375px, kích hoạt trên hầu hết điện thoại thật) khiến mỗi cột chỉ còn ~70-80px, nhãn và số bị bóp. | **`StudentProfilePage.tsx`** (`ScoreSummary`): đổi `sm:grid-cols-5` (kích hoạt quá sớm trên điện thoại) thành `grid-cols-2 md:grid-cols-5` — mặc định (mọi điện thoại) xếp 2 cột/hàng (5 mục thành 2-2-1, mục thứ 5 tự xuống hàng riêng, thoáng hơn hẳn 5 cột chật hay 1 cột dài lê thê), chỉ về đúng 5 cột 1 hàng khi màn hình đủ rộng thật (`md:` 768px). Giảm nhẹ `p-4` → `p-3 sm:p-4` và cỡ chữ số điểm `text-2xl` → `text-xl sm:text-2xl` để mỗi ô 2-cột trên điện thoại nhỏ (ví dụ 360px) vẫn đủ chỗ, tránh số/nhãn tràn ra ngoài ô. `tsc`/`npm run build`/`npm test` đều pass. |
| C179 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thiết kế lại nút "Thu gọn"/"Mở rộng" (`inline-flex h-9 shrink-0 items-center justify-center rounded-md border border-slate-300 bg-white px-3 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-100`) trong `ProfileSectionHeader` của trang thông tin học sinh thành 1 icon nhỏ ở góc thay vì chữ nhìn thô — làm trước 1 chỗ, nếu ổn thì triển khai cho tất cả các nơi dùng cùng mẫu nút "Thu gọn"/"Mở rộng" trong app. | **`StudentProfilePage.tsx`** (`ProfileSectionHeader`): đổi nút chữ thành nút tròn nhỏ `h-8 w-8 rounded-full` chỉ chứa icon mũi tên "▾" (không còn chữ "Thu gọn"/"Mở rộng"), xoay 180° (`rotate-180`) khi đang mở để chỉ hướng "thu gọn lại", giữ nguyên hướng khi đang gọn để chỉ hướng "mở ra" — vẫn giữ `aria-label`/`aria-expanded` để đọc được bằng screen reader dù không còn chữ hiển thị. Bố cục tiêu đề đổi từ `flex-col md:flex-row` (từng phải xếp dọc trên di động vì nút chữ dài) về lại 1 hàng ngang cố định `flex items-center justify-between` — vì nút icon giờ rất nhỏ (32px), không còn cần xếp dọc để tránh chèn tiêu đề nữa, gọn hơn hẳn. `tsc`/`npm run build`/`npm test` đều pass. Các nơi khác đang dùng nút chữ "Thu gọn"/"Mở rộng" tương tự (`DashboardPage.tsx` — 3 chỗ: `SectionHeader` dùng chung, khối điểm thi đua, `GroupViolationView`; `StudentsPage.tsx` — nút thu gọn danh sách chính và nút "Chi tiết"/"Thu gọn" theo từng dòng) CHƯA đổi — đang chờ người dùng xác nhận thiết kế icon này ổn rồi mới triển khai đồng loạt như đã hẹn. |
| C180 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Sau C179, người dùng thấy `ProfileSectionHeader` (`flex items-center justify-between gap-2`) nhìn chưa nổi bật, không phân biệt được với vùng nội dung phía dưới; sau khi thử bản nền trắng viền xám, người dùng góp ý luôn muốn nền màu đậm hẳn cho thật nổi bật thay vì chỉ viền nhạt. | **`StudentProfilePage.tsx`** (`ProfileSectionHeader`): đổi hẳn thành thanh tiêu đề nền xanh đậm đặc `rounded-lg bg-blue-700 px-4 py-2.5 shadow-sm`, chữ tiêu đề đổi sang `text-white` cho tương phản rõ — dùng chung 1 màu xanh brand cho mọi section bất kể section đó bên dưới màu gì (sky/blue/emerald/amber/violet), nổi bật tách biệt hẳn khỏi nền `bg-slate-200` của trang. Nút icon tròn đổi thành `bg-white/20` (nền trắng bán trong suốt) + `text-white`, nổi rõ trên nền xanh đậm mà không cần thêm viền. `tsc`/`npm run build`/`npm test` đều pass. |
| C181 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng duyệt mẫu icon+nền đậm ở C179/C180, yêu cầu áp dụng cho tất cả các chỗ "Thu gọn"/"Mở rộng" còn lại trong app, màu nền lấy theo tông màu riêng của từng section cho hợp gu (không dùng chung 1 màu xanh). | **`DashboardPage.tsx`**: thêm `SectionTone` + `SECTION_TONE_BAR` (map tone → `bg-{màu}-700`, các tông: slate/sky/cyan/indigo/violet/amber/emerald). `SectionHeader` (component dùng chung 5 section: Tóm tắt nhanh=slate, Bộ lọc thời gian=sky, Thống kê tổng quan=cyan, Sự kiện của lớp/tổ=amber, Nhật ký theo ngày=emerald) đổi hẳn cấu trúc: thanh tiêu đề nền đậm theo tone + nút icon tròn thay chữ, mô tả dài tách xuống dưới thanh màu (không nhét chung vào nền đậm vì mô tả nhiều dòng). Khối "Điểm thi đua học sinh" (indigo) và `GroupViolationView`/"Xem theo Nhóm vi phạm" (violet) không dùng chung `SectionHeader` nên sửa tay riêng theo đúng công thức, phần checkbox "Hiện cả học sinh không vi phạm" tách khỏi thanh màu, đặt xuống dòng riêng bên dưới mô tả. **`StudentsPage.tsx`**: header "Danh sách học sinh (N)" đổi tương tự, tone slate (card trung tính). Cố tình KHÔNG đổi nút "Chi tiết"/"Thu gọn" theo từng dòng học sinh trong bảng — đây là action nhỏ cấp hàng bảng giống hệt các nút "Sửa"/"Xoá" cạnh nó, không phải thanh tiêu đề section, đổi riêng nó sẽ lệch tông với các nút hàng khác. `tsc`/`npm run build`/`npm test` đều pass. |
| C182 | ⛔ Bị chặn (cần xác nhận) | 26/07/2026 | Người dùng muốn thêm hệ thống đăng nhập/đăng xuất cho trang công khai hồ sơ học sinh (`/hs/:token`), dựa trên SĐT 1 của học sinh: username = SĐT 1, mật khẩu = 3 số cuối của chính SĐT 1 đó. | Xác thực làm ở tầng server (Postgres `security definer`), không chỉ chặn trên UI — vì RPC `lay_ho_so_cong_khai` cũ chỉ cần đúng `token` là trả toàn bộ dữ liệu, nếu chỉ thêm màn hình đăng nhập ở React thì dữ liệu vẫn lộ qua network request đầu tiên. **Migration mới** `20260725000500_dang_nhap_ho_so_cong_khai.sql`: drop hàm cũ `lay_ho_so_cong_khai(text)`, tạo lại với 3 tham số `(p_token, p_sdt, p_mat_khau)` — chuẩn hoá cả số nhập lẫn số lưu (`regexp_replace(..., '\D', '', 'g')`, chỉ giữ chữ số) trước khi so sánh để tránh sai lệch do khoảng trắng/dấu gạch ngang; so `p_sdt` với `sdt_1` và `p_mat_khau` với `right(sdt_1, 3)`, sai 1 trong 2 đều trả rỗng (không lộ sai ở đâu, đúng pattern PIN lớp trưởng đã dùng). Vẫn giữ nguyên việc KHÔNG trả `sdt_1`/`sdt_2` thật trong kết quả (bảo mật số điện thoại phụ huynh) — số điện thoại chỉ dùng để xác thực, không hiển thị lại. **`DataSource.ts`/`SupabaseDataSource.ts`**: `getPublicStudentProfile(token)` đổi thành `getPublicStudentProfile(token, sdt, matKhau)`; xoá `getStudentByToken` (không còn ai gọi, và không thể giữ nguyên logic không-cần-mật-khẩu nữa). **`StudentProfilePage.tsx`**: thêm state `'login'` vào `ProfileState`, hiện form đăng nhập (SĐT + 3 số mật khẩu) khi chưa xác thực; đăng nhập thành công lưu `{ sdt, matKhau }` vào `sessionStorage` (khoá riêng theo từng `token`, tự xoá khi đóng tab/trình duyệt — không dùng `localStorage` để tránh đăng nhập "vĩnh viễn" trên máy dùng chung) để refresh trang trong cùng phiên không phải nhập lại; mở lại link sau khi đóng trình duyệt thì phải đăng nhập lại. Thêm nút "Đăng xuất" trong menu ☰ sẵn có (xoá `sessionStorage`, quay lại form đăng nhập). Nút ☰ chỉ hiện khi đã đăng nhập thành công. **Lưu ý quan trọng**: học sinh nào chưa có SĐT 1 trong hệ thống sẽ KHÔNG đăng nhập được vào chính hồ sơ của mình nữa (trước đây chỉ cần link là xem được) — cần rà lại toàn bộ 36 học sinh đã có đủ SĐT 1 chưa trước khi áp dụng migration này lên Supabase thật, nếu thiếu phải bổ sung trước. `tsc`/`npm run build`/`npm test` đều pass. Chưa test được luồng đăng nhập/đăng xuất thật (cần áp dụng migration lên Supabase trước, agent không có trình duyệt thật để kiểm tra) — cần người dùng dán migration vào Supabase SQL Editor rồi tự thử đăng nhập bằng SĐT thật của 1 học sinh. |
| C183 | ⛔ Bị chặn (cần xác nhận) | 28/07/2026 | Người dùng muốn mở rộng quyền "Nhập đề xuất ghi nhận cho lớp" (gửi đề xuất ghi nhận qua link hồ sơ công khai) — hiện chỉ dành cho học sinh giữ chức vụ "Lớp trưởng" — để áp dụng thêm cho "Lớp phó học tập". | Rà bằng `grep` xác nhận chuỗi `'Lớp trưởng'` chỉ xuất hiện đúng 1 lần trong toàn bộ migration SQL, nằm trong mệnh đề `where` của hàm `_qlhs_lop_truong_ma_hs` (helper xác thực token+PIN dùng chung cho mọi RPC liên quan: `xac_thuc_pin_lop_truong`, `lay_du_lieu_lop_truong`, `gui_de_xuat_ghi_nhan`, `sua_de_xuat_ghi_nhan`, `xoa_de_xuat_ghi_nhan`, `lay_lich_su_de_xuat_lop_truong`) — nên chỉ cần sửa đúng 1 chỗ là quyền mới lan ra toàn bộ 6 RPC mà không phải sửa từng cái. **Migration mới** `20260726000100_mo_rong_quyen_lop_pho_hoc_tap.sql`: `create or replace function _qlhs_lop_truong_ma_hs` giữ nguyên chữ ký, chỉ đổi điều kiện `bcs.chuc_vu = 'Lớp trưởng'` thành `bcs.chuc_vu in ('Lớp trưởng', 'Lớp phó học tập')`. **`StudentProfilePage.tsx`**: đổi hằng số `LOP_TRUONG_ROLE` (dùng đúng 1 lần, để gate hiển thị `<LopTruongPanel>`) thành mảng `LOP_TRUONG_ELIGIBLE_ROLES = ['Lớp trưởng', 'Lớp phó học tập']`, điều kiện hiển thị đổi từ `state.role === LOP_TRUONG_ROLE` sang `LOP_TRUONG_ELIGIBLE_ROLES.includes(state.role)`. **`TeacherStudentDetailPage.tsx`**: giữ nguyên `LOP_TRUONG_ROLE` (còn dùng ở chỗ khác trong file) và thêm mới `LOP_TRUONG_ELIGIBLE_ROLES`, áp dụng ở 3 chỗ — logic lưu `ma_pin` trong `saveRole()` (chỉ giữ/lưu PIN khi chức vụ chọn nằm trong danh sách được phép, thay vì chỉ đúng "Lớp trưởng"), đoạn mô tả hướng dẫn dưới tiêu đề "Chức vụ trong lớp" (cập nhật nêu cả 2 chức vụ), và điều kiện hiện ô nhập "Mã PIN" trong form gán chức vụ (hiện cả khi chọn "Lớp phó học tập", không chỉ "Lớp trưởng"). `tsc`/`npm run build`/`npm test` (10/10) đều pass. Migration mới cần người dùng dán vào Supabase Dashboard SQL Editor thì quyền mới có hiệu lực thật — trước đó "Lớp phó học tập" dù đăng nhập đúng SĐT/mật khẩu vẫn sẽ không thấy được form gửi đề xuất vì RPC nền chưa cập nhật. |
---

**Chú thích trạng thái**: `☐ Chưa làm` / `🔄 Đang làm` / `✅ Xong` / `⛔ Bị chặn (cần xác nhận)`
